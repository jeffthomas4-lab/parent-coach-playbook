// Leftover scratch file from an interactive verification session (used to
// manually exercise the exported functions in offsite-backup-upload.mjs and
// offsite-backup-retrieve-verify.mjs against a broken local vitest/rolldown
// native binding, since `npx vitest run` could not execute in that sandbox).
// It is not referenced anywhere and is safe to delete manually; the
// mounted-drive deletion block prevented removing it programmatically.
// The real, permanent test coverage lives in tests/offsite-backup.test.ts.
export {};
