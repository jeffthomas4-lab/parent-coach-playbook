# Security Audit — parent-coach-desk (parentcoachdesk.com)

Date: 2026-07-05 (re-check pass per the consultant-tightened Pillar 1; prior full confirm 2026-06-27, no changes since 2026-06-25 audit)
Stack: Cloudflare Pages + Astro SSR (Cloudflare adapter), D1, R2.
Data home: D1 `activity-radar` (database_id 8cc3694a-26f8-4a56-b131-d5d3a68c49ef) holds organizations, programs, camp_claims, camp_reviews, submitters, search_events, geocoded_addresses, domain_quality, org_claims, org_suggestions. R2 `activityradar-photos` holds org logos and program photos. No KV.

**2026-07-13 note (ActivityRadar merge):** ActivityRadar folded into this repo as its camp data layer (schema history, discovery pipeline, two Cloudflare Workers — `activityradar-enrichment`, `activityradar-yelp` — writing to this same D1). ActivityRadar's own `SECURITY-AUDIT.md` is archived at `activityradar-archive/`, historical record only. Neither Worker has a secrets inventory or gitleaks pass of its own yet — flagged as an open item, not a Critical (they don't touch customer email or handle payment data).

## Secrets inventory (added 2026-07-05, per the tightened Pillar 1)

| Secret | Where it lives | Last rotated | Note |
|--------|----------------|--------------|------|
| `BULK_IMPORT_TOKEN` | Pages secret (`parent-coach-playbook`) | Unknown — needs Jeff to confirm the date it was set and rotate if past a year | Used by `scripts/import-camps-csv.mjs --auto-approve`. Compared with plain `===`, not constant-time (existing LOW finding below). |
| `CRON_KEY` | Pages secret (`parent-coach-playbook`) | Confirmed 2026-07-14 via `npx wrangler pages secret list --project-name parent-coach-playbook` — listed as `Value Encrypted` alongside `BULK_IMPORT_TOKEN` and `GITHUB_TOKEN`. Value itself was not printed, per the security gate. | Resolved. The Pages secret exists. Still open: the matching GitHub Actions repo secret for `.github/workflows/camps-sweep-cron.yml` is **not** set (`gh secret list --repo jeffthomas4-lab/parent-coach-playbook` returned empty) — the daily cron workflow still cannot call the route. Rotating/setting that secret needs the plaintext value, which this session did not generate or expose; Jeff needs to set `gh secret set CRON_KEY --repo jeffthomas4-lab/parent-coach-playbook` with the same value as the Pages secret (or rotate both together). |
| `ADMIN_EMAILS` | Plaintext var in `wrangler.jsonc` (not a secret — this is an allowlist, not a credential) | N/A | Correctly a var, not a secret. No action needed. |

**Gitleaks scan:** not run this session. A full-history scan (`gitleaks git .`) needs a working shell in this repo's git history, which was not available in this session. Flagged as an open item — run it and log the scan date here before this row can go back to a clean pass.

## Findings from the 2026-07-05 re-check

## Critical / High findings
None. This repo is in good shape. The admin surface is gated by Cloudflare Access + an email allowlist + a same-origin (CSRF) check, every D1 query is parameterized, and error responses are generic.

## Medium / Low findings (flagged, not changed)
- LOW — admin-auth.ts decodes the Cloudflare Access JWT without verifying its signature (it trusts that only CF Access can mint the `CF_Authorization` cookie for the domain). The code already flags this as a phase-2 hardening item: verify against the Access JWKS. Acceptable while the route sits behind an Access policy. Do not ship admin routes without the Access policy actually attached in the Cloudflare dashboard.
- LOW — `BULK_IMPORT_TOKEN` in `/api/camps/submit` is compared with plain `===` (not constant-time). It auto-approves listings, not money or PII, so impact is low. Fine to leave.

## 11-point gate
1. Privacy policy — PASS. `/disclosure/` is the "privacy and disclosure page" (linked from `/terms/`); states what data is collected and how the site is funded. Data lives in D1 `activity-radar` + R2 `activityradar-photos`.
2. No client-side DB access — PASS. All D1/R2 access is server-side (Astro SSR + API routes). Admin pages and `/api/admin/*` call `requireAdmin` (CF Access email vs `ADMIN_EMAILS` allowlist). Row-returning public routes scope by slug/id; no whole-table dump is exposed to the public.
3. Failure paths — PASS. Duplicate-email submitter handled via upsert; existing-claim and already-claimed states return clean 400/409; invalid JSON/missing fields return 400; tampered/expired admin cookie returns 401/403; honeypot short-circuits bot submits to 200.
4. Security headers — PASS (already present). `public/_headers` sets HSTS, X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy, COOP, and a hashed Content-Security-Policy. Admin paths set `no-store` + noindex.
5. OWASP (injection/XSS/auth) — PASS. Every D1 statement uses `.prepare(...).bind(...)`. The one dynamic SQL builder (`updateCamp`) assembles only fixed, allowlisted column names — values stay bound. CSP + Astro auto-escaping cover XSS. Auth via CF Access + allowlist.
6. Server-side validation — PASS. `/api/camps/submit` and `/api/admin/camps/[id]/update` validate types, integer/date formats, enums, length bounds, age ranges, and email format server-side, independent of any client JS.
7. No leaks — PASS. No secrets in the frontend bundle; API responses return scoped fields; full errors go to `console.error` server logs only.
8. No API keys in frontend — PASS. No third-party API keys; Nominatim geocoding needs none. `BULK_IMPORT_TOKEN`/`CRON_KEY` are server-side env/secrets.
9. Rate limits on paid routes — N/A / WAIVED. No paid third-party API is called from a public route. Geocoding (free Nominatim) is cached in D1 and only runs on submit/admin-edit. Cron route is key-gated. If a paid API is ever added, add a limiter (flag).
10. Turnstile + CORS — PARTIAL. CORS: PASS — no route emits `Access-Control-Allow-Origin`, and admin/mutating POSTs enforce same-origin via `requireSameOrigin`. Turnstile: WAIVED — public forms rely on a honeypot today; adding a Turnstile widget needs site keys and is out of scope for this safe-fix pass. Recommend adding before launch.
11. Generic errors to user — PASS. Client sees `{ ok:false, error:'forbidden' }`-style generic messages; SQL/stack stays in server logs.

## Changes made this session (2026-06-27)
None. No code changes were needed in this repo.

## Changes made this session (2026-07-05)
- Added the secrets inventory table above (Pillar 1 was tightened 2026-07-05 to require one).
- Found that `CRON_KEY` is referenced in code but was never documented as a configured secret. Not a vulnerability (the route fails closed), but it means the Pillar 8 business-metric alert and stale-camp sweep have likely never actually run on a schedule. See `STANDARD-AUDIT.md` Pillar 8 open items.
- No gitleaks scan run this session (no shell access). Flagged as an open item, not marked pass.
- No Critical or High findings. The 11-point gate below is unchanged from the 2026-06-27 pass.

## Changes made this session (2026-07-14)
- Confirmed `CRON_KEY` exists as a live Cloudflare Pages secret (see secrets inventory row above) — this session had shell access, unlike prior passes. Value not printed, per the security gate item 3 instruction. Found in the same pass that the GitHub Actions repo secret of the same name is still unset, so the cron workflow remains non-functional pending Jeff setting it.
- `npm audit`: fixed the critical `vitest` CVE chain (bumped `vitest` 2.1.4 → 4.1.10) and the `uuid`/`gaxios`/`@astrojs/tailwind` moderate/low chain (bumped `linkinator` 6.1.2 → 7.6.1, its only consumer). Verified with a full green test run (107/107) after each bump. 14 vulnerabilities → 8, all remaining. See `STANDARD-AUDIT.md` open item #13 for the waiver reasoning on the remaining 8 (they require a major Astro v5→v7 upgrade with a Content Layer API migration across 15 content collections — out of scope for a same-session safe-fix pass).
- Wrote the 3-test-minimum suite (happy/failure/auth) for all 14 previously-untested API routes flagged in open item #5. All 107 tests pass (35 pre-existing + 72 new). See `STANDARD-AUDIT.md` Pillar 9 for the full list.
- `npm run check` (astro check): 215 → 0 type errors (the 245 cited in the prior session had already dropped to 215 by this session's start). Root causes and fixes are detailed in `STANDARD-AUDIT.md` open item #14. One fix (adding `team-parent` to the shared `PILLARS` object so `ArticleLayout.astro`'s phase lookup is exhaustively typed) initially broke the homepage's "three drives" section and `ThreeDrivesNav.astro`, both of which assumed `Object.values(PILLARS)` was always exactly 3 items — caught by a full `npm run build`, not just the type checker, and fixed by making both call sites reference the three drive keys explicitly instead of iterating the whole object. Full build (`npm run build`) verified clean afterward, all pages render, homepage's three-drives section confirmed to still render exactly 3 cards.
- Gitleaks still not run (no gitleaks binary available in this session's shell). Still an open item.

## Changes made this session (2026-07-15) — Security & Revenue lane

**Hardcoded live OpenAI key — FIXED (sanitize), key itself still burned pending rotation.** `gen_hero_image.py` at the Cowork root (outside this repo) had a live OpenAI API key hardcoded on line 4. Sanitized this session: the script now reads `OPENAI_API_KEY` from the environment and exits with a clear message if it's unset, instead of carrying a literal key. The same live key was also found in plaintext at `About Me/openai-config.md` line 3 — that file is outside this repo and outside this session's file lane, not edited, but it needs the same rotation treatment. **Jeff must rotate this key in the OpenAI dashboard; it is burned until he does.** See HANDOFF below.

**Secrets sweep — clean.** Full-tree pattern sweep across the Cowork working directory (OpenAI, Anthropic, AWS, GitHub, Slack, Stripe, Notion key shapes) plus a manual `git log --all -p` pass over this repo's full history (no gitleaks binary available, same gap as the 2026-07-05 and 2026-07-14 passes) turned up no other real hardcoded secret. `.env` is gitignored and confirmed never tracked (`git ls-files` shows only the placeholder `.env.example`, which carries no real values). No non-`PUBLIC_`-prefixed secret (`OPENAI_API_KEY`, `SANITY_API_TOKEN`, `CRON_KEY`, `BULK_IMPORT_TOKEN`) is referenced from any client-side script or `.astro` file that ships to the browser — every reference is in a server-side API route or SSR frontmatter. No `console.log`/`console.error` call prints a key or token value; the cron worker logs presence/absence and response status only. Every other regex hit (`AKIA…`, `sk_live_…`, `xoxb-…`) was a false positive: unrelated projects' test fixtures asserting a secret-scanner works, an AWS SDK test vector inside `.pnpm-store`, or a Slack setup doc's placeholder token format.

**Gitleaks:** still not run as an actual tool (binary unavailable). This session's manual full-history grep is a partial substitute, not a replacement — recommend Jeff install `gitleaks` locally and run `gitleaks git .` from the repo root at least once to close this item for real.

**`/what-to-buy/` stale build — diagnosed, not fixed (see HANDOFF, needs deploy access).** Confirmed live: `/what-to-buy/` and its guide pages serve `<meta name="generator" content="Astro v4.16.19">` while the homepage serves `v5.18.2`. This is not a Cloudflare edge-cache issue — `cf-cache-status: DYNAMIC` on both paths (checked via a live `fetch()` in-browser) means Cloudflare is not caching either page; every request hits the origin deployment directly. The shared `NavBar` component (same component, same `BUYING_GUIDES` data import, present on every page) renders a shorter, older guide list on `/what-to-buy/` (missing Rugby, Pickleball, Wrestling, and the entire "For coaches" nav section) than it does on the homepage — since one Astro build compiles one copy of a shared component, this proves `/what-to-buy/`'s HTML was baked by a genuinely older build than the homepage's HTML, not a template or content bug. No code-level exclusion explains it: the routes are wired normally in `astro.config.mjs`, no `public/` static file shadows them, `_headers`/`_redirects` carry no exception for this path, and the source (`src/data/site.ts`, `src/content/guides/`) is current on `main`. One concrete repo-side lead for whoever fixes this: `astro.config.mjs`'s `vite.cacheDir` is pinned to a fixed path (`/tmp/pcd-vite-verify-cache`, added 2026-07-10 in the ActivityRadar merge commit) instead of Vite's default `node_modules/.vite`. If the Cloudflare Pages build environment persists `/tmp` across builds, a stale Vite transform cache at that fixed path is a plausible mechanism for one build to silently reuse old output for some routes while freshly rebuilding others — worth trying as a fix (repoint or remove `cacheDir`) and reverifying. This session did not edit `astro.config.mjs`: the working tree was checked out on `migration/pages-to-workers-staging`, not `main`, and this session's hard rules forbid git checkout/build/deploy, so a fix here has to land on `main` and be verified with a real build, which is outside this session's tool access.

**`/go/` affiliate redirect — VERIFIED WORKING, no P0.** Followed one live `/go/` link per network in a real Chrome session:
- Amazon: `/go/baseball-bat-28in/` → landed on `amazon.com/dp/B0D6QPSW3X?tag=parentcoachpl-20` — correct product (Victus Vibe Pencil bat), associate tag `parentcoachpl-20` present in the URL bar.
- CJ / SoccerGarage (SoccerGarage is the only CJ-network merchant configured in this repo — `tkqlhce.com` is CJ's own click-tracking domain, so this one test covers both labels): `/go/soccer-goalie-gloves-youth/` → landed on `soccergarage.com/goalkeeping.html?cjevent=…` with a valid `cjevent` tracking parameter. Lands on the goalkeeping category page rather than one specific glove — matches the known SoccerGarage deep-linking limitation already logged in `AFFILIATE_MASTER_LIST.md`, not a broken link; the tracking parameter that carries commission credit is present and correct.
- Bookshop.org: `/go/book-changing-the-game/` → landed on the correct book's Bookshop page with `affiliate=125074` (matching the account ID in `src/data/affiliates.json`) and `utm_source=parentcoachdesk&utm_medium=affiliate&utm_campaign=books-sports-parenting` attached, per the non-Amazon/non-CJ UTM rule in `src/pages/go/[slug].astro`.

No missing or wrong tag on any of the three. This corroborates (independently, via live browser navigation rather than reading the automation's own report) what `pcd-link-health-monitor`'s 2026-07-13 report already claimed. **Verdict: the revenue mechanism works end to end for all three live networks. No P0 finding here.**
