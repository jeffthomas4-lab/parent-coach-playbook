---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import NewsletterSignup from '../components/NewsletterSignup.astro';
import LeadMagnetCTA from '../components/LeadMagnetCTA.astro';
import { PILLARS, SITE, CONTRIBUTORS } from '../data/site';

const all = (await getCollection('articles', ({ data }) => !data.draft))
  .sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime());

// Featured cornerstone (the manifesto). Pick the first essay marked featured.
const featured = all.find(a => a.data.featured && a.data.format === 'essay') ?? all[0];
// The rest of the feed: most recent, excluding the featured.
const feed = all.filter(a => a.slug !== featured?.slug).slice(0, 8);

const pillars = Object.values(PILLARS);
const contribs = Object.values(CONTRIBUTORS);
---
<BaseLayout>
  <!-- HERO -->
  <section class="border-b border-bone">
    <div class="container-px py-16 md:py-24 grid lg:grid-cols-12 gap-12 items-start">
      <div class="lg:col-span-8">
        <p class="font-display italic text-rust mb-5" style="font-weight: 500;">
          Hi. We're glad you're here.
        </p>
        <h1 class="font-display text-ink text-[2.5rem] md:text-[4.25rem] leading-[1.0]" style="font-weight: 500;">
          The drive home is the <em class="text-rust">real game</em>.
        </h1>
        <p class="font-display text-ink-soft text-xl md:text-2xl mt-8 max-w-2xl leading-snug">
          Sideline notes from parents who coach, organize, drive, snack-pack, and supervise the
          group chat. Mostly short reads. Sometimes longer ones. From three of us, to the rest of you.
        </p>
        <div class="mt-10 flex flex-wrap gap-3">
          <a href={`/${featured.data.phase}/${featured.slug}/`} class="btn">Read the cornerstone</a>
          <a href="/resources/drive-home-playbook/" class="btn btn-ghost">Get the free playbook</a>
        </div>
      </div>

      <aside class="lg:col-span-4 lg:pl-8 lg:border-l lg:border-bone">
        <p class="font-display italic text-rust mb-3" style="font-weight: 500;">Who writes here</p>
        <ul class="grid gap-4">
          {contribs.map((c) => (
            <li class="flex items-start gap-3">
              <div
                class="w-10 h-10 rounded-full flex items-center justify-center font-display italic flex-shrink-0"
                style={`background: ${c.accentBg}; color: ${c.accent}; font-weight: 500; font-size: 1rem;`}
                aria-hidden="true"
              >{c.avatarLetter}</div>
              <div>
                <a href="/contributors/" class="font-display text-ink hover:text-rust" style="font-weight: 500;">{c.name}</a>
                <p class="font-display text-ink-soft text-sm leading-snug">{c.short}</p>
              </div>
            </li>
          ))}
        </ul>
      </aside>
    </div>
  </section>

  <!-- FEATURED CORNERSTONE -->
  {featured && (
    <section class="border-b border-bone">
      <div class="container-px py-16">
        <p class="font-display italic text-rust mb-3" style="font-weight: 500;">If you only read one thing</p>
        <a href={`/${featured.data.phase}/${featured.slug}/`} class="block group max-w-3xl">
          <h2
            class="font-display text-ink text-4xl md:text-[3.5rem] leading-[1.05] group-hover:text-rust transition-colors"
            style="font-weight: 500;"
            set:html={featured.data.title.replace(/\*([^*]+)\*/g, '<em>$1</em>')}
          />
          {featured.data.dek && (
            <p class="font-display text-ink-soft text-xl mt-5 leading-snug max-w-2xl">
              {featured.data.dek}
            </p>
          )}
          <div class="t-cta mt-6">Read it →</div>
        </a>
      </div>
    </section>
  )}

  <!-- FEED -->
  <section class="border-b border-bone">
    <div class="container-px py-16">
      <div class="flex items-baseline justify-between mb-6">
        <p class="font-display italic text-rust" style="font-weight: 500; font-size: 1.05rem;">What's new</p>
        <a href="/newsletter/" class="t-cta">See the archive →</a>
      </div>
      <div class="grid md:grid-cols-2 gap-x-10">
        {feed.map((a) => (
          <ArticleCard
            href={`/${a.data.phase}/${a.slug}/`}
            contributor={a.data.contributor}
            format={a.data.format}
            sport={a.data.sport}
            age={a.data.age}
            phase={a.data.phase}
            title={a.data.title}
            dek={a.data.dek}
          />
        ))}
      </div>
    </div>
  </section>

  <!-- THE THREE DRIVES (softer) -->
  <section class="border-b border-bone">
    <div class="container-px py-16">
      <p class="font-display italic text-rust mb-3" style="font-weight: 500;">Three drives. One relationship.</p>
      <h2 class="font-display text-ink text-3xl md:text-4xl leading-[1.07] max-w-3xl" style="font-weight: 500;">
        The relationship gets built on either side of the game. Most of us forget that until Saturday at 3pm.
      </h2>

      <div class="mt-12 grid md:grid-cols-3 gap-6">
        {pillars.map((p, i) => (
          <a
            href={`/${p.slug}/`}
            class="block group p-7 transition-colors"
            style={`background: ${p.accentBg};`}
          >
            <div
              class="font-display italic"
              style={`color: ${p.accent}; font-size: 0.95rem; font-weight: 500;`}
            >
              {p.eyebrow.split(' / ')[1]}
            </div>
            <h3 class="font-display text-ink text-2xl mt-2 leading-tight group-hover:text-rust transition-colors" style="font-weight: 500;">
              {p.label}
            </h3>
            <p class="font-display text-ink-soft mt-3 leading-snug text-sm">
              {p.blurb}
            </p>
            <div
              class="font-display italic mt-5"
              style={`color: ${p.accent}; font-weight: 500;`}
            >
              Open this section →
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- LEAD MAGNET -->
  <LeadMagnetCTA />

  <!-- NEWSLETTER -->
  <NewsletterSignup
    headline="Slide into the inbox"
    blurb="One short note a week. From Maren, Dan, and Jeff. No fluff, no tracking, easy unsubscribe."
  />
</BaseLayout>
