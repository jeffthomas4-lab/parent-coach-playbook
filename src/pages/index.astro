---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import LeadMagnetCTA from '../components/LeadMagnetCTA.astro';
import { PILLARS, EDITORIAL, TOPICS } from '../data/site';
import { isLive, freshnessDate, wasUpdated } from '../lib/publishFilter';
import { renderTitle } from '../lib/title';

const all = (await getCollection('articles', ({ data }) => isLive(data)))
  .sort((a, b) => freshnessDate(b.data).getTime() - freshnessDate(a.data).getTime());

// "Pinned at the Desk" — three reads to surface above the fold each week.
// To curate, list slugs here. If a slug isn't found (typo, unpublished),
// it falls back to the most recent article so the section always renders
// three cards. Update weekly when the Friday Letter ships.
const PINNED_SLUGS: string[] = [
  // Add 3 article slugs here. Empty list = auto-fill from the 3 newest.
];
const pinned = PINNED_SLUGS
  .map(s => all.find(a => a.slug === s))
  .filter(Boolean) as typeof all;
const pinnedFinal = pinned.length === 3 ? pinned : all.slice(0, 3);

// Featured cornerstone (the long read). Pick the first essay marked featured.
const featured = all.find(a => a.data.featured && a.data.format === 'essay') ?? all[0];
// The centered feed: most recent, excluding the featured. Six cards.
const feed = all.filter(a => a.slug !== featured?.slug).slice(0, 6);
// The right rail: most recent, slim list. Five items, excludes featured.
const latestRail = all.filter(a => a.slug !== featured?.slug).slice(0, 5);

const drives = Object.values(PILLARS);

// Photo per drive, in order: before, during, after.
// Loaded from /public/illustrations/. Same aspect ratio across all three so the
// triptych reads as one composition.
const driveImages = [
  {
    src: '/illustrations/walk-to-the-field.webp',
    alt: 'A dad and son walking together through the parking lot toward the field at twilight.',
  },
  {
    src: '/illustrations/coach-setting-cones.webp',
    alt: 'A coach kneeling on the field at golden hour, setting up orange cones for a drill.',
  },
  {
    src: '/illustrations/the-car-ride-home.webp',
    alt: 'A dad driving with his young son in the passenger seat, soccer cleats in the foreground, sunset out the windshield.',
  },
];

// timeZone: 'UTC' is required because publishedAt in YAML frontmatter parses to
// midnight UTC, and the build runs in the deployer's local TZ. Without forcing
// UTC here, every date renders one day early for any deployer west of UTC.
const fmtDate = (d: Date) =>
  d.toLocaleDateString('en-US', { month: 'short', day: 'numeric', timeZone: 'UTC' });

// WebSite schema with SearchAction, on the homepage where Google looks for it.
// Moved here from /about/ (2026-06-10 SEO pass).
import { SITE } from '../data/site';
const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: SITE.name,
  url: SITE.url,
  description: SITE.description,
  publisher: {
    '@type': 'Organization',
    name: SITE.name,
    url: SITE.url,
  },
  potentialAction: {
    '@type': 'SearchAction',
    target: {
      '@type': 'EntryPoint',
      urlTemplate: `${SITE.url}/search/?q={search_term_string}`,
    },
    'query-input': 'required name=search_term_string',
  },
};
---
<BaseLayout>
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
  {/* SECTION 1 of 6: HERO + latest-reads rail (top right, above the fold) */}
  <section class="border-b border-bone relative overflow-hidden">
    {/* Hero photo + paper-tinted overlay so dark hero type stays readable. */}
    <img
      src="/illustrations/sports-family-sideline.webp"
      alt="A family of four sitting close together on the sideline at a youth game, smiling."
      class="absolute inset-0 w-full h-full object-cover"
      loading="eager"
      fetchpriority="high"
      decoding="async"
    />
    <div
      class="absolute inset-0"
      style="background: linear-gradient(to right, rgba(250,246,238,0.94) 0%, rgba(250,246,238,0.88) 55%, rgba(250,246,238,0.72) 100%);"
      aria-hidden="true"
    ></div>
    <div class="container-px py-14 md:py-20 relative">
      <div class="grid lg:grid-cols-12 gap-10 lg:gap-14 items-start">
        {/* Hero copy */}
        <div class="lg:col-span-8">
          <p class="font-display-soft text-rust mb-5" style="font-weight: 600; letter-spacing: 0.04em;">
            Hi. So glad you found us.
          </p>
          <h1 class="font-display-soft text-ink text-[2.25rem] md:text-[4rem] leading-[1.1]" style="font-weight: 700;">
            Every parent is a coach. Some on the field. <span class="font-display italic text-rust font-medium">All at the dinner table after.</span>
          </h1>
          <p class="font-display-soft text-ink-soft text-xl md:text-2xl mt-7 max-w-2xl leading-relaxed" style="font-weight: 400;">
            A warm, honest place for parents in the middle of it. Youth sports, dance, theater, band, ballet, choir, whatever your kid is into this season. Short reads, kind voice, real moments.
          </p>
          <div class="mt-9 flex flex-wrap gap-3">
            <a href="/start-here/" class="btn">Start here</a>
            <a href="/about/" class="btn btn-ghost">About this site</a>
            <a href="/resources/what-to-say-when/" class="btn btn-ghost">Get the free field guide</a>
          </div>
          <p class="font-display italic text-ink-soft text-sm mt-6 font-medium">
            Written by the <a href="/about/" class="hover:text-rust underline-offset-2 hover:underline">{EDITORIAL.byline}</a>.
          </p>
        </div>

        {/* Latest reads rail — top right, above the fold on desktop */}
        <aside class="lg:col-span-4">
          <p class="font-display italic text-rust mb-2 font-medium">Latest</p>
          <h2 class="font-display text-ink text-xl md:text-2xl leading-tight mb-4 font-medium">
            Right off the <em class="text-rust">desk</em>
          </h2>
          <ul class="border-t border-bone">
            {latestRail.map((a) => (
              <li>
                <a href={`/${a.data.phase}/${a.slug}/`} class="block py-3 border-b border-bone hover:bg-paper-warm transition-colors group">
                  <p class="font-display italic text-rust text-xs font-medium">
                    {a.data.topic ? TOPICS.find(t => t.slug === a.data.topic)?.label : 'General'} · {wasUpdated(a.data) ? 'Updated' : ''} {fmtDate(freshnessDate(a.data))}
                  </p>
                  <h3 class="font-display text-ink text-sm leading-snug mt-1 group-hover:text-rust transition-colors font-medium" set:html={renderTitle(a.data.title)} />
                </a>
              </li>
            ))}
          </ul>
          <p class="font-display italic text-rust text-sm mt-4 font-medium">
            <a href="/reads/" class="hover:underline">→ Browse the full archive</a>
          </p>
        </aside>
      </div>
    </div>
  </section>

  {/* PINNED AT THE DESK: three curated reads, sticky-note feel.
       Edit PINNED_SLUGS in the frontmatter to curate weekly.
       Defaults to the three most recent when no slugs are pinned. */}
  <section class="border-b border-bone" style="background: #F2E2D5;">
    <div class="container-px py-10 md:py-14">
      <p class="font-display italic text-rust mb-2 font-medium">Pinned at the Desk</p>
      <h2 class="font-display text-ink text-2xl md:text-3xl leading-tight max-w-2xl font-medium">
        What we're <em class="text-rust">reaching for</em> this week.
      </h2>
      <div class="mt-7 grid md:grid-cols-3 gap-px bg-bone">
        {pinnedFinal.map((p, i) => (
          <a
            href={`/${p.data.phase}/${p.slug}/`}
            class="bg-paper p-6 group block hover:bg-paper-warm transition-colors"
            style={`transform: rotate(${(i - 1) * 0.4}deg);`}
          >
            <p class="font-display italic text-rust mb-2 text-xs font-medium">Pinned</p>
            <h3
              class="font-display text-ink text-xl leading-tight group-hover:text-rust transition-colors font-medium"
              set:html={renderTitle(p.data.title)}
            />
            {p.data.dek && (
              <p class="font-display text-ink-soft text-sm mt-2 leading-snug">{p.data.dek}</p>
            )}
            <p class="font-display italic text-ink-soft text-xs mt-3 font-medium">
              {wasUpdated(p.data) ? 'Updated ' : ''}{fmtDate(freshnessDate(p.data))}
            </p>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* SECTION 2 of 6: THE THREE DRIVES (lead visual) */}
  <section class="border-b border-bone">
    <div class="container-px py-12 md:py-16">
      <div>
        <p class="font-display italic text-rust mb-2 font-medium">The frame</p>
        <h2 class="font-display text-ink text-3xl md:text-[2.75rem] leading-[1.05] font-medium">
          Every game is <em class="text-rust">three drives</em>.
        </h2>
        <p class="font-display text-ink-soft text-lg mt-3 max-w-2xl leading-snug">
          The car ride there. The game itself. The car ride home. Most of the work as a parent, whether you're their coach or not, happens on the way there or back.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-px bg-bone mt-8">
        {drives.map((p, i) => (
          <a
            href={`/${p.slug}/`}
            class="group block transition-colors overflow-hidden"
            style={`background: ${p.accentBg};`}
          >
            <div class="aspect-[3/2] overflow-hidden bg-bone">
              <img
                src={driveImages[i].src}
                alt={driveImages[i].alt}
                class="w-full h-full object-cover group-hover:scale-[1.02] transition-transform duration-500"
                loading="lazy"
                decoding="async"
              />
            </div>
            <div class="p-7 md:p-8">
              <div class="flex items-baseline gap-3 mb-4">
                <span class="font-display italic text-sm" style={`color: ${p.accent}; font-weight: 500;`}>
                  Drive {String(i + 1).padStart(2, '0')}
                </span>
                <span class="font-display italic text-ink-soft text-xs font-medium">
                  {p.eyebrow}
                </span>
              </div>
              <h3 class="font-display text-ink text-2xl md:text-3xl leading-tight group-hover:text-rust transition-colors font-medium">
                {p.label}
              </h3>
              <p class="font-display text-ink-soft text-sm mt-3 leading-snug">
                {p.blurb}
              </p>
              <p class="font-display italic text-sm mt-5" style={`color: ${p.accent}; font-weight: 500;`}>
                → Read the {p.label.toLowerCase()} pieces
              </p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* PARENT-COACH PILLAR — names the niche so they don't get lost in the broad welcome. */}
  <section class="border-b border-bone relative overflow-hidden" style="background: #2D2520; color: #FAF6EE;">
    {/* Empty-gym photo lives behind the dark overlay — quiet, "lineup at midnight" feel. */}
    <img
      src="/illustrations/empty-gym-after-practice.webp"
      alt="An empty high school gym in late afternoon light: basketball on the floor near the bench, Teamwork Respect Integrity banner on the wall."
      class="absolute inset-0 w-full h-full object-cover opacity-40"
      loading="lazy"
      decoding="async"
    />
    <div
      class="absolute inset-0"
      style="background: linear-gradient(to right, rgba(45,37,32,0.94) 0%, rgba(45,37,32,0.82) 55%, rgba(45,37,32,0.55) 100%);"
      aria-hidden="true"
    ></div>
    <div class="container-px py-14 md:py-20 relative">
      <div class="grid lg:grid-cols-12 gap-10 items-start">
        <div class="lg:col-span-7">
          <p class="font-display italic mb-3 font-medium" style="color: #D4AB6A;">If you're also on the bench</p>
          <h2 class="font-display text-3xl md:text-[2.75rem] leading-[1.05] font-medium" style="color: #FAF6EE;">
            Coaching your kid's team is its <em class="italic" style="color: #C5713D;">own job</em>.
          </h2>
          <p class="font-display text-lg mt-4 max-w-xl leading-snug" style="color: #DDD2BD;">
            Most clinics don't cover the part where it's also your kid. The lineup at midnight. The walk to the dugout. The drive home, knowing they're not getting the truth from anyone else either. We built this site partly because nobody was writing for the parent-coach honestly. This is where those pieces live.
          </p>
          <div class="mt-7 flex flex-wrap gap-3">
            <a href="/parent-coach/" class="btn" style="background: #C5713D; border-color: #C5713D; color: #FAF6EE;">
              The parent-coach pieces
            </a>
            <a href="/coaching-tips/" class="btn btn-ghost" style="color: #FAF6EE; border-color: #DDD2BD;">
              Drills you can run
            </a>
          </div>
        </div>
        <ul class="lg:col-span-5 grid gap-1">
          <li>
            <a href="/game/coaching-your-own-kid-in-front-of-the-team/" class="block py-4 border-b group transition-colors" style="border-color: #5F5448;">
              <span class="font-display italic text-xs" style="color: #D4AB6A;">Essay</span>
              <h3 class="font-display text-xl leading-tight mt-1 font-medium" style="color: #FAF6EE;">
                Coaching your own kid <em class="italic" style="color: #C5713D;">in front of the team</em>
              </h3>
              <p class="font-display text-sm mt-1 leading-snug" style="color: #DDD2BD;">
                I made the lineup at midnight. Five seasons later, I think I have it figured out. Sort of.
              </p>
            </a>
          </li>
          <li>
            <a href="/drive-home/three-drives-one-relationship/" class="block py-4 border-b group transition-colors" style="border-color: #5F5448;">
              <span class="font-display italic text-xs" style="color: #D4AB6A;">Framework</span>
              <h3 class="font-display text-xl leading-tight mt-1 font-medium" style="color: #FAF6EE;">
                Three drives. <em class="italic" style="color: #C5713D;">One relationship.</em>
              </h3>
              <p class="font-display text-sm mt-1 leading-snug" style="color: #DDD2BD;">
                Why what happens in the car matters more than what happens on the field.
              </p>
            </a>
          </li>
          <li>
            <a href="/drive-home/the-you-should-be-assistant-coaching-pressure/" class="block py-4 group transition-colors">
              <span class="font-display italic text-xs" style="color: #D4AB6A;">Decision</span>
              <h3 class="font-display text-xl leading-tight mt-1 font-medium" style="color: #FAF6EE;">
                The <em class="italic" style="color: #C5713D;">"you should be assistant coaching"</em> pressure
              </h3>
              <p class="font-display text-sm mt-1 leading-snug" style="color: #DDD2BD;">
                Coach asked. Two parents asked. You're considering it. Read this first.
              </p>
            </a>
          </li>
        </ul>
      </div>
    </div>
  </section>

  {/* SECTION 3 of 6: FOUR-TILE ENTRY GRID. The four situations that send parents to the site. */}
  <section class="border-b border-bone bg-paper-warm">
    <div class="container-px py-12 md:py-16">
      <p class="font-display italic text-rust mb-2 font-medium">For the moment you're in</p>
      <h2 class="font-display text-ink text-3xl md:text-4xl leading-[1.07] max-w-3xl font-medium">
        Tools, not <em class="text-rust">advice columns</em>.
      </h2>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-px bg-bone mt-8">
        <a href="/scripts/" class="bg-paper p-6 group block hover:bg-[#F5E9D2] transition-colors" style="border-top: 3px solid #D4AB6A;">
          <p class="font-display italic text-sm font-medium" style="color: #D4AB6A">Scripts</p>
          <h3 class="font-display text-ink text-xl leading-tight mt-2 group-hover:text-rust transition-colors font-medium">
            What to <em>actually</em> say.
          </h3>
          <p class="font-display text-ink-soft text-sm mt-3 leading-snug">
            The first 90 seconds after a bad game. The night before tryouts. The post-cut text.
          </p>
          <p class="font-display italic text-sm mt-4 font-medium" style="color: #D4AB6A">→ Browse the scripts</p>
        </a>

        <a href="/decisions/" class="bg-paper p-6 group block hover:bg-[#F2E2D5] transition-colors" style="border-top: 3px solid #C5713D;">
          <p class="font-display italic text-rust text-sm font-medium">Decisions</p>
          <h3 class="font-display text-ink text-xl leading-tight mt-2 group-hover:text-rust transition-colors font-medium">
            The big calls, walked through.
          </h3>
          <p class="font-display text-ink-soft text-sm mt-3 leading-snug">
            Travel ball or rec. Specialize or stay multi-sport. Quit or push through.
          </p>
          <p class="font-display italic text-rust text-sm mt-4 font-medium">→ Walk through a decision</p>
        </a>

        <a href="/youth-sports-pendulum/" class="bg-paper p-6 group block hover:bg-[#EAEFE7] transition-colors" style="border-top: 3px solid #5C7459;">
          <p class="font-display italic text-sm font-medium" style="color: #5C7459">Pendulum</p>
          <h3 class="font-display text-ink text-xl leading-tight mt-2 group-hover:text-rust transition-colors font-medium">
            Where is your family, right now?
          </h3>
          <p class="font-display text-ink-soft text-sm mt-3 leading-snug">
            Fun-to-performance spectrum. The check-in that pulls families back from the edge.
          </p>
          <p class="font-display italic text-sm mt-4 font-medium" style="color: #5C7459">→ Take the check-in</p>
        </a>

        <a href="/cost-calculator/" class="bg-paper p-6 group block hover:bg-paper-warm transition-colors" style="border-top: 3px solid #B0894A;">
          <p class="font-display italic text-sm font-medium" style="color: #B0894A">Cost calculator</p>
          <h3 class="font-display text-ink text-xl leading-tight mt-2 group-hover:text-rust transition-colors font-medium">
            What does a year <em>actually</em> cost?
          </h3>
          <p class="font-display text-ink-soft text-sm mt-3 leading-snug">
            Pre-filled with national medians at every tier. Edit any line. Honest math either way.
          </p>
          <p class="font-display italic text-sm mt-4 font-medium" style="color: #B0894A">→ Run your numbers</p>
        </a>
      </div>
    </div>
  </section>

  {/* COST CALCULATOR HERO — promote the most-shareable tool to its own block */}
  <section class="border-b border-bone relative overflow-hidden" style="background: #F5E9D2;">
    {/* Sunset-scoreboard photo behind a cream wash — sets the community-cost stakes. */}
    <img
      src="/illustrations/sunset-field-scoreboard.webp"
      alt="A community field scoreboard at sunset, parents watching from camping chairs along the sideline."
      class="absolute inset-0 w-full h-full object-cover"
      loading="lazy"
      decoding="async"
    />
    <div
      class="absolute inset-0"
      style="background: linear-gradient(to right, rgba(245,233,210,0.94) 0%, rgba(245,233,210,0.82) 55%, rgba(245,233,210,0.45) 100%);"
      aria-hidden="true"
    ></div>
    <div class="container-px py-14 md:py-20 relative">
      <div class="grid lg:grid-cols-12 gap-10 items-center">
        <div class="lg:col-span-7">
          <p class="font-display italic mb-3 font-medium" style="color: #B0894A;">Most-asked question on the site</p>
          <h2 class="font-display text-ink text-3xl md:text-[3rem] leading-[1.05] font-medium">
            What does a year of youth sports <em class="text-rust">actually cost</em>?
          </h2>
          <p class="font-display text-ink-soft text-lg md:text-xl mt-5 max-w-xl leading-snug">
            Pre-filled with national medians for every sport at every tier. Edit any line. The total updates as you go. Share the result with your spouse before the registration window closes.
          </p>
          <div class="mt-7 flex flex-wrap gap-3">
            <a href="/cost-calculator/" class="btn">Open the calculator</a>
            <a href="/cost-calculator/methodology/" class="btn btn-ghost">Where the numbers come from</a>
          </div>
        </div>
        <div class="lg:col-span-5">
          <div class="border border-ink bg-paper p-6 md:p-8">
            <p class="font-display italic text-rust text-xs mb-2 font-medium" style="letter-spacing: 0.06em;">Sample output</p>
            <div class="font-mono text-ink-soft text-sm leading-relaxed">
              <p class="font-display italic text-ink mb-3 font-medium">Travel baseball, age 11</p>
              <div class="border-t border-bone pt-3 mt-2">
                <div class="flex justify-between"><span>Registration</span><span>$1,800</span></div>
                <div class="flex justify-between"><span>Tournaments &amp; travel</span><span>$2,400</span></div>
                <div class="flex justify-between"><span>Gear</span><span>$650</span></div>
                <div class="flex justify-between"><span>Lessons</span><span>$1,200</span></div>
                <div class="flex justify-between"><span>Misc &amp; food</span><span>$800</span></div>
              </div>
              <div class="border-t border-ink pt-3 mt-3 flex justify-between text-ink font-medium">
                <span>Annual total</span><span>$6,850</span>
              </div>
              <p class="font-display italic text-ink-soft text-xs mt-3" style="font-style: italic;">
                Edit any line. Share the result. Honest math either way.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* SECTION 4 of 6: FEATURED LONG READ */}
  {featured && (
    <section class="border-b border-bone">
      <div class="container-px py-14 md:py-16">
        <p class="font-display italic text-rust mb-3 font-medium">If you only read one thing</p>
        <a href={`/${featured.data.phase}/${featured.slug}/`} class="block group max-w-3xl">
          <h2
            class="font-display text-ink text-4xl md:text-[3.5rem] leading-[1.05] group-hover:text-rust transition-colors font-medium"
            set:html={renderTitle(featured.data.title)}
          />
          {featured.data.dek && (
            <p class="font-display text-ink-soft text-xl mt-5 leading-snug max-w-2xl">
              {featured.data.dek}
            </p>
          )}
          <div class="t-cta mt-6">Read it →</div>
        </a>
      </div>
    </section>
  )}

  {/* SECTION 5 of 6: LATEST FEED with age filter (single column, regular blog grid) */}
  <section class="border-b border-bone">
    <div class="container-px py-14 md:py-16">
      <div class="flex items-end justify-between mb-8 flex-wrap gap-3">
        <div>
          <p class="font-display italic text-rust mb-2 font-medium">Fresh off the desk</p>
          <h2 class="font-display text-ink text-3xl md:text-[2.75rem] leading-[1.05] font-medium">
            What's <em class="text-rust">new</em> this week.
          </h2>
        </div>
        <a href="/reads/" class="t-cta">See the archive →</a>
      </div>

      <div class="flex flex-wrap items-center gap-2 mb-6" data-age-filter>
        <span class="font-display italic text-ink-soft text-sm font-medium">Filter by your kid's age:</span>
        <button data-age="" class="font-display italic px-3.5 py-2 border border-ink bg-ink text-paper text-sm font-medium">All</button>
        <button data-age="5-7" class="font-display italic px-3.5 py-2 border border-bone text-ink-soft text-sm hover:bg-paper-warm font-medium">5–7</button>
        <button data-age="8-10" class="font-display italic px-3.5 py-2 border border-bone text-ink-soft text-sm hover:bg-paper-warm font-medium">8–10</button>
        <button data-age="11-12" class="font-display italic px-3.5 py-2 border border-bone text-ink-soft text-sm hover:bg-paper-warm font-medium">11–12</button>
        <button data-age="13-14" class="font-display italic px-3.5 py-2 border border-bone text-ink-soft text-sm hover:bg-paper-warm font-medium">13–14</button>
        <button data-age="15-plus" class="font-display italic px-3.5 py-2 border border-bone text-ink-soft text-sm hover:bg-paper-warm font-medium">15+</button>
      </div>

      <div class="grid md:grid-cols-2 gap-x-10" data-feed>
        {feed.map((a) => (
          <div data-age-tag={a.data.age ?? 'all-ages'}>
            <ArticleCard
              href={`/${a.data.phase}/${a.slug}/`}
              format={a.data.format}
              sport={a.data.sport}
              age={a.data.age}
              phase={a.data.phase}
              topic={a.data.topic}
              title={a.data.title}
              dek={a.data.dek}
              publishedAt={a.data.publishedAt}
              reviewedAt={a.data.editorial?.jeffReviewedAt ?? a.data.editorial?.claudeReviewedAt}
            />
          </div>
        ))}
      </div>
    </div>
  </section>

  <script>
    const bar = document.querySelector('[data-age-filter]');
    const feedEl = document.querySelector('[data-feed]');
    if (bar && feedEl) {
      bar.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        if (target.tagName !== 'BUTTON') return;
        bar.querySelectorAll('button').forEach(b => {
          b.classList.remove('bg-ink', 'text-paper', 'border-ink');
          b.classList.add('border-bone', 'text-ink-soft');
        });
        target.classList.remove('border-bone', 'text-ink-soft');
        target.classList.add('bg-ink', 'text-paper', 'border-ink');
        const age = target.getAttribute('data-age') ?? '';
        feedEl.querySelectorAll<HTMLElement>('[data-age-tag]').forEach((row) => {
          const tag = row.dataset.ageTag ?? '';
          if (!age || tag === age || tag === 'all-ages') {
            row.style.display = '';
          } else {
            row.style.display = 'none';
          }
        });
      });
    }
  </script>

  {/* SECTION 6 of 6: LEAD MAGNET (component handles the layout) */}
  <LeadMagnetCTA />
</BaseLayout>
