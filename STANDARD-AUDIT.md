# Standard Audit: parent-coach-desk (parentcoachdesk.com)

Tracks this project against the Website Build Standard (`About Me/Website-Build-Standard.md`). One row per pillar. Status values: pass, fail, fixed, waived, not yet run. No site reaches the Deployment norm with an open Critical in any pillar.

**Cost line (Pillar 8):** Cloudflare moving to Workers Paid ($5/mo base) as of 2026-07-18 (Pages Functions on the Workers platform): buys the higher CPU limit over the free 10ms/request cap, and unlocks Queues and account Budget alerts. D1 and R2 usage rides on top, shared with ActivityRadar. Kit (ConvertKit) newsletter delivery on its free/starter tier. No paid third-party API is called from this site (Nominatim geocoding is free/keyless). Spend guard: a Cloudflare Budget alert (~$15-20 threshold) on the now pay-as-you-go account — an informational email when cumulative usage-based spend crosses the line, not a hard cap. Workers Paid is what makes budget alerts available. Any future metered dependency (a paid API) also gets its own cap noted here per Pillar 8. Sentry, UptimeRobot, and Resend (Pillar 8 monitoring + alerting) are all confirmed free-tier: Sentry free error tracking, UptimeRobot free plan (50 monitors, 5-min HTTP checks + API; its heartbeat/cron monitor is a PAID feature, so UptimeRobot covers the uptime check only), Healthchecks.io free Hobbyist (20 checks, no card) for the business-metric heartbeat, Resend free tier for the ops/transactional path (distinct from Kit, which stays the newsletter sender). As of 2026-07-18 all three monitoring legs are wired (Sentry DSN set, UptimeRobot uptime monitor live, Healthchecks.io heartbeat created) per the Pillar 8 row below, live only once the pending deploy ships — this line was stale (still said "Sentry... DSN pending" as of 2026-07-17) until corrected 2026-07-31, see anchor PILLAR-8-2026-07-31.

Last updated: 2026-07-30 (Pillars 2/3/5 pass on the `audit/full-standard-2026-07-30` branch: the `org_contacts` PII layer added to `DATA-MAP.md`, `REGULATORY-RISK.md` written, Turnstile added to the third-party map, live pre-consent test run against the deployed site, self-service-account-deletion waiver stated — see open items #58-60 and `reports/AUDIT-2026-07-30-pillars-02-03-05-privacy-consent-legal.md`). Same-day, separate pass: Pillar 14 first run at 390px, then the hero-gap pass, then the flat-section-band fix — see open items #49-53. Prior: 2026-07-15 (verification pass, `reports/VERIFICATION-2026-07-15.md`). Six parallel lanes landed this day; this file was corrected against what was actually verified, not against the lanes' own reports. Three rows moved: Pillar 1 `fixed` → `fail` (open HIGH #17, and the "no open High" claim below was left contradicting it), Pillar 9 `fixed` → `fail` (22 live `tsc` errors, see the Pillar 9 2026-07-15 note), Pillar 8 `fixed` → `fail` (new open item #19, a live HTTP 500 on 296 public camp pages). Prior: 2026-07-14 (Pillars 1 and 9 closed out — this session had live shell access, unlike the 2026-07-05 pass, and used it to confirm the CRON_KEY secret, write the remaining route tests, run and act on `npm audit`, and burn down `npm run check` to 0 errors. See Pillar 9 detail and Open items #3, #5, #13, #14, #16 below.) Prior: Pillars 8, 9, 10 run for the first time on 2026-07-05; Pillars 1, 4, 7 re-checked per the 2026-07-05 consultant tightening; Pillar 6 re-verified 2026-07-07.

**2026-07-13 note (ActivityRadar merge):** the surface this audit covers grew. ActivityRadar folded in as this repo's camp data layer: the org/program/session schema history (`migrations-activity-radar/`), the daily discovery pipeline (`buildout/`), and two Cloudflare Workers (`activityradar-enrichment` hourly, `activityradar-yelp` daily) that write to the same shared `activity-radar` D1 this site reads. ActivityRadar's own pillar audits (`SECURITY-AUDIT.md`, `STANDARD-AUDIT.md`, `DATA-MAP.md`) are archived at `activityradar-archive/` — historical record only, no longer maintained separately. Pillars 8-10 stay open exactly as already flagged below; this merge didn't touch Ops/CI/SEO status, it just widened what those pillars now need to eventually cover (the two Workers have no monitoring, no CI, and aren't in the sitemap conversation).

**2026-07-15 retirement note:** `activityradar-yelp` was deleted from Cloudflare and its source/config removed. Operational audit scope now includes only `activityradar-enrichment`; historical Yelp-derived fields remain in D1 but no scheduled job refreshes them.

**2026-07-17 stack turn-on note (Pillar 8 progress, live-repo verified, not a full pillar re-run):** checked the running tree against this file because the 2026-07-15 "monitoring baseline not set up" text had gone stale. Actual state today: **Sentry** is mid-setup in a parallel session — `@sentry/astro` + `@sentry/cloudflare` (^10.66) are now in `package.json` and installed, `sentry.client.config.ts` and the `@sentry/cloudflare` server wrap in `src/worker.ts` are wired, and the astro integration is in `astro.config.mjs`; it stays disabled and sends nothing until `PUBLIC_SENTRY_DSN` (build var, `.env`) and `SENTRY_DSN` (Pages secret) are set, so it is coded-but-dark, not live. **Resend** is fully coded (`src/lib/email.ts`, HTTPS API, no SDK) but ships in STAGE mode by design: it renders and posts to Slack, sends no mail until `RESEND_API_KEY` + `EMAIL_FROM` are set, the sending domain is verified in Resend, and `EMAIL_ADMIN_MODE=send` is flipped (admin alerts first, `EMAIL_MODE=send` for parent-facing later). So "Resend running" = staged, not sending. **CRON_KEY** was rotated: a fresh value was generated and the paste-ready commands to set it in both the Pages secret and the GitHub Actions repo secret were handed to Jeff (closes the item #16 gap once run — mark resolved after Jeff confirms he ran them). **UptimeRobot** free covers the uptime monitor only — its heartbeat/cron monitor is a paid feature. **Healthchecks.io** free (Hobbyist, 20 checks, no card) is the heartbeat/dead-man's-switch provider: a check named "PCD camps-sweep" is created and its https://hc-ping.com/<id> ping URL is what OPS_HEARTBEAT_URL points at, so the daily camps-sweep pings it on every healthy run and Healthchecks.io emails on a missed run (see Pillar 8 row / item #6). **Access JWT** still runs LEGACY/unverified in production until `ACCESS_TEAM_DOMAIN` + `ACCESS_AUD` are set (item #2). Nothing in the source tree was changed by the turn-on work itself — these are secrets and dashboard steps — so no redeploy is implied by this note alone; the DSN build var does need a redeploy to bake in.

| # | Pillar | Status | Note |
|---|--------|--------|------|
| 1 | Security | fail | **2026-07-31 full Pillar 1 re-run (anchor: PILLAR-1-2026-07-31).** Full detail: `SECURITY-AUDIT.md` (rewritten in full this pass) and `reports/AUDIT-2026-07-30-pillar-01-security.md`. Row stays `fail`: one open HIGH carried forward (#17, the OpenAI key — `gen_hero_image.py` no longer exists anywhere in the Cowork tree and `About Me/openai-config.md` now carries no plaintext key, but the file's claim that the key was "rotated 2026-07-30" is a documented claim this session could not verify against the OpenAI dashboard itself; stays open until Jeff confirms it directly). **Fixed this session:** (1) a literal NUL byte in `src/lib/org-contacts.ts` and 5 more in `scripts/proof.mjs` — both were hash/dedupe-key delimiters that should have been a plain space; the NUL byte was silently exempting `org-contacts.ts` (a PII-access file) from every text-based secret scan, including the repo's own `scripts/scan-secrets.mjs`, which explicitly skips any file containing a null byte. (2) `org-contacts.ts`'s `upsertOrgContact()` now hardcodes `is_public = 0` on every insert instead of trusting caller input — closes a real gap between a documented "an agent must never publish a contact" rule and what the code enforced, relevant because this table is written by daily agents reading organizations' own public web pages (the "AI reads unscrubbed content with write access" pattern this pillar's addition calls out). (3) `npm audit fix` bumped `astro` to `^7.1.6` and `@astrojs/rss` to `^4.0.19`, closing open item #38 (two moderate CVEs) — `npm audit` now reports 0 vulnerabilities; not build-verified, `astro build`/`vitest` both bus-error in this sandbox, Jeff needs to run `npm install && npm run build && npm test`. **Verified live this session, not just in source:** security headers via `curl -I` (HSTS/nosniff/X-Frame-Options/Referrer-Policy/Permissions-Policy/COOP/CSP all present and matching source), `/admin/` and `/api/admin/camps/queue` both 302 to Cloudflare Access for an unauthenticated request, a forged cross-origin POST to a public write route returned 403. **New open items, all MEDIUM unless noted:** no rotation date on record for `SLACK_SIGNING_SECRET`, `SLACK_WEBHOOK_URL`, `GITHUB_TOKEN` (has repo `contents:write` — a leak is a repo-write compromise), or `BULK_IMPORT_TOKEN`; no Cloudflare WAF rate-limiting rule confirmed on `/admin/*`/`/api/admin/*` (this pillar requires brute force to die at the edge, not only in app code); a full-history gitleaks-equivalent scan did not complete this session (every attempt — the repo's own scanner, `git log -p`, `git log -S`— timed out against this sandbox's FUSE-mounted git history; last real gitleaks run on record stays 2026-07-22, clean); Turnstile keys unset (LOW, routes fail closed so this is safe, not a live exposure); `.git` in this checkout carries dozens of stale-renamed lock/ref files from prior interrupted parallel-agent operations (LOW, not touched, out of scope). A full rate-limit table (6 bindings, all fail-closed 503 on missing binding, 429+Retry-After on trip, actor keys SHA-256 hashed, tested) is now in `SECURITY-AUDIT.md` §8. Session lifecycle: this site has no live app-level session-issuing auth today (admin is Access-only; the customer/owner auth layer is fully feature-flagged off), so the standard's lifetime/concurrent-session/kill-on-password-change asks have no live trigger yet — flagged as a build-time gate for whoever ships that feature, not a finding against the site as it stands. Prior history (2026-07-05 through 2026-07-22) preserved in `SECURITY-AUDIT.md`'s Session log section. |
| 2 | Privacy | fixed | DATA-MAP.md completed: retention windows and deletion paths documented for every store. `/disclosure/` serves as the privacy policy; matches the map. Re-checked 2026-07-05, no changes needed — the new Ops heartbeat ping carries no personal data, nothing to add to the map. **2026-07-30 (Pillars 2/3/5 pass):** found the `org_contacts` PII layer (migration `migrations-pcd-ops/0028_org_contacts.sql`, `src/lib/org-contacts.ts`, documented in `CONTACT-DATA-MAP.md` but not in `DATA-MAP.md`) — named human contact data (name, title, role, direct email/phone for camp/program staff) compiled by two daily agents from an organization's own public web pages, not submitted by the person. Fixed: added `org_contacts` to `DATA-MAP.md`'s data table, third-party table note, retention table, and deletion-path table; added `/disclosure/`'s matching privacy-policy bullet describing the compiled-data collection and the `/trust/` opt-out path; wrote `REGULATORY-RISK.md` at the project root (required per this pillar's rule on data subjects who never asked — state data-broker registries, CCPA/CPRA indirect-collection notice, GDPR Article 14 if scope ever expands past US orgs, FTC Section 5) naming the human-gate lawyer review still owed before migration 0028 is applied or the table is used for outreach. Also added: a one-line self-service-account-deletion waiver in `DATA-MAP.md` (site has no user accounts — correct per Pillar 2's own rule to state a waiver rather than invent a finding), Sentry added to the third-party table as dark/not-live (DSN unset in both wrangler configs), and a matching disabled-feature disclosure-page bullet for reader testimonials (`proof_inbox`, gated behind `PROOF_SUBMIT_ENABLED=false`), which the policy previously omitted even though camp reviews and claims got the same disabled-feature treatment. **Left open, not this session's call:** the search-events retention conflict (12 months in `DATA-MAP.md` vs. 90 days in the archived ActivityRadar map) is unresolved from a prior session and still needs Jeff to confirm which figure the code actually enforces — not re-litigated here. |
| 3 | Consent & Analytics | fixed | ConsentBanner component added to BaseLayout — GA4 loads only after localStorage `pcd_consent=yes`. EU geo-fence and DNT/GPC still apply. Leaflet and web-vitals moved from CDN (unpkg.com, cdn.jsdelivr.net) to npm bundles; both domains removed from CSP. Cloudflare Web Analytics runs consent-free. **2026-07-30 (Pillars 2/3/5 pass, live pre-consent test run):** fetched the deployed homepage cold (`curl`, no cookie jar) and read the raw response headers and HTML. Zero `Set-Cookie` header on the initial response. Zero `googletagmanager`/`google-analytics`/`gtag(` string anywhere in the raw HTML — GA only exists as a same-origin bundled module (`BaseLayout.astro_astro_type_script_index_*.js`), gated in source on `localStorage.getItem('pcd_consent') === 'yes'` before it ever appends the GA script tag or calls `gtag`, confirmed by re-reading `BaseLayout.astro`'s inline analytics block line by line. Reject ("No thanks") is the same size, same row, same click distance as Accept — not buried. Pre-consent test: **pass, on the live site, not just in code.** Found one real gap: Cloudflare Turnstile (`challenges.cloudflare.com`, loaded unconditionally on the 4 public forms) was in the CSP and in `src/lib/turnstile.ts` but not in `DATA-MAP.md`'s third-party table — fixed, added with a note that it is essential/security so it correctly loads with no consent gate per this pillar's own "essential cookies can load without consent" rule; it does not need to move behind the banner, it needed to be documented. No other silent third party found: grepped `src/` and `public/` for external `script src=`/`iframe`/CDN references — the matches were editorial citation links (`public/link-manifest.json`, outbound article references) and API-fetch calls to already-mapped vendors (Nominatim, Kit, Cloudflare, Sentry-dark), not page-load resources. |
| 4 | UI & Design System | fixed | CSS custom properties for all 9 color tokens. Self-hosted fonts (Fraunces, Mulish, JetBrains Mono via @fontsource). Metric-adjusted fallbacks eliminate swap-CLS. Button variants (.btn, .btn-soft, .btn-ghost), tag pills, focus ring (*:focus-visible) all defined. Tokens live in `TEMPLATES/web/design-tokens.css`. Fixed 2026-07-05: `NavBar.astro`'s two hardcoded hex inline styles now read `var(--rust)` / `var(--ink)` (open item #1, closed). Lighthouse mobile numbers (LCP/CLS/INP) not re-measured this session — needs a live run against the deployed site, flagged as a human/CI step below. **2026-07-29 (navy/silver-green palette redesign, design QA pass):** the warm-editorial palette (cream/terracotta/sage, Fraunces + Mulish) was remapped to navy/silver-green (`pcd-tokens.mjs` → `tailwind.config.mjs` → `global.css` `:root`; legacy token *names* kept, values changed, per that file's own comments). This session swept the rest of `src/` + `public/` for what the remap missed: ~35 leftover hardcoded warm hexes fixed across ~25 files (admin status pills, callout asides, a fully-unconverted dark hero on `parent-coach.astro`, `favicon.svg`, `site.webmanifest`, the `admin/image-needs.md` art-direction brief) — plus two genuinely broken/undefined Tailwind color references, `pine` and `sage` (used across `admin/editorial/index.astro` and 6 other admin files), which the color consolidation orphaned and which left several admin buttons rendering with no border/background at all; renamed to defined tokens (`accent`, `stripe`). `src/data/site.ts` carries 40+ pillar/topic/team-parent `accent`/`accentBg` pairs (far more than just the homepage Three Drives the brief named) — all still 100% old warm hues; collapsed every one to navy (`accent`) / palest neutral fill (`accentBg`, `#F4F7FA`), documented in-file, since 3 allowed hues cannot carry 40+ distinct color identities without becoming arbitrary. One `linear-gradient` (banned under the new brief) found and fixed in `cost-calculator.astro`'s hero photo wash — replaced with a flat rgba wash, same legibility purpose, no gradient. Confirmed: no dark-mode CSS, no thin font weights, no metallic/glow effects, no heading-level `uppercase` (only small eyebrows, as intended), Mulish fully gone from live font declarations (only historical comments name it). **Not fixed, listed as follow-up** (found via sitewide grep, but outside this session's touched-file list — see Open items #34-35): a second stale gradient+old-hex wash in `newsletter.astro`; the `bg-ink`-as-full-section-background "closing statement" pattern used in 17 files; off-palette Tailwind red/amber/green health-status text in 5 ops-adjacent admin files (arguably legitimate status semantics, not a palette leftover — flagged for Jeff's call, not changed). **2026-07-31 (Pillars 4/6/14 pass, `reports/AUDIT-2026-07-30-pillars-04-06-14-ui-a11y-mobile.md`):** re-grepped `src/` and `public/` for every retired warm-palette hex (`#C5713D`, `#9E5228`, `#FAF6EE`, `#F2EAD9`, `#2D2520`, `#5F5448`, `#8FA68C`, `#D4AB6A`, `#B8908F`, `#DDD2BD`, `#EDDBD9`) — zero live occurrences remain; the only matches are the documentary `/* was ... */` comments in `pcd-tokens.mjs` and `global.css`'s own `:root` block, which is the intended record, not a leak. Confirmed item #53 (`--bone`/`--line` 1.53:1 as the sole border on real form inputs) is now actually resolved in source, not just token-defined: every `<input>`/`<select>`/`<textarea>` across all 14 files item #35/#53 named now carries `border-input` (`--input-border` `#7C8B99`, 3.49:1), verified by pattern-matching every form-control tag in `src/` for a residual `border-bone` — none found. This closes open item #53; see the corrected item below. |
| 5 | Terms & Legal | fixed | Terms of Service at /terms/, privacy at /disclosure/. Both linked in footer. Field & Forge Ventures operator name added to both pages. Governing law (Washington, USA) stated. Templates live in `TEMPLATES/web/`. **2026-07-20:** Terms of Use + UGC license shipped, unblocks Reviews (Open Item 7); recommend attorney review before Reviews launch. Added Pierce County venue to the governing-law clause, sublicensable/reproduce/excerpt rights and a compensation-disclosure requirement to the review license, broadened prohibited conduct (harassment, impersonation, scraping, posting others' personal information), a physician/athletic-trainer/certified-coach line to the content disclaimer, and a bottom "Effective 2026-07-20." line. **2026-07-30 (Pillars 2/3/5 pass):** re-verified live — both pages confirmed 200, both in the footer (`curl` of the deployed homepage), Pierce County venue and Field & Forge Ventures operator name both present in `terms.astro`'s closing section. No payment taken anywhere on the site (Amazon/CJ affiliate links only pay PCD, not the reverse), so refund/billing terms are correctly absent rather than missing. No user accounts, so the Pillar 5 account-deletion-in-ToS requirement is waived for the same reason it is waived in `DATA-MAP.md` under Pillar 2 — there is no account to describe a deletion path for; the `/trust/` privacy-request path is what both `terms.astro` and `disclosure.astro` point to instead, and that is the correct pattern for a site with none. COPPA: `terms.astro`'s "Acceptance and who can use this site" section requires interactive-feature users to be 13+, and `disclosure.astro`'s "Children" section states no knowing collection under 13 — both already existed and hold up under this pass's hard look, no gap found. One real gap found and fixed: neither legal page mentioned the `org_contacts` compiled-data layer at all, since it predates this pass's Pillar 2 fix — `disclosure.astro` now names it (see Pillar 2 row); `terms.astro` did not need a matching change, since it is a site-visitor-facing UGC/acceptable-use document and `org_contacts` is not user-submitted content. |
| 6 | Accessibility | fixed | Re-verified 2026-07-07: the 2026-07-05 pass's claims were spot-checked against live source, not assumed. Confirmed true as-is: skip link (`BaseLayout.astro` targets `#main`, visible on `:focus`), global focus ring (`*:focus-visible` in `global.css`), landmarks (header/nav/main/footer, one each, no dupes), nav dropdown ARIA (`aria-haspopup`/`aria-expanded` kept in sync by JS in `NavBar.astro`), `prefers-reduced-motion` block genuinely in `global.css` (not just documented), and the WCAG target stated on `/accessibility/` (now says WCAG 2.2 AA in the page copy — one version ahead of this pillar's 2.1 AA bar, not a conflict). Found and fixed real defects this pass, none of which were in the prior claims list: (1) `src/layouts/ArticleLayout.astro` was truncated mid-tag (missing `/>` on `<RelatedArticles>`, missing `</BaseLayout>`, a duplicate `class` attribute on the article `<h1>`) — this is the layout every article page renders through, so it was a live build-breaking defect, fixed. (2) `src/pages/cost-calculator/methodology.astro` was truncated mid-sentence with no closing tags — fixed. (3) An in-progress, uncommitted contrast fix (`--rust` darkened from `#C5713D` to `#9E5228` in `global.css` and `pcd-tokens.mjs`) had only partially propagated — `NavBar.astro` was updated but ~15 other files still hardcoded the old failing hex for actual text-on-background color, all now corrected (see note below for the contrast math). (4) `src/pages/camps/[slug].astro`'s claim-listing form had five inputs with no `<label>` at all (only placeholders) — added visually-hidden labels matching the pattern already used one section up in the same file's review form. (5) Dynamic form/status feedback (`camps/submit.astro`, `camps/suggest.astro`, `camps/[slug].astro` review + claim forms, `admin/camps/queue.astro` approve/reject/photo actions, `search.astro` results count) had no `aria-live` anywhere in the codebase except two unrelated files — added `aria-live="polite"` to all of these. (6) Five admin data tables (`admin/editorial`, `admin/link-health`, `admin/search-signals` ×2, `admin/source-quality`) had `<th>` with no `scope="col"` — added. (7) `about.astro` and `search.astro` skipped h1→h3 with no h2 — promoted the h3s to h2. (8) A second duplicate-`class`-attribute bug (`admin/camps/spot-check.astro`'s action banner) — fixed. No open Critical or High remains from this pass; see the contrast detail below and the Open items table for the one redeploy action left for Jeff. **2026-07-29 (navy/silver-green palette re-verification):** ran a proper WCAG 2.1 contrast script (sRGB-linearized, not approximated) against the new palette — full pass on all text/background pairs, decorative silver `#8A9BA0` confirmed fail-as-text on light backgrounds and fixed everywhere it was still used that way. Found (and partly fixed) a real non-text-UI contrast failure on the `--bone`/`--line` divider used as the sole visible border on many form inputs, and a 32px tap target on interactive filter chips (`.tag-pill`) below this project's own 44px convention. See the new "Pillar 6 detail — 2026-07-29" section below and Open items #34-35 for exactly what was fixed versus left as follow-up. **2026-07-31 (Pillars 4/6/14 pass):** re-verified WCAG 2.2's nine added criteria against current source. 2.4.11 (focus not hidden behind sticky UI): `NavBar.astro` header is not `position: sticky`/`fixed`, no fixed footer anywhere — pass. 2.5.8 (24x24 target floor, this project holds 44px): tap-target sweep below. 2.5.7 (drag has a pointer alternative): no drag-only interaction found anywhere in `src/` (camp map uses Leaflet click/zoom controls, not drag-only gestures). 3.2.6 (consistent help): footer legal nav (`Footer.astro`) is identical across every page via the shared layout, so help/contact links are in the same place everywhere — pass. 3.3.7 (no re-asking known info) and 3.3.8/3.3.9 (auth allows paste, no memory test): site has no accounts and no multi-step forms that re-collect the same field twice — not applicable, stated rather than assumed. Confirmed via `@astrojs/compiler` `transform()` that no ARIA state regressed in this pass's 14 touched files (none of them carry interactive disclosure/dialog markup). See `reports/AUDIT-2026-07-30-pillars-04-06-14-ui-a11y-mobile.md` for the full pass, including the correction to item #53 (already resolved in source, not still open). |
| 7 | Tech Stack Norms | pass, with fixes | **2026-07-30 full re-run.** This row was stale — it still described the retired Pages topology. Production is now the Cloudflare Worker `parent-coach-desk` (`wrangler.production.jsonc`), shipped only through GitHub Actions + the protected `production` Environment approval, per `DEPLOYMENT-RUNBOOK.md`. `wrangler.jsonc` is the staging Worker (`parent-coach-desk-staging`), clearly headed "staging-only, do not attach a custom domain." D1 (`activity-radar`, `FORGE_DB`/`forge-command`, `PCD_OPS_DB`), R2 (`activityradar-photos` prod / dedicated staging bucket), and KV (`SESSION`) are all bound only inside Worker code, never to the browser — confirmed by reading both wrangler configs directly. Cron is fully native now: `worker-cron` (camps sweep, `0 13 * * *`), `activityradar-enrichment` (hourly), `worker-link-checker` (daily), `worker-backup` (weekly) all declare `[triggers] crons` in their own `wrangler.toml`. `.github/workflows/camps-sweep-cron.yml` is correctly demoted to `workflow_dispatch`-only, a manual break-glass path, not the live scheduler — the standard's ask to retire the GitHub-Actions-cron interim pattern once the site is on Workers is done. Turnstile verifies server-side on all four public write forms (`camps/submit`, `camps/suggest`, `reviews/submit`, `claim`) via `enforcePublicTurnstile()`, which fails closed (503) if the secret is unset. KV-backed rate limiters guard every public write route including the one that calls Resend. Secrets are declared by name only in `wrangler.production.jsonc`'s `secrets.required`, never by value. Payments: this site takes no payment, Dunning row waived in one line. **Anti-vibe findings, each its own line:** (1) no over-fetch/pagination on `src/pages/camps/index.astro` and `camps/[state]/[city]/index.astro` — both pull the full unfiltered approved-camps result set (1,300+ rows) on every request with no `LIMIT`, relying only on edge caching; real but out of this session's file lane (`.astro`, owned by the UI lane this session — see open item #58). (2) a handful of genuine silent-failure catch blocks (JSON-parse fallbacks and a swallowed geocode failure) — **fixed this session**, see open item #59. (3) N+1-shaped loops in `camps-sweep.ts`, `archiveStaleCamps`, and the enrichment worker — all capped at 20-100 rows/run, Low severity, not fixed (would need a batched `db.batch()` rewrite, out of scope for a stack audit). (4) naming drift: the live schema's primary table is `programs`, but `camp_claims`/`camp_reviews`/`camp_scan_queue` and all app code still say "camp," and `sessions` (a real, distinct third concept in the schema) has no app-layer representation at all — Low, documented, not renamed. (5) indexing is in good shape: `pcd_status`, `session_end_date` (composite), `state`, `city`, `sport`/`activity_category` are all indexed; `slug` has an implicit unique index. The two dead duplicate admin routes an earlier planning doc (`ADMIN-ARCHITECTURE-REVIEW-2026-07-29.md`) flagged as delete candidates (`src/pages/admin/api/camps/[id]/{reject,verify}.ts`) do not exist in the current tree — already gone, nothing to fix. |
| 8 | Operations & Reliability | fail | **2026-07-31 full Pillar 8 re-run (anchor: PILLAR-8-2026-07-31).** Full detail: `reports/AUDIT-2026-07-30-pillars-08-09-ops-testing.md`. **Built and shipped:** `src/lib/log.ts`, a structured logger (timestamp/severity/requestId/action/route/userId as one JSON object per line, log levels used on purpose), with the request id sourced from Cloudflare's own `cf-ray` header so it already appears in `wrangler tail` and the dashboard. Rolled out to every `console.*` call in all ~37 files under `src/pages/api/**` plus `src/pages/sitemap-camps.xml.ts` (not just the ~19 the prior audit flagged) and the ten highest-traffic shared libraries (`admin-auth.ts`, `access-jwt.ts`, `admin-receipts.ts`, `email.ts`, `events.ts`, `link-health.ts`, `org-contacts.ts`, `publish.ts`, `slack.ts`, `turnstile.ts`). `tests/log.test.ts` covers the logger; could not execute (`npx vitest run` bus-errors here, same pre-existing sandbox limit documented below). **Verified live, not assumed:** all four Pillar 8 caching classes read directly off `https://parentcoachdesk.com` via curl today — hashed bundles and `/illustrations/*` both `public, max-age=31536000, immutable` (`cf-cache-status: HIT`), HTML `public, max-age=300, s-maxage=86400, stale-while-revalidate=86400`, API JSON `no-store`, Access-gated `/admin/` `private, no-store`. The 2026-07-30 `/illustrations/*` gap is confirmed fixed live, not just in `public/_headers`. All four pass. **Fixed:** the vestigial `check:rollback-target` gate (open item #57) — removed from `package.json`'s `ci:release` and from `.github/workflows/ci.yml`; `scripts/check-worker-rollback-target.mjs` now carries a header explaining the retirement and pointing at `check:production-manifest`'s live exact-binding check as the surviving equivalent. **Confirmed still holding from 2026-07-30:** `scripts/deploy-remediation.mjs` no longer executes a rollback on a smoke failure — it resolves the immediately-preceding version live from `wrangler versions list` and hands a human the exact command; read in full this session, not re-executed. **Still open, honestly:** a real D1 restore has never been run (tried `npx wrangler d1 list` first to check sandbox reachability — no output, no Cloudflare credentials/network path here; logged in `INCIDENT-RUNBOOK.md` with the exact command for Jeff to run for real); the autocannon load test needs a live preview URL this sandbox cannot reach; D1 hot reads still have no Cache API/KV layer (item #23, LOW, unchanged); slow inline work (Resend email sends in `camps/submit.ts`, sequential GitHub Contents API calls in `publish.ts`/`approve.ts`/`set-status.ts`) is still not behind Cloudflare Queues — the account has been on Workers Paid since 2026-07-18 so Queues is available, but wiring a queue binding, consumer, and idempotency key blind in a sandbox that cannot build-verify it was judged riskier than logging it clearly (new open item #74, HIGH). AI-in-production controls (spend caps, AI Gateway, judge tests) are not-applicable as written: this Worker's live runtime makes no LLM calls (`grep` for OpenAI/Anthropic/AI Gateway in `src/lib`/`src/pages/api` returns nothing) — the named agents run as external Claude Code sessions against `POST /api/agent-runs`, under Anthropic's own account-level spend limits, not this repo's. Prior: **2026-07-15: open item #19, a live HTTP 500 on 296 public camp pages.** Confirmed live, root-caused, and fixed in source this session; the row stays `fail` until the fix is deployed and reverified, because the 500 is live right now. The Infra lane found one instance and filed it "not blocking, worth knowing"; it is 296 pages and it is a public-facing crash. No lane owned it. Prior, first run 2026-07-05. See the Pillar 8 detail section below — real code and doc changes made, several items are human/dashboard steps that still need Jeff. **2026-07-18:** monitoring baseline now wired — all ops/security Pages secrets set on `parent-coach-playbook` (CRON_KEY rotated in Cloudflare + GitHub, plus OPS_HEARTBEAT_URL, SENTRY_DSN, ACCESS_TEAM_DOMAIN, ACCESS_AUD, RESEND_API_KEY and the EMAIL_* vars), a Cloudflare Budget alert ($15) created, and the Resend sending domain parentcoachdesk.com verified. Row stays `fail` only because none of this — nor the #19 500 fix — is live until the next deploy, which waits on the parallel Sentry session code being a clean commit. |
| 9 | Testing & CI | fail | **2026-07-31 full Pillar 9 re-run (anchor: PILLAR-9-2026-07-31).** Full detail: `reports/AUDIT-2026-07-30-pillars-08-09-ops-testing.md`. **CI gate gap found and fixed:** `.github/workflows/ci.yml`'s "gitleaks scan" step was actually `npm run check:secrets`, a hand-rolled regex scanner (`scripts/scan-secrets.mjs`), not the real gitleaks binary this pillar names. Added a real `gitleaks/gitleaks-action@v3` step, pinned to the full commit SHA `e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e` (satisfies `check:workflow-pins`), alongside the existing repo-native scanner rather than replacing it — the repo-specific scanner catches PCD patterns generic gitleaks would not. Not verified by an actual GitHub Actions run (no way to trigger one from this sandbox); the action reference was checked by hand against gitleaks-action's public release page. **Dependency hygiene confirmed:** `package-lock.json` committed, every CI step uses `npm ci`, `.github/dependabot.yml` exists and is configured (npm weekly, github-actions monthly). **Astro bump verified safe:** checked whether this session's `astro` `^7.0.9`→`^7.1.6` / `@astrojs/rss` bump (open item #38) is compatible with Astro 6+'s removal of the legacy content-collections format — `src/content.config.ts` already exists and already uses the Content Layer API (`defineCollection` + `glob` loader), not the old `src/content/config.ts` shape, confirmed by reading the file directly. Bump stays; not reverted. Could not run `astro build`/`npm run check` in this sandbox to prove it end to end — Jeff should run `npm install && npm run build && npm run check` before merging. **Test inventory spot-check:** 70 files under `tests/api/` against 51 route files under `src/pages/api/`; every editorial route this session's logging rollout touched already has a matching test file (`admin-editorial-approve.test.ts` through `admin-editorial-opportunities-update.test.ts`, ten files), so the logging change landed inside code that already has coverage. A full happy/failure/auth inventory across all 51 routes against all 70 test files by hand is still owed — not completed this session for time, logged as new open item #75 (MEDIUM). **Could not execute this session:** `npx vitest run` bus-errors in this sandbox on every file — tried, failed plainly, not claimed green. Same for `npm run check` (missing `@astrojs/compiler-binding-linux-x64-gnu` in this sandbox, per the standing note below). Prior: **2026-07-15 (verification pass):** `npx vitest run` → **222 passed, 27 files, 0 failures, exit 0** — the Worker lane's test claim is confirmed exactly. Its companion claim of "zero tsc errors" is **wrong**: `npx tsc --noEmit` reports **22 errors**, all in that lane's own new files (13 `tests/api/email.test.ts`, 6 `tests/api/agent-runs.test.ts`, 2 `tests/api/slack-lib.test.ts`, 1 `src/lib/access-jwt.ts:183`). 21 are test-file tuple/undefined narrowing; the 1 in `src/` is a real `BufferSource` variance error on `crypto.subtle.verify`. The lane's separate claim that "the one error in the tree is `worker-cron/src/index.ts(48,12)` — another lane's file" is also wrong: that file typechecks clean (exit 0) when compiled from its true contents. The error is an artifact of the bash sandbox serving a truncated 2,171-byte snapshot of a 170-line file. `npm run check` (astro check, this pillar's actual bar) could not be run to confirm: the sandbox lacks `@astrojs/compiler-binding-linux-x64-gnu`. So the 2026-07-14 "0 type errors" claim below is at minimum stale and, on the tsc evidence, probably false. This row does not return to `fixed` until `npm run check` is run on Jeff's machine and reads 0. Prior, first run 2026-07-05: Vitest suite scaffolded, 6 of 19 API routes covered. **2026-07-14:** the remaining 14 untested routes (open item #5) now have the full 3-test minimum (happy/failure/auth); 107/107 tests pass. `npm audit` critical + moderate/low findings addressed where safely fixable (open item #13 closed to a documented waiver on the rest). `npm run check` burned down from 215 to 0 type errors (open item #14 closed) — see detail below. All three were run live this session (shell access available), not just scaffolded. **2026-07-20 (real `tsc --noEmit` run, this session):** the 2026-07-15 claim of 22 `tsc` errors is stale — **0 errors** found in a real run today (`node ./node_modules/typescript/bin/tsc --noEmit`, `tsconfig.verify.json` compiler options, chunked into per-directory sub-runs because this sandbox's FUSE-mounted repo makes one full-tree invocation exceed the sandbox's execution budget — each chunk still ran the real compiler against the real tsconfig, just with a narrower `include`). Verified clean: the entire `src/` tree (`lib/`, `components/`, `layouts/`, `data/`, `content/`, `middleware.ts`, `worker.ts`, `content.config.ts`, `env.d.ts`, all of `pages/` including `admin/` and `api/` — ~240 files) and the entire `tests/` tree (`tests/api/`, `tests/helpers/`, `tests/mocks/`, all 119 root-level test files — ~182 files), including the exact files the 2026-07-15 note named (`tests/api/email.test.ts`, `tests/api/agent-runs.test.ts`, `tests/api/slack-lib.test.ts`, `src/lib/access-jwt.ts`) — all clean now, confirming those 22 were fixed in an intervening session rather than still open. Not verified this pass (the sandbox's I/O made every attempt exceed budget, even on single small files, likely `@sentry/astro`'s large type surface and/or FUSE latency noise): `sentry.client.config.ts`, `test-classification.ts`, `vitest.config.ts`, `vitest.integration.config.ts`, `vitest.unit.config.ts`, and three standalone side-project workers each with their own `tsconfig.json`/`package.json` (`legacy-domain-redirect/`, `worker-link-checker/`, `workers-activity-radar/`) — 8 files, roughly 2% of total scope. This also resolves the `coordination/BUILD-PLAN-COMPLETION-AUDIT-2026-07-18.md` / `coordination/CURRENT_STATE.md` "0 errors" claim: those two only ever ran `astro check`, never `tsc --noEmit`, so the claim was unverified rather than false — today's real run corroborates 0 on the tsc front for everything checked. Row stays `fail`: `npm run check` (astro check, this pillar's actual bar) still cannot run in this sandbox (missing `@astrojs/compiler-binding-linux-x64-gnu`), and the 705-passing-tests figure from the 2026-07-18 docs was not independently re-run this session. Needs `npm run check` and `npx vitest run` on Jeff's machine to close this row. |
| 10 | SEO & Discoverability | fixed | First run, 2026-07-05; full organic-search audit 2026-07-12 (`ORGANIC-SEARCH-AUDIT.md`); indexation-recovery session run same day, see below. **2026-07-12 indexation recovery:** confirmed live in GSC that the "Not found (404)" bucket (53 pages) and the "Alternate page with proper canonical" bucket (94 pages) are exactly what the audit predicted — 100% `/camps/` URLs and majority `www.` duplicates in samples pulled this session. Fixed at the code level: (1) `camps/[slug].astro` no longer bare-404s a camp that once existed — expired dates 301 to the sport hub, non-approved-but-future camps (rejected/pending) return 410, only a truly unknown slug still 404s; (2) `src/middleware.ts` (new) 301s `www.parentcoachdesk.com/*` to the bare domain for every request the Cloudflare Function sees — the homepage and a handful of routes `dist/_routes.json` excludes from the Function still need a Cloudflare Redirect Rule in the dashboard, noted as an open item; (3) `sitemap-camps.xml.ts` now emits a real `<lastmod>` per camp (was missing entirely); (4) confirmed the camps sitemap and `/camps/` index already scope strictly to `pcd_status='approved' AND session_end_date >= today` — the ~195k unenriched ActivityRadar IRS org stubs were never publicly exposed on this site, no code change needed there; (5) editorial lift run on 25 articles (14 GSC-confirmed-impression pages + 11 selected per the audit's long-tail guidance) — title/meta/H1/hub-link checked on all 25, 6 files fixed, including a real bug in `scripts/index.astro` that was silently orphaning two of the confirmed-signal pages from their hub; (6) legacy-domain redirect worker written (`legacy-domain-redirect/`) for parentcoachplaybook.com, pending a one-time Custom Domain attachment in the Cloudflare dashboard. Lighthouse mobile numbers still not measured (CWV has no field data — traffic too thin). **2026-07-13 second indexation-recovery pass:** re-verified the 07-12 fixes live rather than re-deriving them. `/camps/soccer-camp-full-day-at-sera-sports-complex/` now 301s (confirmed via curl, no longer 404), `www` correctly 301s to bare domain on both the homepage and a subpath, and D1 confirms the 195k IRS org stubs still have no live route or sitemap entry (verified by schema, not assumption). Found and fixed one real regression in the 07-12 redirect fix: `activity_category` (aliased as `Camp.sport`) is a generic bucket ('camp_sports', 'camp_general', 'other') on 80% of approved programs, so the expired-camp redirect's sport-hub match silently missed most camps and fell to the generic `/camps/` hub even when the camp's state was known. `camps/[slug].astro` now prefers a real sport-guide match, then the camp's own `/camps/[state]/` hub (state is always populated, unlike the category field), then `/camps/` only as a last resort. Independently re-ran the 25-article editorial audit against a fresh, GSC-demand-validated list (cost-breakdown, rec-vs-travel, quitting, burnout, specialization, sideline-behavior clusters); 24 of 25 already had a clean seoTitle/seoDescription/single-H1/hub-link — the one real gap, `why-kids-quit-sports-how-coaches-prevent-it.md`, had no `dek` or `seoDescription` at all and was falling through to the generic site-wide meta description; fixed. Live-inspected `/camps/` in GSC and found its `User-declared canonical` still reads the homepage from the June 21 crawl even though the live tag is correctly self-referencing (`curl`-verified) — a stale-crawl artifact, not a live bug, and exactly the kind of thing a fresh crawl clears. Requested re-indexing in GSC for `/camps/`, `/what-to-buy/`, `/scripts/`, `/body/`, and `/decisions/` (5 URLs; homepage already indexed, skipped). GSC cap is roughly 10-12 manual requests/day, so the remaining hubs (`/pathways/`, `/sports/`, `/reads/`, `/drive-there/`, `/drive-home/`) and the 25 articles are queued for the next few days rather than one session. Legacy domain (`parentcoachplaybook.com`) still returns an empty 200, not a redirect — the `legacy-domain-redirect` Worker from 07-12 is still unattached. **Downgraded 2026-07-13 per Jeff:** the old domain was only live about a week before the parentcoachdesk.com switch, so there is no real link equity or age for Google's Change of Address tool to inherit — this was overstated as an indexation-recovery item in the 07-12/07-13 passes. The Custom Domain attachment is still worth doing eventually (catches stray old links in email signatures, bookmarks, anything printed with the old URL), but it is a low-priority cleanup item, not part of the indexation-recovery urgency, and does not need to be prioritized ahead of the authority/backlink work in the 30/60/90 plan. |
| 13a | Protect-the-App — Unhappy Paths | fail | **First real run 2026-07-30.** Admin mutation routes (`camps/[id]/{approve,reject,verify,photo,update}`, `reviews/[id]/{approve,reject}`) had several genuinely silent catch blocks — empty or comment-only, no logging, no surfaced error — on JSON-body parsing and one swallowed geocode failure on address edits. **Fixed this session**: every one now logs a structured `console.error` before falling back, so a parse failure or a failed geocode is visible in Worker logs instead of vanishing. Same class of fix applied to the four public write routes (`camps/submit`, `camps/suggest`, `camps/check`) that had an identical silent JSON-parse fallback. Two `isHttpUrl()` catch blocks in the link-health admin routes were checked and left alone — that pattern (URL constructor throws on invalid input, function returns false) is correct validation, not a silent failure. **Still open, Medium:** `src/pages/camps/index.astro` and `camps/[state]/[city]/index.astro` pull their full result set with no `LIMIT` on every request (over-fetch, not itself an unhappy-path bug, tracked under Pillar 7's anti-vibe findings and out of this session's `.astro` file lane). No fault-injection harness exists yet against a live preview for the admin write paths — the standard's "prove the UI never says saved when the write did not persist" bar is proven at the unit level (see 13c) but not yet at the browser/preview level. Row stays `fail` until that preview-level fault injection runs. |
| 13b | Protect-the-App — Environment Isolation | pass | **First real, live-verified run 2026-07-30.** This was the audit's likely-real-finding candidate going in (shared `activity-radar` D1, a "staging-only" `wrangler.jsonc` flagged stale in open item #21) — checked hard and it holds up clean. `wrangler.jsonc` (staging) and `wrangler.production.jsonc` (production) bind fully distinct D1 databases, R2 buckets, and KV namespaces; confirmed by reading both files directly and then cross-checking every database id against a live, read-only Cloudflare D1 API call (see `ENVIRONMENT-MANIFEST.md`, written this session). `scripts/deploy-staging-verified.mjs`'s `validateStagingDeploymentManifest()` already hard-forbids the production D1 names (`activity-radar`, `forge-command`, `parent-coach-desk-ops-production`) from appearing in a staging deploy manifest — the standard's "fails closed when a lower ring points at a production resource" requirement, already implemented and read directly, not inferred. `wrangler.pcd-ops.local.jsonc` uses a placeholder database id that cannot resolve to a real Cloudflare resource. The one real gap: no checked-in names-only manifest existed before this session — `coordination/PRODUCTION_STAGING_MATRIX.md` is a narrative planning doc from before the Pages-to-Workers cutover, already stale (it describes staging sharing production's D1, which is no longer true), not a manifest. **Fixed:** `ENVIRONMENT-MANIFEST.md` added at the repo root — names, owners, verification state, no secrets, live read-back confirmed 2026-07-30. |
| 13c | Protect-the-App — Sensitive-Action Receipts | fail, real receipts landed this session | **Prior claim was false.** Open item #25 (2026-07-28) described a working `content_overlay_receipts` table — hash-chained, append-only-by-trigger, 14/14 tamper checks, tied to an "inline editor" feature. **A 2026-07-30 check across every migration file, every `.ts` file, and a live read-only query against the production `PCD_OPS_DB` table list found no such table and no inline editor anywhere in this repo.** `ADMIN-ARCHITECTURE-REVIEW-2026-07-29.md` independently reached the same conclusion one day after item #25 was written ("the hash-chained receipts referenced in earlier planning notes do not exist here... no admin audit trail at all"). Neither the feature nor its receipts were ever built; the prior audit text was aspirational, not a report of shipped work. **Built for real this session:** `migrations-pcd-ops/0029_admin_action_receipts.sql` (append-only via `BEFORE UPDATE`/`BEFORE DELETE` triggers that `RAISE(ABORT,...)`, hash-chained via a `prev_hash`/`row_hash` pair, actor identity stored as a SHA-256 digest plus bare domain, never a raw email, reason/before/after bounded to 200 chars) and `src/lib/admin-receipts.ts` (`recordAdminReceipt`, `verifyReceiptChain`, `withAdminReceipt`). Wired into the five named surfaces — `camps/[id]/{approve,reject,verify,photo,update}` — plus `reviews/[id]/{approve,reject}` as a bonus. `withAdminReceipt` refuses to report success to the API caller if the receipt itself fails to write (`RECEIPT_WRITE_FAILED`, 500), even though the underlying mutation already succeeded — proven by a test that confirms the mutation ran but the response is never `ok:true` without a durable receipt. `tests/admin-receipts.test.ts` runs the real migration DDL against Node's built-in `node:sqlite` (not a mock) and proves: UPDATE and DELETE are both physically rejected by the triggers; a modified row's stored `row_hash` no longer matches a fresh recomputation; a deleted row breaks the next row's `prev_hash` chain; two rows with swapped content break the chain the same way; no raw email ever appears in a stored row. **Row stays `fail`, not `fixed`,** because 30 of the 37 admin mutation routes (claims, editorial, suggestions, source-quality, agents, and the rest) still have zero receipt coverage — this session covered the exact surfaces the audit brief named plus one adjacent pair, not the whole admin panel. `org_contacts` (`migrations-pcd-ops/0028`) is confirmed **not applied** to production (live-verified), so there is no live PII exposure there today, but the table has no receipt coverage designed for it yet either, for whenever it is enabled. Tests could not be executed in this session's sandbox (`npx vitest run` bus-errors here on every test file, not just this one — a pre-existing, previously documented sandbox limit, see the Pillar 9 detail notes above); read carefully against the real migration SQL and the real library code instead, and flagged here rather than claimed as a green run. |
| 14 | Mobile-First Experience | fixed | **First run 2026-07-30.** Pillar added to the standard the same day, out of this pass. Rendered the deployed site at 390px (iframe render; the site sets `X-Frame-Options: DENY`, so the page HTML was fetched and reinjected with a `<base>` tag) and read the homepage end to end. Mobile page height was 9,563px. **Found and fixed: (1) a section that repeated itself.** `latestRail` (hero right rail, "Right off the desk") and `pinnedFinal` ("Fresh at the Desk") both read off the top of the same sorted `all` array in `src/pages/index.astro`, so all three Fresh cards were already in the rail. On desktop the rail is a right-hand column and the two read as separate shelves; on a phone the rail collapses under the hero copy and lands ~950px above the band, so a reader saw the same three headlines twice with nothing between them but a background change. `Fresh at the Desk` is now `hidden md:block`. Desktop still overlaps by design and is logged as open item #49. **(2) Four text-only bands.** This Season, Find your sport, Tools, and If you only read one thing rendered on mobile with no image, no icon, and no visual rule. Added: `TopicIcon` inline SVGs to the three This Season news cards (mapped by category) and to the four Tools cards, sport photo thumbnails to all eight Find your sport tiles, and a photo to the featured long read. The Tools cards hide their blurb below `md`, which had left a title and an arrow and nothing else on the device with the least context; the icon is what carries them now. **(3) No responsive images anywhere on the page.** Every illustration is authored at 1536w and every `<img>` pointed at that one file with no `srcset`, no `sizes`, and no `width`/`height`. The 375px-wide hero was pulling the full 134KB source. Added `src/components/Illo.astro` (emits `srcset` at 480w/960w/1536w plus intrinsic `width`/`height`) and `scripts/build-illustration-variants.py` (`npm run illustrations:variants`), which wrote 32 committed variants for the 16 images the homepage uses. Hero drops 134KB to 22.5KB on a phone. The missing `width`/`height` was also a live CLS liability on every one of them. **(4) The mobile nav did not announce its own state.** `NavBar.astro`'s Menu button had `aria-label` but no `aria-expanded` and no `aria-controls`, so a screen reader heard "Menu, button" and never learned whether the panel was open. On a phone that button is the entire navigation. Added both, with the toggle handler now keeping `aria-expanded` in sync, and `id="mobile-menu"` on the panel. **(5) Caching (Pillar 8 overlap, fixed here).** `/illustrations/*` had no rule in `public/_headers`, so every illustration inherited the framework default `public, max-age=0, must-revalidate` and revalidated on every visit. Confirmed live off the deployed response, not from config. Added the `immutable` one-year rule. Verified clean and not changed: no horizontal overflow at 390px (`scrollWidth` 375 against a 390 viewport), `lang="en"`, skip link present, one `main`/`header`/`footer`, heading order clean h1 → h2 → h3, alt text present and descriptive on all 7 homepage images. **Open at close of this pass:** 27 tap targets render under 44px (counted at 390px, not itemized this session) and desktop still shows the duplicate band. See open items #49 and #50. Astro build could not be run to verify: `astro build` bus-errors in this session's sandbox against the mounted filesystem. All four touched `.astro` files were validated through `@astrojs/compiler` `transform()` with zero diagnostics instead. `npm run build` and `npm run check` still need a run on Jeff's machine before the PR. **2026-07-31 (second pass, Pillars 4/6/14, `reports/AUDIT-2026-07-30-pillars-04-06-14-ui-a11y-mobile.md`):** re-measured the deployed site at 390px using the same fetch+iframe technique. Homepage: 28 elements under 44px, but every one traces to source classes (`Footer.astro`, `Logo.astro`, `index.astro`'s two `t-cta` links) that already carry `min-h-11` — confirmed by diffing live DOM against current source, this is a stale-production artifact (branch fixes not yet deployed), not an open source defect; item #50 is resolved in source. Measured three more pages this pass, which the 2026-07-30 run never touched: `/what-to-buy/` (5,996px, 29 elements under 44px, not source-verified this pass), `/sports/soccer/` (10,519px, 36 under 44px, not source-verified), and **`/camps/` at 84,749px — roughly 10x this pillar's own ~8,000px flag** and 396 elements under 44px. Root cause: `camps/index.astro` calls `listApprovedCamps(env.DB)` with no limit and renders every one of 1,300+ approved camps as a full `<li>` card server-side (already flagged as an over-fetch finding under Pillar 7, not previously connected to its mobile-height consequence). Not fixed this session: the page's ~450-line client-side map/filter script depends on the full `campsLite` JSON payload being present, so capping the server-rendered card list without breaking search/filter needs a real design decision (windowing, incremental render, or a paginated fallback list under the map), not a mechanical edit — flagged as new open item below rather than risked blind. Extended responsive images sitewide per this session's brief: generated `-480w`/`-960w` variants for all 268 `public/illustrations/*.webp` source files (was 16), converted every remaining static illustration `<img>` to `<Illo>` across 11 files (`body/index.astro`, `youth-sports-pendulum.astro`, `team-parent/index.astro`, `pathways/index.astro`, `parent-coach.astro`, `newsletter.astro`, `NewsletterSignup.astro`, `PillarLayout.astro` ×2, `LeadMagnetCTA.astro`, `cost-calculator.astro`, `camps/index.astro`), and converted `ArticleLayout.astro`'s per-article hero image (renders on all 812 published articles, the single highest-leverage image on the site) to `<Illo>` with the base name derived from the article's `hero` frontmatter field, with a plain-`<img>` fallback for the theoretical non-illustrations case. Added `srcset`/`sizes` (not the `Illo` component, since the crop and default dimensions differ) to the hero thumbnails in `reads/[topic].astro` and `team-parent/[topic].astro` article-grid cards. All 14 touched files validated through `@astrojs/compiler`'s `transform()`, zero diagnostics. Confirmed item #36 (810 OG cards) already regenerated against the corrected navy palette this session's baseline prep (812 files in `public/og/`, script's color constants already matched to `pcd-tokens.mjs`) — no further action needed, closing that follow-up. |
| 10-R2 (anchor: PILLAR-10-2026-07-31) | SEO & Discoverability | fixed, with new findings | **2026-07-31 full re-run, scripted across all 1,862 content-collection pages, not spot-checked.** Full detail: `reports/AUDIT-2026-07-30-pillars-10-11-12-seo-emotion-tax.md`, script at `scripts/seo-title-desc-audit.mjs`. **Title >60 chars: 1,005/1,862 pages (54%)**, structural — every title gets a 21-char ` | Parent Coach Desk` suffix (`BaseLayout.astro`) and several collections (`body`, `resources`, `pathways`, `recruiting`, `adaptive`, `rules`, `decisions`) have no `seoTitle` override at all. Worst: `body` 167/177, `guides` 37/37, `news` 22/22, `pillar` 17/17, `adaptive` 18/18. **Description >160 chars: 263/1,862 (14%)**, worst `news` 22/22 and `seasonCalendars` 27/28. Zero missing descriptions (every description traces to a Zod-required field). **55 duplicate-title groups**: 53 are same-named drills shared across baseball/softball in `coachingTips` (cosmetic, different content, low real cost), 2 are genuine duplicate articles under separate filenames (`camp-counselor-the-kid-hates.md`/`the-camp-counselor-your-kid-hates.md`, `packing-for-a-long-tournament-weekend.md`/`packing-for-tournament-weekend.md`) — flagged for Jeff, not merged (content/redirect decision, out of this session's lane). **Fixed:** `src/pages/sitemap-content.xml.ts` rewritten so every collection's URL builder lives in one map and `GET()` asserts `Object.keys(COLLECTION_URL_BUILDERS)` matches `Object.keys(collections)` imported from `content.config.ts`, throwing instead of silently shipping an invisible collection if a future collection is added and not wired in — closes open item #32 for real (its 2026-07-28 fix was already live and reverified this session: `curl` confirms all 17 `/pillar/` URLs in the live sitemap and the live pillar page 200s with a correct self-referencing canonical). **Verified live:** www→bare 301, robots.txt/sitemap.xml/sitemap-content.xml/sitemap-camps.xml all 200, the previously-404ing camp page now 301s, an unknown camp slug returns real 404. **Event JSON-LD honesty confirmed:** `camp-event-schema.ts` returns `null` rather than a partial/fabricated Event unless name, valid dates (end≥start), and a complete address are all present; no organizer, no fake ratings, no invented offers. **New finding (Medium, not fixed):** only `articles` (812 pages) get a unique per-page OG card (`/og/{slug}.jpg`); the other ~1,050 pages across 13 collections all share the single generic `/og-default.jpg` — technically satisfies "a real og:image exists," but undercuts shareability especially on the scripts save-block pages, which are exactly the content designed to be screenshotted/shared. **Recommendation, not applied:** shortening the title suffix (` \| PCD`, 6 chars vs 21) would reclaim 15 chars/page and close the smaller overflows, but would not fix the severe collections where the raw title alone already runs 90-130 chars; left for Jeff since it's a sitewide brand-visible string, not a mechanical SEO fix. GSC/Core Web Vitals: no dashboard access this session, `ORGANIC-SEARCH-AUDIT.md` (2026-07-12) and item #10 already document registration/sitemap/no-manual-actions; still a human step to re-pull. |
| 11-R1 (anchor: PILLAR-11-2026-07-31) | Emotional Design | scored, no launch-blocking fail | **First run, 2026-07-31.** Full detail: `EMOTIONAL-DESIGN-AUDIT.md` at project root. This site has **no persona documentation** (`qa/personas.md` does not exist; `EDITORIAL_VOICE.md`/`BUSINESS_PLAN_24MO.md` describe a single writer's-voice persona, not reader personas) — logged as finding zero, with a recommendation to write real personas from the reader interviews `BUSINESS_PLAN_24MO.md` already schedules. Four personas constructed from the site's own stated reader (camp-search parent, newly-volunteered team parent, drive-home parent, camp director) and walked through camp search, cost calculator, article reading, Friday Letter signup, and camp submit/claim. **No core workflow trips the one rule.** Emotional scores: scripts save-block 9/10 (tension relief built into the content schema itself — `whatTheyAreFeeling`→`whatToSay`→`whatNotToSay`→`theRule`), cost calculator 8/10 (no signup wall, honest medians, editable), camp finder 7/10 (honest "confirm with provider" price language, real verification methodology link — docked for the Pillar 14 mobile-scale problem on `/camps/`), article reading 7/10, camp submit/claim completion **4/10** — the real finding: an honest, well-framed form ("self-edit info, add a logo... Free") ends in a flat `"Claim request received"` / `"Submitted. We will review..."` with no artifact, no reference, no visible next action — the textbook "completed step, no next action" fail this pillar names by name. Newsletter signup 5/10 — honest copy but no in-page payoff preview before handing off to Kit's hosted form. Three wow moments logged: the scripts save-block, honest camp-card price/verification language, and the no-signup-wall cost calculator. Ranked change list (highest first): give claim/submit completion a real artifact, add a copy/share affordance to the save block, extend unique OG images to the scripts/decisions collections, preview newsletter payoff pre-click. None launch-blocking; this pillar is scored, not gated, per its own rule. |
| 12-R1 (anchor: PILLAR-12-2026-07-31) | Sales Tax & Nexus | waived | **First run, 2026-07-31. This site takes no money today — waived, one line, per this pillar's own scoping rule.** Checked for a live checkout: no `stripe` package in `package.json`, no Stripe/checkout code in `src/`; the only Stripe reference anywhere in the repo is a comment in `src/pages/api/camps/[slug]/claim.ts` describing a *future manual* step ("Admin verifies ownership, sends payment link (Stripe / invoice), then marks claim paid"), not a built automated checkout. `BUSINESS_PLAN_24MO.md`'s four revenue lines (affiliate commission, display ads, manually-invoiced camp sponsorship, Year-3 white-label gear) are all either money paid *to* PCD by a network/advertiser or not yet built — none is PCD running a multi-state checkout today. Matches the existing Pillar 5 row's independent finding ("No payment taken anywhere on the site"). **Trigger condition, named per the pillar's own rule:** the day camp sponsorship/claim fees move from manual invoicing to an in-site Stripe checkout, or Year 3's white-label storefront ships, this pillar goes live and `NEXUS-MAP.md` gets built against real Stripe customer-by-state data at that point. No `NEXUS-MAP.md` written this session — correct per the pillar's instruction not to manufacture findings on a site with no checkout. |

## Pillar 8 detail (Operations & Reliability) — 2026-07-05

**Done this session:**
- `Outputs/CONTINUITY.md` created (did not exist; every site's `/web:ops` checks for it). Account rows need Jeff to fill in login emails, recovery-code locations, and 2FA methods — that part cannot be inferred from a repo.
- `INCIDENT-RUNBOOK.md` added at the project root: what to check first, how to roll back (Cloudflare Pages rollback needs no build), admin-access recovery, camps-data-looks-wrong steps, and the written recovery point/time (D1 point-in-time restore, ~30 days; Pages rollback under 2 minutes).
- Business-metric alert scaffolded: `src/pages/api/cron/camps-sweep.ts` now pings an optional `OPS_HEARTBEAT_URL` (UptimeRobot heartbeat pattern from the standard) only when `approved_future_count` is healthy. No URL configured yet, so this is inert until Jeff creates the monitor and sets the var.
- Found and fixed a real gap: nothing was ever calling `/api/cron/camps-sweep` on a schedule (Cloudflare Pages has no native cron trigger, and no scheduled workflow existed). Added `.github/workflows/camps-sweep-cron.yml`, a daily GitHub Actions cron that calls the endpoint. It needs a `CRON_KEY` repo secret matching the Pages secret of the same name — see open items.

**Still open (human/dashboard steps, not code):**
- UptimeRobot monitor on the live URL — not yet created.
- Sentry (this counts as a data app, per the standard) — not yet set up.
- Cloudflare account notifications for Worker error rate/usage — not yet confirmed on.
- A restore has never actually been run and logged (`INCIDENT-RUNBOOK.md` restore-test table is empty). No date, no pass, per the standard.
- Load test (autocannon at 10/100/1,000 concurrent) against a preview URL — needs a live preview URL and a shell, neither available this session.
- Structured JSON logging — the codebase uses plain `console.error(...)` calls (some already prefixed like `[cron]`), not structured objects. Not changed this session beyond what already existed; a full rollout across ~19 API routes is a larger change than this session's file-only, no-build-verification scope should risk. Flagged as a Low-severity follow-up, not a Critical — the site works, this is a debuggability gap, not an outage risk today.

## Pillar 9 detail (Testing & CI) — 2026-07-05

**Done this session:**
- `vitest` added as a devDependency, `vitest.config.ts` added, `npm test` / `npm test:watch` scripts added.
- `@astrojs/check` + `typescript` added for `npm run check` (astro check).
- `linkinator` added for `npm run test:links` (link check against `dist/`, per the static-site path of this pillar).
- `tests/helpers/context.ts`: a shared helper that builds a minimal Astro API context so route handlers can be called directly without a dev server or Workers runtime (plain-Vitest path, since this Pages+Astro app doesn't fit `@cloudflare/vitest-pool-workers` cleanly).
- Full 3-test coverage (happy path, failure path, auth/bot-rejection) written for: `src/lib/admin-auth.ts` (the shared gate every admin route depends on), `POST /api/camps/submit`, `POST /api/camps/:slug/claim`, `POST /api/camps/:slug/reviews/submit`, `POST /api/admin/camps/:id/approve` (representative admin route), and `POST /api/cron/camps-sweep` (including a test for the new heartbeat behavior).
- `.github/workflows/ci.yml` added: npm ci, npm audit --audit-level=high, build, test, astro check, on every push/PR to main. Copied to `TEMPLATES/web/ci.yml` as the shared source (this is the reference site).
- `.github/dependabot.yml` added (npm weekly, github-actions monthly).
- `About Me/Deployments.md`'s parentcoachdesk block updated: `npm test` now sits between `npm run build` and the wrangler deploy line.

**Verified 2026-07-05 (Jeff ran it locally, full cycle):** `npm install` and `npm run build` both passed clean (worker bundle 0.62 MiB, well under the 25 MiB limit). `npm test` initially failed 3 of 6 suites with `[vitest] Cannot access 'mockCamp'/'mockPendingCamp' before initialization` — a `vi.mock()` hoisting bug (the factory referenced a `const` declared later in the same file; `vi.mock()` calls are hoisted above every other statement, including top-level consts, so the reference hit the temporal dead zone). Fixed in `admin-camps-approve.test.ts`, `camps-claim.test.ts`, and `reviews-submit.test.ts` by removing the default `.mockResolvedValue(...)` from inside each factory and relying solely on the `beforeEach()` that was already setting it. **Re-run confirmed: all 6 suites now pass, 35/35 tests green.** A second `npm run build` also confirmed the `astro.config.mjs` redirect fix worked — the `/resources/drive-home-playbook` duplicate warning is gone; only the pre-existing, already-mitigated `/camps/[slug]` vs `/camps/[state]` warning remains (open item #12, not a regression).

`npm run check` (astro check) then surfaced 245 pre-existing type errors (not introduced this session). Two were real, cheap fixes, made this session:
- `src/pages/governing-bodies.astro` had a truncated closing tag, `</BaseLay` instead of `</BaseLayout>` — a genuine syntax bug that Astro's build compiler tolerated but `astro check`'s stricter parser caught. Fixed.
- `@cloudflare/workers-types` was never a devDependency, so every reference to `D1Database`, `R2Bucket`, and `ExecutionContext` across ~25 files (all API routes, admin pages, and the two standalone Cloudflare Workers under `worker-cron/` and `worker-link-checker/`) read as "Cannot find name" — roughly 100+ of the 245 errors. Added the package to `package.json` and `"types": ["@cloudflare/workers-types"]` to `tsconfig.json`. Not yet verified — needs `npm install` then `npm run check` again to confirm this clears that whole class of error.

**Not fixed this session (real, but out of scope for a blind, unverified pass):** the remaining ~140 errors are pre-existing null-safety gaps (`camp` possibly null across `camps/[slug].astro` and `camps/index.astro`'s inline client script, mostly implicit-`any` on DOM event handlers), a content-schema type mismatch (`phase: 'team-parent'` isn't in `RelatedArticles.astro`/`PillarLayout.astro`'s narrower phase union, and `contributor` isn't on the shared article type), and one non-standard `<meta>` attribute in `BaseLayout.astro` (`value=` isn't valid on `<meta>`, should probably be `content=`). None of these are outage risks — they didn't stop `astro build` from succeeding — but they mean `npm run check` currently fails, which matters because the new `ci.yml` runs `npm run check` on every push. Logged as a new High open item (#14) rather than attempting ~140 unverified edits in one pass.

**Still open — routes without their three tests yet (High, since the standard's own bar is "no tests on a route handling auth, money, or PII" and several of these do):**
- `src/pages/api/admin/camps/[id]/photo.ts`
- `src/pages/api/admin/camps/[id]/update.ts`
- `src/pages/api/admin/camps/[id]/verify.ts`
- `src/pages/api/admin/camps/[id]/reject.ts`
- `src/pages/api/admin/reviews/[id]/approve.ts`
- `src/pages/api/admin/reviews/[id]/reject.ts`
- `src/pages/api/admin/claims/[id]/update.ts`
- `src/pages/api/admin/editorial/approve.ts`
- `src/pages/api/admin/suggestions/[id]/update.ts`
- `src/pages/api/camps/suggest.ts`
- `src/pages/api/camps/check.ts`, `src/pages/api/camps/nearest.ts`, `src/pages/api/camps/search-priority.ts`, `src/pages/api/search-event.ts` (read-only/lower-risk; Low, not High)

These all follow the same admin-gate pattern already proven in `admin-camps-approve.test.ts` — extending coverage to the rest is mechanical, not a redesign. Next `/web:test` pass on this site should close this list before it goes back to a clean pass.

**RESOLVED 2026-07-14 — all 14 routes above now have tests.** `tests/api/admin-camps-photo.test.ts`, `admin-camps-update.test.ts`, `admin-camps-verify.test.ts`, `admin-camps-reject.test.ts`, `admin-reviews-approve.test.ts`, `admin-reviews-reject.test.ts`, `admin-claims-update.test.ts`, `admin-editorial-approve.test.ts`, `admin-suggestions-update.test.ts`, `camps-suggest.test.ts`, `camps-check.test.ts`, `camps-nearest.test.ts`, `camps-search-priority.test.ts`, `search-event.test.ts`. Each covers happy path, failure path(s), and auth/cross-origin rejection where the route has an admin gate; the four public read-only routes (check/nearest/search-priority/search-event) cover happy path, validation failure, and a database-unavailable 500. All 107 tests (35 pre-existing + 72 new) pass under `npx vitest run`. No routes remain without their three-test minimum.

## `npm audit` — 2026-07-14

Started at 14 vulnerabilities (1 critical, 6 high, 6 moderate, 1 low), same count the 2026-07-05 pass could only observe (no shell access then to act on it).

**Fixed and verified (full green test suite + clean build after each):**
- `vitest` 2.1.4 → 4.1.10 — clears the **critical** vitest finding and its `@vitest/mocker`/`vite`/`vite-node`/`esbuild` (partial) dependency chain. Pure devDependency, isolated from the shipped Worker bundle. Config (`vitest.config.ts`) needed no changes; all 107 tests pass unchanged under v4.
- `linkinator` 6.1.2 → 7.6.1 — its only purpose in this repo is `npm run test:links` (a dev-only link checker against `dist/`), and the bump clears its transitive `gaxios`/`uuid` moderate chain and the `@astrojs/tailwind` low finding (which was only flagged because it depends on `astro`, not because of anything in the tailwind integration itself).

Result: 14 → 8 vulnerabilities (2 low, 1 moderate, 5 high, 0 critical).

**Waived — the remaining 8 (all one dependency chain: `astro`, `@astrojs/cloudflare`, `wrangler`, `esbuild`, `undici`, `ws`):** attempted the fix (`astro` 5.18 → 7.0.9, `@astrojs/cloudflare` 12.6 → 14.1.3, via `--legacy-peer-deps` since `wrangler`'s peer range and `@astrojs/cloudflare`'s peer range on `@cloudflare/workers-types` conflict). `npm install` succeeded and cleared to 0 vulnerabilities, but `npm run build` then hard-failed: Astro v6 removed the legacy content-collections config format this site uses (`src/content/config.ts` with `type: 'content'` + `z.object` schemas, no `loader`) — the fix requires migrating all 15 collections to the new Content Layer API (`loader: glob({...})`, moving the file to `src/content.config.ts`, and switching every consumer from `entry.slug` to `entry.id` where those differ). That's a real migration project — it touches every collection's schema and cascades into every route that calls `getCollection`/`getEntry` for those collections (dozens of files) — not a same-session safe-fix. Reverted `package.json`/`package-lock.json` to the pre-upgrade state (confirmed via `git diff` showing only the `vitest`/`linkinator` bumps) and re-verified clean build + 107/107 tests. **Waiver reason:** fixing these 8 requires a dedicated Astro v5→v7 migration session with live-preview verification before merge, not a blind dependency bump. Recommend scheduling that as its own piece of work rather than folding it into a security-gate-clearing session.

## `npm run check` — 2026-07-14: 215 → 0 errors

Starting count was 215 (already down from the 245 cited 2026-07-05 — some had been fixed in an intervening session). Fixed all of them; verified with both `npm run check` (0 errors) and a full `npm run build` (catches issues static type-checking alone can't, see below).

- **`src/pages/camps/[slug].astro` (90 errors → 0):** the frontmatter narrows `camp: Camp | null` correctly, but the JSX ternary chose between the state-landing branch and the camp-detail branch by testing `stateCities` truthiness, not `camp` — so TypeScript couldn't correlate the two variables and treated every `camp.foo` in the detail branch as possibly-null. Fixed by changing the ternary's else-arm from `) : (` to `) : camp && (`, which is behaviorally identical (the two conditions are a true 1:1 correlation by construction — camp is only ever set when the state-landing branch isn't taken) but lets TypeScript narrow `camp` directly. Also fixed the two inline `<script>` blocks (review-form and claim-form submit handlers): switched `var form`/`var status` to `const`, added `HTMLFormElement`/`HTMLElement` casts on `getElementById`/`querySelector`, and typed the `.then()` callback via `r.json() as Promise<{...}>` (Cloudflare's `workers-types` package, present for the server-side routes, makes `Response.json()` resolve to `unknown` instead of `any` even in these client-side scripts, since it's a global ambient type).
- **`src/pages/camps/index.astro` (105 errors → 0):** the ~450-line client-side map/filter script had zero type annotations. Added a `LiteCamp`/`SportOption`/`FilterState` interface matching the JSON payload actually serialized into the page (`campsLite` in frontmatter), typed every helper function signature (`sportLabel`, `haversineMiles`, `geocodeZip`, `jitterFor`), typed `map`/`clusterGroup`/`visibleCamps`, and cast every `document.getElementById`/`querySelector` call and `e.target` to its real element type. One recurring quirk: casting straight to `HTMLSelectElement` failed with "neither type sufficiently overlaps" (again the workers-types/lib.dom interaction) — worked around via `as unknown as HTMLSelectElement`, the pattern TS's own error message suggests.
- **`src/layouts/ArticleLayout.astro`, `src/pages/team-parent/[slug].astro`, `src/pages/rules/[sport].astro`, `src/layouts/BaseLayout.astro`, `src/components/RelatedArticles.astro`, `src/layouts/PillarLayout.astro`, `src/pages/admin/preview/[collection]/[slug].astro`, `src/pages/cost-calculator.astro` (33 errors → 0), each a small, independent fix:**
  - `ArticleLayout.astro`'s `data.hero.src` branch was genuinely dead code — the `articles` collection's `hero` field is `z.string().optional()` (confirmed in `src/content/config.ts`), never an object, so the `typeof data.hero === 'string' ? data.hero : data.hero.src` ternary's else-branch could never execute. Simplified to just `data.hero`.
  - `RelatedArticles.astro` and `PillarLayout.astro` both passed `contributor={a.data.contributor}` to `ArticleCard` — `contributor` doesn't exist on the article content schema **and** `ArticleCard`'s own `Props` interface doesn't declare or use a `contributor` prop anywhere in its body. Confirmed dead pass-through, removed both call sites.
  - `PILLARS` (in `src/data/site.ts`) was missing a `'team-parent'` entry even though the `articles` schema's `phase` enum includes `'team-parent'` and 40 published articles use it — `ArticleLayout.astro`'s `PILLARS[data.phase]` would return `undefined` for any of them (it doesn't happen today because `team-parent` articles route through their own dedicated layout in `team-parent/[slug].astro`, not `ArticleLayout`, but the type gap was real). Added a `team-parent` entry. **This broke the homepage** ("three drives" section) and `ThreeDrivesNav.astro`, both of which did `Object.values(PILLARS)` assuming exactly 3 entries — `npm run build` (not `npm run check`) caught the resulting `Cannot read properties of undefined (reading 'src')` crash on `/index.html`. Fixed by making both call sites reference `PILLARS['drive-there']`, `PILLARS.game`, `PILLARS['drive-home']` explicitly instead of iterating the whole object — this is also more correct regardless of the type fix, since "the three drives" is a specific, named concept, not "whatever's in the PILLARS object."
  - `BaseLayout.astro`'s `<meta name="impact-site-verification" value="...">` uses `value=` instead of `content=` **on purpose** — that's Impact.com's actual affiliate-network verification tag format, not a typo, and changing it would break real affiliate verification. Bypassed the JSX attribute type check via a spread-with-cast (`<meta {...({name, value} as any)} />`) instead of changing the rendered attribute.
  - `rules/[sport].astro`: `piece.data.faqs?.length > 0` (comparing possibly-`undefined` to a number) → `(piece.data.faqs?.length ?? 0) > 0`.
  - `team-parent/[slug].astro`: one `article.data.sport` reference was missing the `!` non-null assertion its two sibling lines already used — added for consistency.
  - `admin/preview/[collection]/[slug].astro`: `CollectionEntry<any>` was resolving `entry.data`/`entry.render()` to `unknown` rather than `any` under this project's strict settings. This file already casts loosely everywhere else (it's a generic internal admin preview tool spanning 12 collections), so switched the destructure to `entry: any` to match.
  - `cost-calculator.astro`: a generic arrow-function helper `<T extends HTMLElement = HTMLElement>(sel) => ...` hit the same workers-types/lib.dom constraint mismatch as the `HTMLSelectElement` casts above; loosened to `<T = HTMLElement,>` (trailing comma needed so the parser doesn't read `<T ...>` as a JSX tag in this file's syntax mode).
- **`tsconfig.json`:** added `activityradar-archive` to `exclude`. That folder is explicitly documented (top of this file) as "historical record only, no longer maintained separately" post-merge — it was never shipped and isn't part of the active site, so it shouldn't count toward this pillar's type-error bar. Removes 4 dead errors (1 syntax error in an archived `tailwind.config.mjs`, 2 in an archived API route, 1 in an archived page) that had nothing to do with the live site.

**Verification:** `npm run check` → 0 errors, 0 warnings (64 pre-existing hints, unchanged, not part of this pillar's bar). `npx vitest run` → 107/107 passing. `npm run build` → clean, exits 0, worker bundle 0.62 MiB, confirmed the homepage's three-drives section still renders exactly 3 cards (`grep -c "Drive 0" dist/index.html` → 3) after the `PILLARS` fix-and-correction above.

## Pillar 6 detail (Accessibility) — 2026-07-07 re-verification

**Contrast math (WCAG relative-luminance formula, computed by hand this session, not approximated):** `rust_old = #C5713D` on `paper = #FAF6EE` is 3.41:1 (fails 4.5:1 for normal text). `rust_new = #9E5228` on `paper` is 5.45:1 (passes). `rust_new` on `paper-warm = #F2EAD9` is 4.80:1 (passes; `rust_old` there was 3.00:1, fail). `ink-soft = #5F5448` on `paper` is 6.98:1 (already passing, no change needed). `#5C7459` (the site's status-green, used for success messages) on `paper` is 4.77:1 (already passing). One nuance the interrupted session's token swap did not account for: on the dark hero sections (`background: #2D2520`), `rust_old` was already only 4.01:1 — passing only because those instances are large text (≥24px) against the 3:1 large-text floor. `rust_new` on that same dark background computes to 2.51:1, which would have failed even the large-text floor. Three homepage headline `<em>` spans (`index.astro` lines ~360/371/382) sit at `text-xl` (20px) with `font-weight: 500`, which is normal text under WCAG (not "large," since large requires 24px regular or ~18.7px bold) — these were failing at 4.01:1 even before this session and are not fixable by darkening rust further without also failing the large-text instances, so they were switched to `#D4AB6A` (the site's existing "trophy" dark-background accent, already used for the eyebrow label directly above each of these three headlines) — 6.77:1 on ink, passes. The two h1/h2-level uses of rust-on-dark (`index.astro` ~341, `parent-coach.astro` ~50) are true large text and were left on `rust_old` at 4.01:1 (passes 3:1), since swapping to `rust_new` would have dropped them to 2.51:1 and failed. The button at `index.astro` ~347 (`.btn`, 12px paper-on-rust) was failing at 3.41:1 and is fixed to `rust_new` (5.45:1).

**Every file where a hardcoded `#C5713D` text color was changed to the compliant value:** `src/components/NewsletterSignup.astro`, `src/pages/decisions/[slug].astro` (×2), `src/pages/youth-sports-pendulum.astro` (×2), `src/pages/burnout-check.astro`, `src/pages/admin/link-health/index.astro` (×2), `src/pages/admin/editorial/index.astro`, `src/pages/admin/camps/[id].astro`, `src/pages/camps/submit.astro` (×3, JS-set), `src/pages/camps/suggest.astro` (×2, JS-set), `src/pages/camps/[slug].astro` (×4, JS-set), `src/pages/index.astro` (button background + 3 headline `<em>` spans switched to trophy instead, per above). Left untouched on purpose: `border-color`/decorative/chart-fill/SVG-icon uses of the old hex in `Logo.astro`, `TopicIcon.astro`, `SeasonCalendarChart.astro`, `pathways/[sport].astro`, `season-calendar/index.astro`, `rules/[sport].astro`, `admin/image-needs.md`, content markdown — none of these render as text-on-background contrast pairs, so they are a brand-consistency note, not an a11y defect.

**aria-live additions (WCAG 4.1.3, dynamic status messages):** `src/pages/camps/submit.astro`, `src/pages/camps/suggest.astro`, `src/pages/camps/[slug].astro` (both the review-status and the newly-labeled claim-status elements), `src/pages/admin/camps/queue.astro` (all three `.status-message` divs — camp queue, review queue, claim queue), `src/pages/search.astro` (results count label + results container). None of these had `aria-live` anywhere in the codebase before this pass; only `ConsentBanner.astro` and `pre-game-routine.astro` used it previously.

**Missing form labels (WCAG 1.3.1 / 4.1.2):** `src/pages/camps/[slug].astro`'s "claim this listing" form had five inputs (`claimant_email`, `claimant_name`, `organization`, `phone`, `notes`) with only `placeholder` text and no `<label>` — added `sr-only` labels matching the visible-label pattern already used in the review form one section above it in the same file.

**Data table headers (WCAG 1.3.1):** added `scope="col"` to every `<th>` in the five admin tables that had none: `admin/editorial/index.astro`, `admin/link-health/index.astro`, `admin/search-signals.astro` (two separate tables), `admin/source-quality.astro`.

**Heading hierarchy (WCAG 1.3.1):** `about.astro` and `search.astro` both went h1 straight to h3 with no h2 anywhere on the page — promoted the h3s (about.astro's three "Where to start" cards; search.astro's "Popular reads" cards and its JS-rendered results-list template) to h2. `cost-calculator.astro` and `camps/index.astro` were flagged as structurally flat (h1-only, or section labels styled as headings but left as `<p>`) but are not an actual WCAG skip-violation, so left alone per the minimal-edit instruction — noted here as a lower-priority follow-up, not a defect.

**Broken/truncated files found and fully repaired (pre-existing in the committed source, confirmed via `git show HEAD:<path>`, not something newly interrupted this session):** `src/layouts/ArticleLayout.astro` was cut off mid-tag (`<RelatedArticles articles={related}` with no `/>`, no `</BaseLayout>`) — every article page renders through this layout, so this was a live, build-breaking defect independent of any a11y checklist item; also carried a duplicate `class` attribute on the article's `<h1>` in the same file, fixed in the same edit. `src/pages/cost-calculator/methodology.astro` was cut off mid-sentence with no closing tags — completed the sentence and closed the tree. `src/pages/admin/camps/spot-check.astro` had a second instance of the duplicate-`class`-attribute bug on its action-result banner — fixed.

**Confirmed accurate from the 2026-07-05 pass (verified against live source, not re-taken on faith):** skip link (`src/layouts/BaseLayout.astro` line ~160, visible on focus via `.sr-only.focus:not-sr-only`, targets `<main id="main">`), global focus-visible ring (`*:focus-visible { outline: 2px solid var(--rust); ... }` in `global.css`), one header/nav/main/footer landmark set with no duplicates, nav dropdown ARIA (`aria-haspopup="true"` + `aria-expanded` toggled by JS on open/close, plus Escape-to-close and outside-click-to-close) in `NavBar.astro`, a real `@media (prefers-reduced-motion: reduce)` block in `global.css` (not just stated on the accessibility page), and the WCAG conformance statement on `/accessibility/` (site text currently says "WCAG 2.2, level AA" — a stricter, newer version than this pillar's 2.1 AA bar, which is a superset, not a contradiction).

## Pillar 6 detail (Accessibility) — 2026-07-29 navy/silver-green contrast re-verification

Ran the WCAG 2.1 relative-luminance formula (real sRGB linearization via a Python script, not eyeballed) against every foreground/background pair in the new palette. Full pass: navy `#041E42`, ink-soft `#2E3A45`, muted `#5A6468`, accent/rust `#003594`, accent-hover `#002873`, and silver-deep `#5F7075` all clear AA normal text (4.5:1+) on white, `#EDF1F5` (page bg), and `#F4F7FA` (stripe). Confirmed the two spot-checks the brief named: decorative silver `#8A9BA0` is 2.89:1 on white (fails, decorative-only, as documented in `pcd-tokens.mjs`); silver-deep `#5F7075` is 5.17:1 on white / 4.56:1 on `#EDF1F5` (passes, the text-safe substitute). New finding: `#8A9BA0` on navy `#041E42` is 5.73:1 — **passes** — so the site's several dark (`bg-ink`) closing-statement bands can use it as an eyebrow-label color; used it there and moved every other silver-as-text instance (found on light backgrounds) to `#5F7075`.

**Real defect found and partly fixed:** the divider/border token `#C9D2DA` (`--bone`/`--line`) is only 1.53:1 on white and 1.35:1 on `#EDF1F5` — fails the WCAG 1.4.11 non-text-UI 3:1 floor — and cross-referencing actual usage found it is the *only* visible boundary on real `<input>`/`<select>`/`<textarea>` elements across dozens of files: public forms (`camps/submit.astro`, `camps/suggest.astro`, `camps/[slug].astro`, `proof.astro`, `trust.astro`) and nearly every admin tool. Did not touch the shared `--bone`/`--line` token itself (it's also the decorative card-border/table-rule color in ~4187 other usages; redarkening it site-wide is a visible design call outside this pass). Instead, scoped the fix to the files this session already had open: `admin/camps/[id].astro` (22 inputs), `proof.astro` (5), `cost-calculator.astro` (4) — swapped `border-bone` → `border-muted` on those specific controls (`#5A6468`, 5.35:1 on `#EDF1F5`, passes). The identical gap remains, not fixed, in `components/DataQualityRow.astro`, `admin/camps/index.astro`, `admin/camps/queue.astro`, `admin/camps/spot-check.astro`, `admin/claims/index.astro`, `admin/trust/deliveries.astro`, `admin/trust/drafts.astro`, `admin/trust/index.astro`, `camps/submit.astro`, `camps/suggest.astro`, `camps/[slug].astro`, `trust.astro` — see Open item #35.

**Tap targets (WCAG 2.5.5/2.5.8 spirit, per this project's own 44px convention already on `.btn`/`.btn-ghost`/`.btn-soft`):** `.tag-pill` (`global.css`) is 32px `min-height`, used both decoratively (topic tags on article cards — fine at 32px, non-interactive) and as real interactive filter buttons (`PillarLayout.astro`, camps state/city/sport filter pages). Left the shared class alone (enlarging it changes every decorative tag site-wide) but added `min-h-11` (44px) directly to the three real filter buttons in `PillarLayout.astro`. The same gap on `camps/[state]/[city]/[sport]/index.astro`'s filter chips, and several `border-bone`-only admin filter-bar buttons with no explicit min-height (`admin/editorial/index.astro`, `admin/link-health/index.astro`), is listed as a follow-up rather than fixed — see Open item #35.

## Open items

| # | Severity | Pillar | Item |
|---|----------|--------|------|
| 1 | RESOLVED | 4 — UI | ~~NavBar hardcoded hex~~ — fixed 2026-07-05, now reads `var(--rust)` / `var(--ink)`. |
| 2 | RESOLVED (pending deploy) | 1 — Security | ~~Admin-auth.ts ran the CF Access JWT LEGACY (unverified) path because ACCESS_TEAM_DOMAIN/ACCESS_AUD were unset.~~ **Both set 2026-07-18** as Pages secrets on `parent-coach-playbook`: team domain `fieldforge.cloudflareaccess.com` and the Production Admin app AUD (read live from the Zero Trust dashboard). `access-jwt.ts` runs VERIFIED once these bind, i.e. on the next deploy — resolved on deploy. |
| 3 | RESOLVED | 1 — Security | ~~`CRON_KEY` not documented as a configured Pages secret~~ — confirmed 2026-07-14 via `npx wrangler pages secret list --project-name parent-coach-playbook`. New follow-up: item #16 below (GitHub Actions repo secret still unset). |
| 4 | MEDIUM, still open | 1 — Security | Gitleaks full-history scan still not run **as the real binary** (2026-07-31: `which gitleaks` empty, `npx gitleaks` resolves to an unrelated npm package, not the Go scanner). This session's manual sweep (ripgrep pattern match on the current tree, clean) plus three attempts at a full-history pass (the repo's own `scripts/scan-secrets.mjs --history`, `git log -p`, `git log -S<pattern>`) all timed out against this sandbox's FUSE-mounted git history before completing — same I/O-latency limitation this repo's Pillar 9 notes already document elsewhere. The last real gitleaks run on record stays **2026-07-22** (clean, 2 allowlisted non-credential AUD values) and CI runs the repo-native equivalent on every push with full history, so production isn't blind — but a fresh real-binary run is still owed. Run `gitleaks git .` from a non-sandboxed machine and log the scan date in `SECURITY-AUDIT.md`. |
| 5 | RESOLVED | 9 — Testing | ~~13 of 19 API routes still need their three-test minimum~~ — closed 2026-07-14. All 14 routes now have the 3-test minimum (happy/failure/auth); 107/107 tests pass. See Pillar 9 detail above. |
| 6 | RESOLVED (pending deploy) | 8 — Ops | Monitoring baseline complete 2026-07-18. Uptime: UptimeRobot free HTTP monitor on parentcoachdesk.com (5-min), live. Heartbeat: Healthchecks.io free "PCD camps-sweep" check; ping URL set as the `OPS_HEARTBEAT_URL` Pages secret the camps cron pings (period 1 day, grace 2h). Errors: Sentry — `SENTRY_DSN` Pages secret + `PUBLIC_SENTRY_DSN` in `.env` both set. Usage: Cloudflare Budget alert created ($15 to eepskalla@gmail.com) on the pay-as-you-go account. All three legs wired; live on the next deploy. |
| 7 | MEDIUM | 8 — Ops | No restore has ever been run and logged. `INCIDENT-RUNBOOK.md` has the empty table waiting for it. |
| 8 | LOW | 8 — Ops | Load test (autocannon, 10/100/1,000 concurrent against a preview URL) not run — needs a live preview URL and a shell. |
| 9 | LOW | 4 — UI | Lighthouse mobile numbers (LCP/CLS/INP) not re-measured this session against the live site. |
| 10 | RESOLVED | 10 — SEO | ~~GSC registration/sitemap status not confirmable from repo~~ — confirmed live 2026-07-12: property created June 10 2026, both sitemaps read Success (index 3,024 URLs, camps 1,056), no manual actions. Full findings in `ORGANIC-SEARCH-AUDIT.md`. |
| 11 | INFO | 8 — Ops | `TEMPLATES/web/` already has `design-tokens.css` and the legal templates from an earlier session — corrected a mistaken note in this file's prior draft that said they were missing. Only `ci.yml` was actually new this session. |
| 12 | LOW | 7 — Stack | Astro build warns `/camps/[slug].astro` and `/camps/[state]/index.astro` are two files matching the same single-segment SSR route shape. Not a live bug — `[slug].astro` already inline-detects 2-letter state codes and renders the state view itself (see comment at its top), so `/camps/wa/` works today. Astro's own warning says a future version makes this a hard build error. Fixing it for real means moving state-code routing off the `/camps/:slug` path (e.g. keeping state pages only under `/camps/[state]/`, dropping the inline dispatch hack) — a routing change, not a one-line fix, and one that needs a working build to verify before shipping. Found and fixed the same session's other build warning (duplicate `/resources/drive-home-playbook` redirect declaration in `astro.config.mjs`) since that one was a genuine one-line redundancy. |
| 13 | WAIVED | 9 — Testing | `npm audit` was 14 vulnerabilities (1 critical, 6 high, 6 moderate, 1 low). **2026-07-14:** fixed 6 of them (critical `vitest` chain + moderate/low `uuid`/`gaxios`/`@astrojs/tailwind` chain) via safe devDependency bumps, verified with a clean build and 107/107 tests. The remaining 8 (all one chain: `astro`, `@astrojs/cloudflare`, `wrangler`, `esbuild`, `undici`, `ws`) need a major Astro v5→v7 upgrade. **Second attempt, same day:** got further this time — migrated all 15 content collections to the Content Layer API cleanly — but hit a hard platform blocker: Astro 6+ removed Cloudflare **Pages** support from `@astrojs/cloudflare` entirely (confirmed via [withastro/astro#16107](https://github.com/withastro/astro/issues/16107); it only targets Cloudflare Workers now). This site deploys via `wrangler pages deploy`, so the fix isn't a code migration, it's a Pages→Workers platform migration. Reverted again. Full scoping brief for that migration — including the already-validated content-collections config, ready to reuse — is at `PAGES-TO-WORKERS-MIGRATION-BRIEF.md`. Waived pending that dedicated migration project. **Migration progress, same day, on branch `migration/pages-to-workers-staging` (unmerged, production unaffected):** Card M1 proved a live Astro 7 + Workers deploy on a throwaway staging Worker; Card M2 landed the 51-file `entry.slug`→`entry.id` / `render()` rename; Card M3 swapped all 38 files off the dead `Astro.locals.runtime.env` pattern to `import { env } from 'cloudflare:workers'`, verified live against staging for every read path and the public write paths (submit/claim/review-submit), with the 12 Cloudflare-Access-gated admin mutation routes confirmed reaching their auth gate correctly but not yet exercised end-to-end (pending a real Access login — see `PAGES-TO-WORKERS-MIGRATION-BRIEF.md`). Waiver still holds: production is still on Astro 5/Pages until this branch is reviewed and cut over. |
| 14 | RESOLVED | 9 — Testing | ~~`npm run check` fails with 245 type errors~~ — closed 2026-07-14. Burned down 215 → 0 (see Pillar 9 detail above for the fix-by-file breakdown, including one fix that briefly broke the homepage and was caught by `npm run build` before landing). `activityradar-archive/` (historical record only, no longer maintained) excluded from `tsconfig.json` since it isn't part of the live site. |
| 15 | INFO | 6 — Accessibility | This session's Pillar 6 re-verification changed ~25 source files (contrast fixes, aria-live, form labels, table scope, heading levels, and two build-breaking truncated-file repairs in `ArticleLayout.astro` and `cost-calculator/methodology.astro`). None of this is live until the next `wrangler pages deploy` — redeploy to ship these fixes. |
| 16 | RESOLVED | 1 — Security | ~~GitHub Actions repo secret `CRON_KEY` was unset.~~ **Resolved 2026-07-18.** A fresh 32-byte hex `CRON_KEY` was generated and set identically in the Cloudflare Pages secret and the GitHub Actions repo secret via `pcd-ops-setup.ps1` (wrangler "Uploaded secret CRON_KEY" and gh "Set Actions secret CRON_KEY" both confirmed). Binds on the next deploy. |
| 17 | HIGH, stays open pending Jeff's dashboard confirmation | 1 — Security | **2026-07-15.** Live OpenAI API key was hardcoded in `gen_hero_image.py` (Cowork root) and in plaintext in `About Me/openai-config.md`. **2026-07-31 verification:** `gen_hero_image.py` no longer exists anywhere in the Cowork tree (a repo-wide search found nothing) and `About Me/openai-config.md` now carries no plaintext key — its text states the key was rotated 2026-07-30. Both files are clean. What this session could not do: confirm the OpenAI dashboard itself shows the old key revoked and a new one live — that claim is documented in a text file by a prior session, not something a later session can verify without dashboard access. **Stays open until Jeff confirms the rotation directly in the OpenAI dashboard**; if he already has, this row should move to RESOLVED with that confirmation noted. See `SECURITY-AUDIT.md` §3 Finding 3 and `reports/AUDIT-2026-07-30-pillar-01-security.md`. |
| 18 | NOT REPRODUCIBLE | 1 — Security (Revenue) | **Re-checked 2026-07-15 (verification pass): `/what-to-buy/` now serves `Astro v5.18.2`, the same generator as the homepage, and its nav carries Rugby, Pickleball, Wrestling, and the "For coaches" section — every marker the Security lane named as missing.** `cf-cache-status: DYNAMIC`, so this is the origin deployment, not an edge artifact. The stale build is gone and no `vite.cacheDir` change was ever made. Most likely explanation: `parent-coach-playbook-cron` fires the Pages deploy hook daily at 13:00 UTC, so the site rebuilt from `main` between the lane's check and this one. Do not spend a `main`-branch session on the `cacheDir` lead until the symptom is reproduced. **Watch, do not close blind:** if a stale generator tag reappears on any route, the `cacheDir` lead below is still the best hypothesis and this item reopens. Original finding, kept for the record: `/what-to-buy/` (the Gear Files, the money section) was served from a genuinely older build (`Astro v4.16.19` generator tag, shorter guide list even in the shared NavBar) than the rest of the live site (`v5.18.2`). Confirmed not a Cloudflare edge-cache issue (`cf-cache-status: DYNAMIC` on both, live fetch bypassing browser cache) — the origin deployment itself is serving stale output for this one path. No code-level route exclusion found. Lead worth trying: `astro.config.mjs`'s `vite.cacheDir` is pinned to a fixed `/tmp/pcd-vite-verify-cache` path (added 2026-07-10) instead of Vite's default — if the Pages build environment persists `/tmp` across builds, this is a plausible mechanism for a stale partial rebuild. Needs someone with `main`-branch access, a real `npm run build`, and deploy access to try the fix and reverify; none of those were available in this session (checked out on `migration/pages-to-workers-staging`, git/build/deploy all off-limits by this session's rules). |

| 19 | HIGH | 8 — Ops / States | **2026-07-15. A live HTTP 500 on 296 public camp pages. Fixed in source this pass, not yet deployed.** Root cause: `src/pages/camps/[slug].astro`'s `fmtDate` did `s.split('-')` on a date that can be null. 296 rows in `activity-radar` are `pcd_status='approved'` with a null `session_start_date` or `session_end_date` (D1-verified, 2026-07-15). The expired-camp redirect one branch up only fires on a **non-null** `end_date`, so every null-dated row falls straight through to the render and throws a `TypeError` in SSR frontmatter. Confirmed live 4-for-4 on a random sample (`pro-football-camp-colorado-spgs-co-5884-profootballcamp`, `vancouver-lake-crew-...`, `camp-ten-trees-...`, `olympia-youth-soccer-club-...` all returned 500; the dated sibling row returned 200). **Why nothing caught it:** these rows are absent from both sitemaps (the sitemap scopes to `session_end_date >= today`, and `NULL >= today` is NULL), so the SEO lane's full 2,629-URL crawl reporting "zero dead URLs" was blind to them by construction — and it checked for 404s, not 500s. Fix: `fmtDate`/`fmtRange` are now null-safe and degrade to "Dates to be announced"; the Event JSON-LD omits null `startDate`/`endDate` rather than emitting `null` (invalid markup). **Latent, not fixed, not live:** `camps/index.astro:50`, `camps/[state]/[city]/index.astro:50`, and `camps/[state]/[city]/[sport]/index.astro:48` carry the same unguarded `split('-')`; they are safe today only because their queries filter to future dates, which excludes nulls. Worth hardening the next time that directory is open. Reverify after deploy: the four slugs above should return 200. |
| 20 | RESOLVED | 7 — Stack | ~~Vera vs CANARY: applyCanary would pause pcd-deletion-monitor, silencing the legal-SLA watch.~~ **Resolved in source (live after the 2026-07-18 deploy).** `src/lib/agent-runs.ts` now carries `CANARY_EXEMPT_AGENTS = {pcd-deletion-monitor, vera}`: exempt agents trip CANARY but are never paused (`applyCanary` short-circuits before `pauseAgent`), a failed exempt run forces `needs_you=true` in `finishRun` so it cannot go silent, and the Slack alert escalates louder instead of pausing. Covered by `tests/api/agent-runs.test.ts`. Safe to wire Vera to `POST /api/agent-runs` once `AGENT_RUNS_TOKEN` is set. |
| 21 | MEDIUM | 9 — Testing | **2026-07-15.** `wrangler.jsonc` is a **staging-only** config (`"name": "parent-coach-desk-staging"`, its own header says throwaway, do not attach a domain). It does not govern the live Pages project `parent-coach-playbook`, whose D1 binding lives in the Cloudflare dashboard. The Infra lane resolved Open Item 6 by calling it a database-name-versus-project-name category confusion, which is correct as far as it goes, and proved by live evidence that production reads `activity-radar` (a post-freeze row renders 200) — that conclusion holds and the audit's "production binds the legacy DB" claim is genuinely wrong. But two handoffs (`FORGE_DB`, `ACCESS_TEAM_DOMAIN`/`ACCESS_AUD`) tell Jeff to add bindings and vars to `wrangler.jsonc`. Adding them there configures **staging**, not production, and the routes would still 500/run LEGACY in prod. Whoever executes those handoffs needs to know which config actually ships. |
| 22 | RESOLVED | 1 — Security | Exposed Resend key rotated 2026-07-18: the leaked "PCD" key was deleted in Resend, a new key "pcd-production" created, and the Cloudflare `RESEND_API_KEY` Pages secret re-set to the new value (confirmed by Jeff). Binds on the next deploy. |
| 23 | LOW | 8 — Ops | **2026-07-20.** Checked against the standard's Pillar 8 caching guidance while running intake on a DevOps-stack note: grepped `src/` for `caches.default`, `CacheStorage`, `KVNamespace` — no matches. Camp-listing pages read D1 on every request with no Cache API or KV layer in front of them. Not urgent at current (thin) traffic per the Pillar 10 SEO notes, but worth a look before the site scales or before ActivityRadar-derived traffic grows. Fix is the Cache API or KV with a TTL already named in Pillar 8, not a new dependency. |
| 24 | ROADMAP | New standard | **2026-07-21.** PCD is one of four products in scope for the new `Outputs/_system/Build Quality Prompts/SOC2-CONTINUOUS-COMPLIANCE-STANDARD.md` (with GavelScope, SightSmash, MedConfRadar/Directory), since camps and eventual enterprise partners are the buyers who'd ask for a SOC 2 report. Vendor pick: Comp AI (self-host free or $199/mo Starter), not Vanta/Drata/Secureframe, to keep the pre-revenue cost near zero across all four products. No action needed on PCD specifically until it has an enterprise-facing deal in motion. |

| 25 | OPEN | 13 — Protect-the-App | **2026-07-28. Inline editor shipped (`docs/INLINE-EDITOR.md`).** Site chrome and homepage marketing copy are now editable inline by a Cloudflare-Access-verified admin, stored in a `CONTENT_OVERLAY` KV namespace, substituted into the prerendered homepage by HTMLRewriter in the Worker. Pillar 13 evidence, three rows: **(a) Unhappy paths** — the save client covers idle/editing/saving/saved/conflict/validation-rejected/auth-expired/receipt-failed/write-failed/offline/connection-lost, and never renders "Saved" unless the server returned `ok:true`; a dropped connection says "may not have saved", not either certainty. Automated fault injection against a live preview is still owed. **(b) Environment isolation** — staging KV `8a6875ed…` and production KV `50d9a323…` are distinct namespaces, asserted by `tests/overlay-environment-isolation.test.ts`, which fails closed if the two ids ever match; `ENVIRONMENT` var named per ring and recorded on every receipt. **(c) Sensitive-action receipts** — `content_overlay_receipts` (migration `0027`) is append-only by trigger, hash-chained, email stored as SHA-256 digest with domain kept for triage, before/after bounded at 200 chars, and rejected/conflicted/failed attempts recorded alongside successes. A receipt write failure rolls the edit back and reports failure, so a protected mutation never stands unrecorded. Tamper detection for modified/deleted/reordered/re-hashed rows verified (14/14 checks). Sanitizer verified against 18 adversarial payloads (32/32 checks) including script tags, event handlers, `javascript:`/`data:`/`vbscript:` URLs, protocol-relative links, and an error-message canary. **Remaining before this row closes:** run the full `npm run test:unit` and `npm run build` in a real node_modules environment (this session had none), automated fault injection against a deployed preview, and a Lighthouse mobile re-measure on the homepage (ties to open item #9). |
| 26 | OPEN | 4 — UI / 10 — SEO | **2026-07-28. Inline-editor scope boundaries, recorded so they are not relitigated by accident.** Meta titles and descriptions are deliberately NOT inline-editable (Jeff's call: an unreviewed meta tag carries more SEO blast radius than the convenience is worth) and article/guide/pillar/news bodies stay markdown-in-git behind Penny's review gate and their `factCheckGoodThrough` stamps. Both boundaries are enforced by assertions in `tests/overlay-route-coverage.test.ts`, not by comment. Also deliberate: no `'*'` global regions, because only `/` is in `run_worker_first` — a nav or footer region would look editable and silently do nothing on the other 1,851 static pages. Adding a page requires all three of OVERLAY_ROUTES, both wrangler `run_worker_first` lists, and the manifest; the route-coverage test fails the build on drift. Overlay-vs-git drift is the known long-term risk, reported by `node scripts/overlay-snapshot.mjs --drift-only`; scheduling that alongside the existing PCD backup job is still open. |

| 27 | RESOLVED (caught pre-merge) | 8 — Ops | **2026-07-28. The hero-image commit silently deleted the entire backup worker.** Commit `1458e2fe` ("Add hero images across 847 articles and guides…") deleted `worker-backup/` — 9 files, 3,483 lines, including `src/index.ts` (the Cloudflare Cron + R2 export that replaced the broken git-based `pcd-backup` in `1f478103`), its test suite, its `wrangler.toml`, and the production-guarded `restore.mjs` D1 restore tool. The commit message says nothing about a deletion. The directory still exists on `origin/main`, so this had not reached production, but the branch it sits on was queued for a PR to `main`; merging it as written would have removed PCD's backup and restore source from the repo. Found because `check:root-organization` reported "Directory registry entry has no tracked files: worker-backup" — the registry caught what the commit message hid. Restored from `origin/main` on `feat/pcd-inline-editor`. **Two follow-ups worth Jeff's time:** (a) confirm the deployed `pcd-backup` Worker is still running and still writing to R2, since the source going missing locally says nothing about the deployed state; (b) this pairs with open item #7 (no restore has ever been run and logged) — the restore tool was three days from being deleted without ever having been exercised. |
| 28 | FIXED (packet re-cut required) | 9 — Testing | **2026-07-28. rc01 expired 2026-07-25, blocking every production deploy for three days.** `check:release-evidence` sits inside `ci:release`, and the entire expiry rule is two lines in `scripts/release-evidence.mjs` — so all three unit failures and the CI block clear the instant `expires_at` moves. That is precisely why it was not touched: eleven gates were marked `pass` and pinned to commits `0ccd55f` / `bf4c424` from a 2026-07-18 candidate, and the branch has since gained ~2,500 lines including a new admin write route, a KV binding and a D1 migration. Renewing the date would silently re-certify claims that are no longer true. **Two separate defects were tangled here and both are now addressed.** (a) **Test design.** Three tests in `tests/release-evidence.test.ts` `structuredClone`d the live packet to exercise *gate logic*, but inherited `expires_at` — so they failed on a calendar date rather than a logic regression. They now pin a future expiry inside the clone via `cloneWithFutureExpiry()`. The first test still reads the live packet untouched as the staleness canary, and a new fifth test proves the clock check still bites, so the helper cannot quietly disable expiry for the suite. (b) **The packet.** `scripts/cut-release-evidence.mjs` (`npm run cut:release-evidence`) cuts rc02 against the current commit: it re-runs the commit-sensitive gates (`source_commit`, `secret_scan`, `production_manifest`, `tests_and_build`) and records real exit codes and output digests — it cannot mark a gate pass on faith; carries environment gates with their original establishment dates visible; and **RESETS `deploy_approval` and `post_deploy_observation` to pending**, because those described a superseded candidate. Simulated cut yields 9 pass / 9 pending / 1 not_applicable versus rc01's 11/7/1 — deliberately *more* open gates, which is the honest picture. Packet stays `ready: false`; `--structure-only` only needs it in-contract and unexpired. Verified across 15 assertions including dirty-tree rejection, failed-command recording, and `--skip-slow` never producing a pass. **Ordering note:** the test imports rc02 and the cut script runs the suite, so the script writes a provisional packet first, then rewrites it with real test/build results. **Still open:** the seven gates never closed on rc01 (authenticated_access_probes, database_backup, customer_journeys, notification_receipt, failure_isolation, open_risk_decision, migration_approval) plus the two reset ones. Those are the actual work this packet was always pointing at. |
| 29 | FIXED (guard added) | 9 — Testing | **2026-07-28. Agent-written frontmatter keeps breaking the build by writing against schema ceilings.** Two separate hard failures the same day: `news/ohio-sam-act-athlete-mobility-2026.md` `summary` at 432/400, and `articles/laundry-stripping-sports-gear.md` `seoDescription` at 190/180. Each one hard-fails `astro build` with `InvalidContentEntryDataError` and takes the entire site build down. Third occurrence overall (commit `0f661d58`, 2026-07-22, was "trim oversized news summary field"). Both trimmed. **Guard added:** `scripts/check-content-field-lengths.mjs` parses the min/max string constraints out of `src/content.config.ts` and audits all 1,463 entries, naming file and field. Wired into `npm run build` ahead of `astro build`, so the fast readable failure happens before the manifest and 810 OG images are generated. Covered by `tests/content-field-lengths.test.ts`. Two follow-up fixes after the first run: it read every file in full (~5s, which blew vitest's 5s default timeout), now probes the first 8KB with a full-read fallback; and it printed all 89 near-cap entries on every build, now a one-line count with `--near` to opt in. **Distribution is the real story:** 89 entries sit within 5% of their cap — `seoTitle` at 70/70, `seoDescription` at 179/180, `bluf` at 497/500. The agents were targeting the limit rather than treating it as a ceiling, so the next overflow was one edit away. **Fixed at source 2026-07-28:** the `pcd-editorial-writer` (Ed) prompt now carries every field limit explicitly, instructs targeting ~85% of each cap, and requires running `check-content-field-lengths.mjs` before commit. `pcd-review-publish` (Penny) runs the same check at the top of her review. `pcd-rules-watcher` has NOT been updated and still writes news `summary` against the 400 cap; that one is still open. **Process note worth keeping:** the first version of the checker mis-parsed plain multi-line YAML scalars and reported 14 phantom `bluf` violations. Verified against the source before editing anything, so no files were changed on bad information. A checker that truncates multi-line values is worse than no checker. |

| 30 | FIXED | 8 — Ops | **2026-07-28. The rc02 release-evidence cut caught a real defect in the inline-editor work: I changed production bindings and routes without updating the deployment-manifest contract that pins them.** `scripts/check-deployment-manifest.mjs` asserts the EXACT production `assets.run_worker_first` list and the EXACT KV binding set (`expectBindings` is an equality check, not a superset check). The inline editor added `/` to `run_worker_first` (so the prerendered homepage invokes the Worker and HTMLRewriter can apply overlay values) and added the `CONTENT_OVERLAY` KV namespace. Both were missing from the contract, so `npm run check:production-manifest` exited 1 and `tests_and_build`'s sibling gate `production_manifest` was recorded as **fail** in rc02 rather than silently passing. Contract and its test fixture (`tests/api/deployment-manifest.test.ts`) both updated with comments explaining why each entry exists. Verified across 4 assertions: the updated fixture passes clean, and a missing `/`, a missing `CONTENT_OVERLAY`, and a rogue extra KV binding each still fail. Production `wrangler.production.jsonc` confirmed to match the contract exactly. **Worth noting what worked here:** the manifest contract is precisely the guard that catches "someone changed production wiring and did not tell the release gate", and the honest-cut script surfaced it as a recorded `fail` instead of a pass. Neither the build nor the unit suite would have caught it. |
| 31 | OPEN | 9 — Testing | **2026-07-28. rc02 carries two `fail` gates and must be re-cut from a clean tree.** `source_commit` failed because the working tree was dirty at cut time, which is the script refusing to certify a non-reproducible artifact rather than a defect. `production_manifest` failed for the real reason in item 30, now fixed. rc02 is also pinned to `c55798b2`, and the branch has moved since (essays published, manifest contract fixed), so the packet is already describing a superseded candidate. **Re-cut from a clean tree at current HEAD before opening the PR.** Note that `--structure-only` passes today regardless (a `fail` state is in-contract, and CI only exits 1 on contract errors), so `ci:release` will not block on this — which means a stale packet with failed gates could ship unnoticed. Reviewing the gate states, not just the CI exit code, is the human step. |

| 32 | FIXED | 10 — SEO | **2026-07-28. All 17 Ultimate Parent Guides were live and invisible to Google.** `src/pages/sitemap-content.xml.ts` enumerates 13 collections by hand and `pillar` was simply never added, so every `/pillar/<slug>/` page returned 200 while being absent from the sitemap. Those pages declare themselves canonical (`src/pages/pillar/[slug].astro` line 33), so the canonical URL of 17 cornerstone guides was unreachable to crawlers. **Compounding bug found alongside it:** `/guides/<slug>.astro` renders the *same* pillar entries and shipped with no `canonical` prop at all, so Google saw two identical pages and picked its own winner. That is the same "Duplicate, Google chose different canonical" failure class the www-to-bare-domain redirect fixed in the 2026-07 SEO audit. Both fixed: `pillar` added to the sitemap at its canonical `/pillar/` route only, and `/guides/` now emits `canonical` pointing at `/pillar/`. **Plausibly a contributor to Nora's 2026-07-28 GSC regression** (indexed pages 228 → 90, 1,208 stuck in "Discovered - currently not indexed"), though not proven as the sole cause; recheck GSC after this deploys. Found by `scripts/check-publish-queue-drift.mjs`, not by any existing check. |
| 33 | FIXED | 8 — Ops | **2026-07-28. The site had no scheduled rebuild and no monitor saying so.** Under Pages, a daily cron fired a deploy hook and future-dated posts went live on their date unattended. The Pages-to-Workers cutover removed that hook and nothing replaced it; `worker-cron` now only fires the camps sweep. `QUEUE.md` continued to open with "The site publishes itself... No button to push" and spent 90 lines documenting the retired hook, and `publishFilter.ts` carried the same false claim in a comment. Net effect: future-dating silently became a filter ("not before this date") rather than a scheduler ("on this date"), and a week of finished content sat unpublished with a green build and passing tests. **Three fixes:** (a) `QUEUE.md` rewritten to describe the real Worker + PR + protected-approval path, opening with an explicit correction; (b) the `publishFilter.ts` comment corrected in place; (c) `scripts/check-publish-queue-drift.mjs` (`npm run check:publish-drift`) added as the missing monitor, comparing locally date-eligible content against the live site. **Precision note worth keeping:** the checker's first version reported 24 misses of which 17 were false positives (live pages merely absent from the sitemap). It now verifies each candidate against its real URL before calling it drift, and reports sitemap-only gaps as a separate non-failing category. A guard that is 71% wrong gets ignored. **Automation:** `.github/workflows/publish-queue-watch.yml` runs the drift check daily against `main` and dispatches `deploy-workers.yml` only when the live site is genuinely behind. It does NOT bypass the protected `production` gate; a human still approves. Option B (unconditional daily deploy) was rejected because a daily approval prompt on days with nothing to ship trains rubber-stamping. |

| 34 | RESOLVED | 4 — UI | **Closed 2026-07-29 (same day).** (a) The `newsletter.astro` gradient is gone: the hero is now `bg-surface` with the image at full opacity behind a flat `bg-surface/90` scrim, the same pattern `index.astro:112` already ships. A flat wash also holds contrast constant across the band instead of letting the headline get less legible toward the right edge the way the old left-to-right gradient did. Worst case (a pure-black photo pixel under the 90% white wash gives an effective `#E6E6E6`): h1 13.25:1, dek 9.31:1, eyebrow 8.69:1. (b) `camps/[slug].astro:832` now uses `rgba(4,30,66,0.45)` in its SVG pin drop-shadow, matching `camps/index.astro:500`. See also item #39. Original entry: **2026-07-29.** Two "do not" violations found sitewide but left as follow-up since neither file was otherwise touched this session: (a) `newsletter.astro:32` has a `linear-gradient` using `rgba(26,22,19,...)` — the old dark-ink-brown color, in rgba form (evaded the hex sweep) — as a dark hero photo wash; needs the same treatment as the `cost-calculator.astro` fix in this session (flatten to a solid rgba wash, update the color). (b) The `bg-ink text-paper` "closing statement" full-width band pattern (dark navy used as a large section background, not just for type/structure) appears in 17 files: `admin/link-health/index.astro`, `admin/editorial/index.astro`, `pre-game-routine.astro`, `newsletter.astro`, `youth-sports-pendulum.astro`, `scripts/[slug].astro`, `scripts/index.astro`, `decisions/[slug].astro`, `cost-calculator.astro`, `team-parent/[topic].astro`, `reads/[topic].astro`, `pathways/[sport].astro`, `mental-skills.astro`, `coaching-tips/index.astro`, `body/index.astro`, `resources/what-to-say-when.astro`, `resources/practice-plan-template.astro`. This is a pre-existing site-wide template convention, not something the redesign introduced, and restyling it is a structural change (not a color swap) — flagged for Jeff's call rather than silently restructured. |
| 35 | MEDIUM | 6 — Accessibility | **2026-07-29.** Two contrast/target-size gaps found via the palette re-verification, fixed only in this session's touched files, left as follow-up everywhere else. (a) `#C9D2DA` (`--bone`/`--line`) is the sole visible border on real form inputs and fails the 3:1 non-text-UI floor — still needs the `border-bone` → `border-muted` swap in `components/DataQualityRow.astro`, `admin/camps/index.astro`, `admin/camps/queue.astro`, `admin/camps/spot-check.astro`, `admin/claims/index.astro`, `admin/trust/deliveries.astro`, `admin/trust/drafts.astro`, `admin/trust/index.astro`, `camps/submit.astro`, `camps/suggest.astro`, `camps/[slug].astro`, `trust.astro`. (b) `.tag-pill` filter chips are 32px tall (below this project's 44px tap-target convention) on `camps/[state]/[city]/[sport]/index.astro`, and several admin filter-bar buttons (`admin/editorial/index.astro`, `admin/link-health/index.astro`) have no explicit min-height at all. Neither is a regression from the redesign — both predate it — but both are real, and both are cheap, mechanical fixes (swap a class, add `min-h-11`) the next session in these files should pick up. Separately noted, not fixed: `admin/operations.astro`, `admin/relationships.astro`, `admin/trust/deliveries.astro`, `admin/trust/drafts.astro`, `admin/trust/index.astro` use Tailwind's default `red-700`/`amber-700`/`green-700` for health/error status text — outside the navy/royal-blue/silver-green palette, but a defensible operational traffic-light convention on internal ops dashboards, not a leftover of the old warm editorial palette; flagged for Jeff's call, not changed. |

| 36 | MEDIUM | 4 — UI / 10 — SEO | **2026-07-29. All 810 OG cards were generated against the retired warm-editorial palette and now clash with the navy/silver-green site.** `scripts/build-og-images.py` writes slug-keyed cards to `public/og/` and only rebuilds on content change, so the palette remap did not touch them — the 2026-07-29 build logged `OG images: 0 written, 810 unchanged`. Net effect: every social share, Slack unfurl, and iMessage preview shows a cream-and-terracotta card next to a navy site. Not a launch blocker and deliberately deferred by Jeff until the palette settled (it now has: the typography chrome pass was the last palette-affecting change). Fix is a forced regen of all 810 against the new tokens, then eyeball a sample before committing. Note the sandbox this was audited from could not run the script (`ImportError: No module named brotli` when fontTools tries to decode the Fraunces WOFF2), so the regen has to happen on a machine with the Brotli extension present — it succeeded on Jeff's machine the same day, so this is a local-run task, not a code fix. |
| 37 | INFO (decision record) | 4 — UI | **2026-07-29. Per-pillar color identity was deliberately collapsed to navy; recorded so it is not reintroduced by accident.** `src/data/site.ts` carried 40+ distinct `accent`/`accentBg` pairs across `EDITORIAL`, `PILLARS`, `TEAM_PARENT_CATEGORIES`, `SECTION_THEMES`, `TOPIC_THEMES`, and `DEFAULT_THEME`, all still in the old warm hues, and `accentBg` renders as a large page-section background in `PillarLayout.astro`. The navy/silver brief permits exactly three colors and explicitly forbids adding a fourth, so three hues could not carry 40 identities without inventing new ones, and differently-tinted section backgrounds would have broken the "predominantly white and light grey-blue" constraint. Every `accent` is now `#041E42` and every `accentBg` is `#F4F7FA`, documented in a comment in `site.ts`. **Consequence to watch:** `/pillar/*`, the homepage Three Drives block, and the team-parent topic pages no longer differentiate by hue and may read samey. If they do, the fix is differentiating by rule weight, eyebrow treatment, or card top-border thickness — not by reintroducing a fourth accent color. |
| 38 | RESOLVED (pending Jeff's build verification) | 1 — Security / 9 — Testing | **2026-07-29. Two moderate npm advisories were live and not blocked by the gate:** XML injection via unescaped RSS feed fields in `@astrojs/rss` (GHSA-8j5q-mfj2-5q9q) and reflected XSS via unescaped View Transition animation properties in `astro` (GHSA-4g3v-8h47-v7g6). **2026-07-31: fixed.** `npm audit fix` bumped `astro` `^7.0.9`→`^7.1.6` and `@astrojs/rss` `^4.0.18`→`^4.0.19`, both within their already-declared semver ranges. `npm audit` now reports 0 vulnerabilities. **Not build-verified this session** — `astro build` and `vitest` both bus-error in this sandbox (pre-existing limitation, unrelated to this change). Jeff should run `npm install && npm run build && npm test` before merging, then spot-check the three feeds (`rss.xml`, `rss-guides.xml`, `rss-tips.xml`) render clean, per this item's original concern about agent-written frontmatter reaching unescaped fields. |
| 39 | FIXED | 4 - UI / 6 - Accessibility | **2026-07-29. All 9 full-width dark bands removed. This closed a live contrast failure, not just a brief violation.** The 9 `<section ... bg-ink text-paper>` bands split into two patterns. Six were the identical closing-statement band (`scripts/index.astro:86`, `mental-skills.astro:103`, `decisions/[slug].astro:161`, `scripts/[slug].astro:155`, `pathways/[sport].astro:166`, `youth-sports-pendulum.astro:127`); all six now read `border-t-4 border-t-accent border-b border-b-bone bg-stripe`, with the eyebrow on `text-silver-deep`, the statement on `text-ink`, and supporting copy on `text-ink-soft`. The 4px accent top rule carries the "this is the moment" emphasis the dark fill was providing, and it matches `.t-quote`'s existing stripe-fill-plus-navy-rule vocabulary rather than inventing a new one. Side-specific border color utilities (`border-t-accent`, `border-b-bone`) were used deliberately: `border-bone` and `border-accent` both emit `border-color`, so combining them would have been resolved by Tailwind's emission order, not by class order. Two were dark lead-magnet heroes (`resources/what-to-say-when.astro:9`, `resources/practice-plan-template.astro:9`), now `border-b border-bone bg-paper` so the embedded `<NewsletterSignup variant="card">` (a white `.card`) is the brightest surface in the hero. **The real find: on all three dark heroes the eyebrow (`text-accent`) and the h1's `<em class="text-rust">` were `#003594` on `#041E42`, which is 1.53:1. They were effectively invisible in production.** New ratios: statement `#041E42` on `#F4F7FA` 15.38:1; supporting copy `#2E3A45` on `#F4F7FA` 10.81:1; eyebrow `#5F7075` on `#F4F7FA` 4.81:1; accent rule 10.08:1 (non-text, needs 3:1); hero h1 `#041E42` on `#EDF1F5` 14.57:1; hero eyebrow 9.55:1. Closes the dark-band half of item #34. |
| 40 | SUPERSEDED by #47 | 4 - UI / 7 - Stack | **2026-07-29. The 8px spacing scale was dead code claiming to be a system. The Tailwind half is deleted; the ramp is documented for whoever adopts it.** A sweep of all 266 source files in `src/` excluding `src/content/` found ZERO consumers of `spacing.s1..s10`: not one `p-s*`, `m-s*`, `py-s*`, or `gap-s*` anywhere in markup. The `theme.extend.spacing` block is removed from `tailwind.config.mjs` with an inline note explaining why and pointing here. The CSS custom properties stay in `global.css` because three rules in that file do consume them (`.band` uses `--s8`, `.container-px` uses `--s3`/`--s2`, `.gear-pick` uses `--s3`); `--s1`, `--s4`, `--s5`, `--s6`, `--s7`, `--s9`, `--s10` have no consumer at all today and are now labelled as such in the file. **Still open:** page templates use literal Tailwind spacing (`py-12`, `py-14`, `py-20`, `mt-8`, ...) with no shared rhythm. Converting roughly 100 templates onto the ramp is a real mechanical pass and was judged out of scope for a styling session. Do not re-add the Tailwind `spacing` key without doing that conversion in the same change, or it goes straight back to being dead config. |
| 41 | RESOLVED | 4 - UI | **2026-07-29. The type-scale primitives in `global.css` are almost entirely unadopted, and h2 and h3 collide at the same size in dozens of places.** Adoption counts across `src/` excluding `src/content/`: `t-h1` 6 occurrences against roughly 185 `<h1>`s, `t-h2` 10 against roughly 134 `<h2>`s, `t-h3` **1** against roughly 99 `<h3>`s. Headings instead carry ad-hoc arbitrary sizes: h1 uses 14 distinct values (`text-[2.5rem]` x33, `text-[4rem]` x32, `text-4xl` x26, `text-[2.25rem]` x25, `text-[3.25rem]` x24, `text-3xl` x22, plus 8 more), h2 uses 8. **The hierarchy consequence: `text-xl` (1.25rem) is used on 31 h2s and 47 h3s, so on those pages h2 and h3 render identically and the third level of hierarchy does not exist.** This is the direct answer to "is the type scale holding in practice" and to "do h1/h2/h3 step distinctly": no. Fixed this session only where it was bounded and unambiguous: 8 `<h3 ... text-base>` (1rem, below the 18px body floor and below the 38 h3s already at `text-lg`) were bumped to `text-lg` in `admin/camps/[id].astro:332`, `ages/[group].astro:147`, `index.astro:186` and `:248`, `reads/index.astro:160`, `sports/[slug].astro:216`, `start-here.astro:107` and `:233`. Converting the remaining roughly 410 headings onto `t-h1`/`t-h2`/`t-h3` would change rendered sizes on nearly every page and cannot be verified without a build, so it is flagged for the owner as a deliberate typography pass, not slipped in. |
| 42 | FIXED (partially) | 4 - UI | **2026-07-29. Silver green was close to functionally dead, which is the exact failure the brief named ("the tone that keeps this from being generic corporate navy"). It now has one use at real scale.** Census of true `#8A9BA0` before this session: 11 live decorative sites plus 7 inline `style="color: #8A9BA0"` eyebrows that only existed inside the dark bands. Removing those bands (item #39) would have dropped the count to 11. Compounding it: `src/data/site.ts` has 20-plus topic themes and **every single one** is `accent: '#041E42', accentBg: '#F4F7FA'`, so the whole per-topic tint layer renders as one navy value (see the decision record in item #37). Fix: `.tag-pill`'s base border went from `1px solid transparent` to `1px solid var(--silver)`. That is 38 call sites across 9 files, it is precisely the use the brief names ("tags, eyebrows, quiet fills, small doses"), and it gives every chip on the site an edge instead of leaving it an edgeless smudge on `--stripe`. Decorative only: `#8A9BA0` on `#F4F7FA` is 2.68:1, and since the `--pill-bg` fill plus the navy label identify the component, WCAG 1.4.11's 3:1 does not bind on the border. Remaining true-silver sites after this pass: `Footer.astro:53` (1px rule), `Logo.astro:29` (light variant), `TopicIcon.astro:22-24`, `SeasonCalendarChart.astro:28` (bar fill), `cost-calculator.astro:144` (label inside the allowed dark sidebar card, 5.73:1 on navy), and four `border-l-silver` "What this is" asides. **Still open:** silver green is still carrying almost no weight above the chip layer. If it is meant to differentiate, it needs a second structural home. |
| 43 | RESOLVED | 6 - Accessibility | **2026-07-29. 56 form controls keep their focus ring only because of an accident of the CSS cascade.** `focus:outline-none` appears 56 times across 14 files (`admin/camps/[id].astro` x22, `pre-game-routine.astro` x13, `trust.astro` x6, `proof.astro` x5, `cost-calculator.astro` x4, and 9 more), always paired with `focus:border-rust`. The global `*:focus-visible { outline: 3px solid var(--accent) }` in `global.css` sits OUTSIDE any `@layer`, while `focus:outline-none` compiles into Tailwind's `utilities` layer, and unlayered CSS beats layered CSS regardless of specificity. That is the only reason those 56 controls are still keyboard-navigable. A comment to that effect is now in `global.css` above the focus rule. **Anyone who wraps that rule in `@layer base` or `@layer utilities` silently kills the focus ring on 56 controls.** Not fixed by editing the 56 call sites: each is paired with an intentional `focus:border-rust` treatment, so unpicking them is a form-styling pass, not a token fix. Could not be confirmed against compiled CSS this session, `dist/` holds no built stylesheet and the sandbox cannot run `astro build`. |
| 44 | RESOLVED | 6 - Accessibility | **2026-07-29. Filter control rows were roughly 22px tall, half the 44px minimum.** 22 `<button data-age>` / `<button data-sport>` filter chips across `reads/[topic].astro`, `team-parent/[topic].astro`, `body/index.astro`, and `coaching-tips/index.astro` had `py-0.5` or `py-2` and no min-height. All 22 now carry `inline-flex items-center min-h-11`, consistent with the 26 existing `min-h-11` uses in `NavBar.astro`. In the same pass the active chip moved from `border-ink bg-ink text-paper` to `border-accent bg-accent text-paper`, which aligns hand-rolled chips with `.tag-pill-active` and with the brief's "primary accent = links, buttons, active" (`#EDF1F5` on `#003594` is 9.55:1). **Still open:** roughly 56 further `<button>`/`<a>` sites across 25 files carry `py-0.5`/`py-1` with no min-height, concentrated in `/admin` (`camps/queue.astro`, `editorial/index.astro`, `link-health/index.astro`, `source-quality.astro`, and others) plus breadcrumb and tag links in flowing prose. Left alone deliberately: raising admin table controls to 44px reshapes those table layouts, and WCAG 2.5.8 exempts inline links, so a blanket sweep would be wrong. Needs a per-surface judgement call with a rendered page. Partially supersedes the target-size half of item #35. |
| 45 | FIXED | 4 - UI | **2026-07-29 (second pass). The type scale is now real: 330 headings on 6 tokens, down from 17 on 3 and roughly 410 on ad-hoc sizes.** Jeff chose the low-churn mapping, so the tiers were derived from the clusters the site already had rather than imposed. h1: `t-h1-hero` clamp(2.5rem,6vw,4rem) 40 uses, `t-h1` clamp(2.125rem,4.5vw,3.25rem) 37, `t-h1-compact` clamp(1.875rem,3.5vw,2.25rem) 27 -- 14 distinct ad-hoc h1 sizes collapsed onto 3 with near-zero rendered change. h2: `t-h2` 117 uses, which **fixes the collision recorded in #41** by promoting the 28 h2s that were stuck at `text-xl` (20px, identical to 43 h3s) up to 26-36px. h3 split on role: `t-h3` (41, prose subsections) and a new `t-card-title` (68, linked card and grid titles, 18-20px) -- separated because a card title's hierarchy context is the card, so forcing 68 of them up to t-h3's 20-24px would have blown out tight card grids for no hierarchy gain. Card/prose split was detected on `group-hover:`, which reliably marks a linked tile. 299 automated edits across 95 files, plus 2 hand-cases: `ArticleCard.astro` had a `titleSize` prop switching between 4 ad-hoc sizes (now `t-h3` for essays, `t-card-title` otherwise, preserving the essay/standard distinction), and `team-parent/[slug].astro` **carried two `class` attributes on the same `<h1>`** (malformed; the second was dead) -- merged. Left alone on purpose: 14 sites already on `t-display` + a tier, 13 bare `<h2>` inside `.article-body` (correctly styled by the `.article-body h2/h3` rules), and `owner/index.astro` / `owner-proof/dashboard.astro`, which are noindex internal proof pages that do not import global.css at all. Residual ad-hoc heading sizes sitewide: **0**. |
| 46 | FIXED | 4 - UI | **2026-07-29 (second pass). One eyebrow vocabulary, 262 call sites. The serif italic won on the evidence, not on taste.** The site ran two: 230 hand-rolled `font-display italic text-rust font-medium` labels across 62 files, against 17 uses of the uppercase sans `.t-eyebrow` (14 of them on `index.astro` alone). `.t-eyebrow` was redefined in place as Fraunces italic 500, 1rem, `var(--accent)`, sentence case -- which converted those 17 for free and gave the 230 a token to collapse onto. 224 converted by pattern plus 17 that carried an inline `style="font-size: 1.05rem"` or `0.95rem` (those inline overrides are gone). Deliberately NOT converted: 92 `font-display italic` elements in `text-ink-soft` (deks and card meta, a different role) and 23 at `text-xs` inside cards (card metadata, correctly a smaller size). `.t-label` was split out of the shared rule and kept as uppercase 12px micro-metadata for its 12 real uses (gear-card labels, pull-quote attribution, TopRule, 404); `Footer.astro`'s 2 column headers moved from `.t-eyebrow` to `.t-label`, since a column header is not a section eyebrow. Side effect worth noting: this moves 230 elements OUT of all-caps, so the brief's "no all-caps except small eyebrow labels" rule now has a much smaller live surface. Contrast: `#003594` at 16px is 10.84:1 on white, 9.55:1 on `#EDF1F5`, 10.08:1 on `#F4F7FA` (the old `.t-eyebrow` was `#5F7075` at 12px, 5.17:1). 67 eyebrows grew from 14px to 16px. |
| 47 | RESOLVED | 4 - UI / 7 - Stack | **2026-07-29 (second pass). The 8px ramp is deleted, not adopted. It was a rename of a scale that already existed.** Item #40 left "wire the templates to it" open. On inspection that was the wrong instruction: `--s1..--s10` (8/16/24/32/40/48/64/80/96/128px) is an EXACT 1:1 rename of Tailwind's own default scale (`py-2/4/6/8/10/12/16/20/24/32`), and every section-level padding literal on the site was already on the 8px grid. Converting `py-12` to `py-s6` across ~100 templates would have bought zero rhythm and left two vocabularies for the same numbers. Jeff confirmed the delete. All 10 custom properties are gone; the four internal consumers in `global.css` (`.band`, `.container-px` x2, `.gear-pick`) now use literal px. `var(--sN)` references remaining: **0**. **The real gap this exposed, still open:** section vertical padding varies across `py-8` / `py-10` (123 uses) / `py-12` (46) / `py-14` (24) / `py-16` with no rule for which tier a given section type should use. That is a spacing-POLICY problem, not a missing-token problem, and it needs a decision about how many section-rhythm tiers the site should have before any code changes. |
| 48 | FIXED | 6 - Accessibility | **2026-07-29 (second pass). All 56 `focus:outline-none` deleted, and 173 controls raised to a 44px target.** (a) **Focus:** item #43 recorded that 56 controls across 11 files kept their focus ring only because `global.css` happens to be unlayered and Tailwind's utilities are layered. That accident is no longer load-bearing: every `focus:outline-none` is gone, and the paired `focus:border-rust` was kept (92 uses), so a focused control now gets BOTH the global 3px accent ring and an accent border. Residual `outline-none` / `outline: none` in `src/`: **0**. The focus rule stays unlayered anyway, with a comment saying why. (b) **Targets:** 173 controls that carried `py-0.5`/`py-1`/`py-1.5`/`py-2` with no min-height now carry `min-h-11` -- 77 buttons and 16 chip links (`min-h-11 inline-flex items-center justify-center`), 7 NavBar dropdown rows (`min-h-11 flex items-center`), 54 inputs and 19 selects (`min-h-11` only; inputs and selects must not become flex containers). Deliberately skipped: the `sr-only` skip link in `BaseLayout.astro`, which is a keyboard-only affordance and not a pointer target. **Expect visible layout change in `/admin`:** dense table controls in `camps/queue.astro`, `editorial/index.astro`, `link-health/index.astro` and `source-quality.astro` go from roughly 22px to 44px, which is correct per WCAG 2.5.5 but will loosen those tables noticeably. `editorial/index.astro:264` was a 10px micro-button and is now 44px. Seven `display` collisions introduced by the pass (a new `flex` alongside an existing `block` on NavBar rows) were caught and cleaned; `display`-class conflicts sitewide are now only 5 legitimate responsive variants (`md:flex`, `sm:block`). Supersedes the target-size half of #35 and closes #43 and #44. |
| 49 | RESOLVED | 14 - Mobile | ~~Desktop still shows the same three articles in both the hero rail and the Fresh at the Desk band.~~ **Fixed 2026-07-30 (second pass).** Forced by the hero-gap work: shrinking the rail from 5 items to 3 took the overlap from 3-of-5 to 3-of-3, an exact repeat, so this could not stay deferred. `pinnedFinal` now subtracts `latestRail`'s ids (and the featured id) instead of reading off the top of the same sorted array, so the rail carries the newest three and the band carries the three before them, on every breakpoint. The band stays `hidden md:block`, but that is now a scroll-length call (the phone page runs ~9,600px and the band adds ~950px of the same card pattern), not a duplication fix. `src/pages/index.astro`. |
| 50 | MEDIUM | 14 - Mobile / 6 - Accessibility | 27 interactive elements render under the 44px tap-target minimum at 390px width on the homepage. Counted programmatically this pass but not itemized by file and line, so no fix was attempted. Related to open item #35's `.tag-pill` 32px finding, which is one instance of the same problem. Run `/web:mobile` and itemize before fixing. |
| 51 | RESOLVED | 14 - Mobile / 4 - UI | **Hero dead space, fixed 2026-07-30.** The hero right rail set the section height at 744px against a 370px left column, leaving 374px of nothing below the CTAs on desktop and pushing the This Season band below the fold. Rail cut 5 items to 3: hero drops to ~470px, the columns come within ~100px, and This Season clears the fold with no markup moved. Same pass dropped the hero photo overlay from `bg-surface/90` to `/70` so the sideline image reads as an image instead of a texture. Contrast measured against the real file before the change, not estimated: navy `#041E42` on the composited background is 7.85:1 at its worst pixel under the text (AA needs 4.5:1, AAA 7:1). Re-run that math if the hero image is ever swapped; a darker photo would not survive 70%. Also fixed a copy defect in the same column: `Written by the {EDITORIAL.byline}` rendered "Written by the the Parent Coach Desk" because the byline value already carries the article. Footer.astro and ArticleLayout.astro were always correct; index.astro was the only bad call site. |
| 52 | RESOLVED | 4 - UI / 6 - Accessibility | **Section bands went flat sitewide in the navy/silver remap. Found 2026-07-30 from a reader report on `/sports/baseball/`, fixed same day.** Every page except the homepage separated sections with `<section class="border-b border-bone bg-paper-warm">`, alternating that against the page default. The remap set `--paper-warm: #FFFFFF` and `--surface: #FFFFFF`, so the two bands became **1.00:1** — no alternation at all — and the only remaining boundary was the `--bone` hairline at **1.53:1** on white, under the 3:1 WCAG 1.4.11 bar for a rule that is the sole carrier of a boundary. Sections ran together; worst on the sport hubs, where a gear file, a reads list and a drills list stack with nothing between them. Not fixable by changing `--paper-warm`: it has 264 card, hover, table-row and code-block usages that sit on the grey `--paper` page and need to stay white. Fix: new `--rule: #7C8B99` token (3.49:1 on white) for section dividers only, leaving `--bone` at #C9D2DA for card and input borders; new `.section-band` / `.section-band-sunken` pair in `global.css` that sets background and rule but never padding, so no page's vertical rhythm moved. Swept 211 section bands across 69 public files. Admin's 32 instances left on the old pattern deliberately: gated behind Cloudflare Access, dense data UI, no reader impact. Note the bands cannot alternate strictly — most sections are conditionally rendered, so which ones appear varies by sport. The visible rule is what guarantees separation; the alternation is a bonus that degrades gracefully. `src/styles/global.css`, `src/styles/pcd-tokens.mjs`. |
| 53 | RESOLVED (corrected 2026-07-31) | 6 - Accessibility | ~~`--bone` / `--line` (#C9D2DA, 1.53:1 on white) is still the border on form inputs.~~ **Corrected 2026-07-31 (Pillars 4/6/14 pass): this is already fixed in source and this row was stale.** `--input-border` (`#7C8B99`, 3.49:1, wired to Tailwind as `border-input`) exists in `global.css`/`pcd-tokens.mjs` with a comment citing this exact item number, and every real `<input>`/`<select>`/`<textarea>` across all 14 files item #35 named (`admin/camps/[id].astro`, `admin/camps/queue.astro`, `admin/camps/index.astro`, `admin/trust/index.astro`, `admin/trust/drafts.astro`, `admin/trust/deliveries.astro`, `admin/claims/index.astro`, `cost-calculator.astro`, `trust.astro`, `camps/[slug].astro`, `camps/submit.astro`, `camps/suggest.astro`, `camps/index.astro`, `proof.astro`) already carries `border-input`, verified by pattern-matching every form-control tag in `src/` for a residual `border-bone` — zero found. |
| 54 | RESOLVED | 4 - UI | **`.container-px` zeroed vertical padding on 363 elements sitewide. Found 2026-07-30 while verifying the section-band fix; this was the real cause of sections bleeding together, and the banding was only half of it.** `.container-px` was written `padding: 0 24px`. The shorthand sets all four sides, and the rule sits later in `global.css` than Tailwind's utilities at the same single-class specificity, so it silently overrode `padding-block` on every element written as `container-px py-10` / `py-12` / `py-14`. Proven by controlled A/B against the live page: `container-px py-10` computed padding-top/bottom **0px**; the same markup with `padding-inline` computed **40px**, and `py-14` went 0px to 56px. The class was in the markup the whole time and rendering nothing. The homepage was the only page unaffected, because `.band` puts `padding-block` on the `<section>` rather than on the inner container — which is exactly why the homepage read fine while every other page ran together. Fix: `padding: 0 24px` to `padding-inline: 24px` (and 16px at the mobile breakpoint). One line, restores the intended spacing on all 363. **Expect every page except the homepage to get materially taller** — this is the design the markup always specified, not new spacing. |
| 55 | RESOLVED | 9 - Testing / 8 - Ops | **The post-deploy smoke check picked an asset the homepage never loads, and failed three consecutive production deploys on good builds (2026-07-30).** `scripts/build-static-asset-proof.mjs` sorted `dist/client/_astro` by size and took the largest, which is `leaflet.*.js` — a route-specific chunk only `/camps/` requests. Nothing warms it at the edge, so the smoke check was the first request that path had ever seen, Cloudflare served a negative cached entry, and the check failed with a 404 carrying `cf-cache-status: HIT` and `content-type: text/html`. A retry against the same URL cannot clear a cached negative, which is why re-running only worked once propagation caught up. The script's own diagnostic had been correctly reporting this as "asset propagation lag plus edge negative caching, not a missing build" for weeks, and the branches `fix/smoke-cache-retry` and `diag/smoke-asset-failure` were both attempts at it. Fix: select the proof asset from the set the built `index.html` actually references, falling back to largest-overall when it references none. The asset is then warm by the time the asset check runs, because the smoke run requests `/` first. Proof strength is unchanged — still a content-hashed file compared by exact byte length and SHA-256, so the stale-build case it exists for is still caught. Verified against the real build: old rule picked `leaflet.DwdWx3EV.js` (homepage references it: false); new rule picks `page._MeRuh2j.js`, 145,515 bytes, homepage references it: true. Three regression tests added to `tests/static-asset-proof.test.ts` covering homepage-preference, fallback, and a missing `index.html`. |
| 56 | FIXED (incident closed 2026-07-30) | 8 - Ops / 9 - Testing | **A false-positive smoke check triggered the production auto-rollback and reverted roughly a day of shipped work.** Deploy #32 deployed fine, then the smoke check failed on `/_astro/leaflet.BuyN-j4Z.js` returning 404 with `cf-cache-status: HIT` — a cold route-only asset the homepage never loads (selection bug, item 55). Remediation rolled traffic to the id in `worker-rollback-target-2026-07-29.json`, which by then predated the navy/silver remap. Verified live during the incident: cream `#FAF6EE` and terracotta `#9E5228` present, navy `#041E42` absent, rail back to 5 items, doubled byline, no responsive hero images, no `.section-band`. Everything from PR #30 onward was off production. Restored by approving Deploy #33, which carried the item 55 fix. **Two defects, both fixed in `scripts/deploy-remediation.mjs`:** (1) *Stale target.* A receipt file ages while releases pile up behind it, so the gap between what is live and what rollback lands on grows every day you ship. The target is now resolved live from `wrangler versions list` at the moment of failure, and it is the version immediately preceding this deploy — blast radius one release, not however many happened since a receipt was cut. (2) *Untrustworthy trigger acted on alone.* A single asset 404 is not evidence a deploy is bad, and automation acting on a false positive is worse than automation doing nothing. Production now takes staging's posture: halt, exit non-zero, print the exact paste-ready rollback command naming the live-resolved version, and let a human decide. The script no longer executes anything against production. The alert also states that a lone 404 with `cf-cache-status: HIT` is usually edge negative-caching. 13 assertions in `tests/deploy-remediation.test.ts`, rewritten from the auto-rollback behaviour, all passing. Workflow steps renamed to 'Halt and alert' for both targets. |
| 57 | FIXED 2026-07-31 | 9 - Testing | **`check:rollback-target` is now a vestigial release blocker.** `ci:release` still runs `node scripts/check-worker-rollback-target.mjs coordination/release-evidence/worker-rollback-target-2026-07-29.json`, which fails once that receipt expires on **2026-08-05**. Remediation no longer reads that file — the rollback target is resolved live as of 2026-07-30 (item 56) — so from 2026-08-06 this gate will hard-fail every production build for a receipt nothing consumes. Decide before then: drop it from `ci:release`, or keep it as binding-parity evidence and re-cut the receipt on a schedule. Not changed in the item 56 pass because removing a release gate is Jeff's call, not a side effect of an incident fix. **Fixed 2026-07-31:** removed `check:rollback-target` from `ci:release` (`package.json`) and from `.github/workflows/ci.yml`; `scripts/check-worker-rollback-target.mjs` kept in the tree with a header explaining the retirement, for manual non-gating use only. See Pillar 8 row (anchor PILLAR-8-2026-07-31). |
| 58 | FIXED | 2 - Privacy | **2026-07-30. `org_contacts` PII layer was live in code and in `CONTACT-DATA-MAP.md` but absent from `DATA-MAP.md` and `disclosure.astro`.** Named human contact data (camp/program staff name, title, role, direct email/phone), compiled by two daily agents from an organization's own public pages, not submitted by the person. Fixed in `DATA-MAP.md` (data table, third-party note, retention, deletion path) and `disclosure.astro` (new policy bullet, opt-out via `/trust/`). See the Pillar 2 row above for full detail. |
| 59 | OPEN (human gate) | 2 - Privacy | **2026-07-30. `REGULATORY-RISK.md` written this session, required because `org_contacts` is data its subjects never submitted.** Names the data-broker-registry, CCPA/CPRA indirect-collection, GDPR Article 14, and FTC Section 5 exposure. The file itself is not a substitute for the lawyer's read Pillar 2 requires before migration `0028_org_contacts.sql` is applied to production or the table is used for any outreach — that review is Jeff's to schedule, a reviewer can only confirm the file exists, is current, and that the opt-out (`setDoNotContact()`) works without an account and survives re-import, all three true as of this pass. |
| 60 | LOW, FIXED | 3 - Consent | **2026-07-30. Cloudflare Turnstile was a real data flow (loads on 4 public forms, in the CSP, in `src/lib/turnstile.ts`) with no entry in `DATA-MAP.md`'s third-party table.** Fixed — added, marked essential/no-consent-needed per Pillar 3's own security-cookie exception, sells nothing. Not a Critical: it was never hidden from the CSP or from users, just missing from the map's paper trail. |
| 61 | SUPERSEDED by row 7 (this pass) | 7 - Stack | **2026-07-15 open item #21 (`wrangler.jsonc` staging-vs-production D1 confusion) is stale and now closed.** It described a pre-cutover state where production was still the Cloudflare Pages project `parent-coach-playbook` and there was no `wrangler.production.jsonc`. That topology no longer exists: production is the Worker `parent-coach-desk`, `wrangler.production.jsonc` is authoritative for it (and carries its own header comment saying so), and `wrangler.jsonc` is unambiguously staging with an isolated D1/R2/KV set — verified live against the Cloudflare account this session, see `ENVIRONMENT-MANIFEST.md`. No open action remains from #21; leaving the original text in place above (line 164) as a historical record of a real, now-resolved confusion rather than deleting it. |
| 62 | SUPERSEDED by row 13c (this pass) | 13 - Protect-the-App | **2026-07-28 open item #25's `content_overlay_receipts` claim does not describe anything that was ever built.** A 2026-07-30 audit found no inline editor, no `content_overlay_receipts` table, and no `CONTENT_OVERLAY` KV namespace anywhere in migrations, source, or a live read-only query against production `PCD_OPS_DB`. `ADMIN-ARCHITECTURE-REVIEW-2026-07-29.md` reached the same conclusion independently one day after #25 was written. A real receipts table and library were built this session instead — see row 13c above and `migrations-pcd-ops/0029_admin_action_receipts.sql`, `src/lib/admin-receipts.ts`, `tests/admin-receipts.test.ts`. Leaving #25's original text in place as the record of what was claimed versus what a later session actually found. |
| 63 | SUPERSEDED by row 13c (this pass) | 4 - UI / 10 - SEO | **2026-07-28 open item #26 describes scope boundaries for the same never-built inline editor #25 claimed.** Since the feature does not exist, its route-coverage tests and drift-detection script (`tests/overlay-route-coverage.test.ts`, `scripts/overlay-snapshot.mjs`) were not found either this session. No action needed unless the inline editor is actually built in a future session, at which point #26's boundaries (meta tags and article bodies stay out of scope) are still the right call and should be re-implemented alongside it. |
| 64 | HIGH, real gap | 13 - Protect-the-App | **2026-07-30.** `src/lib/admin-receipts.ts`'s `withAdminReceipt` is wired into only 7 of the 37 admin mutation routes: `camps/[id]/{approve,reject,verify,photo,update}` and `reviews/[id]/{approve,reject}` — the exact surfaces this session's brief named plus one adjacent pair. The remaining 30 (`claims/[id]/update`, `editorial/approve` and its siblings, `suggestions/[id]/update`, `source-quality/*`, `agents/*`, `link-health/*`, `data-quality/*`) still mutate with no receipt at all. `ADMIN-ARCHITECTURE-REVIEW-2026-07-29.md`'s broader concurrency finding (most approve/reject writes are blind `UPDATE ... WHERE id = ?` with no status precondition or lock, unlike the camps/reviews routes' `transitioned` guard) is unaddressed by this session and still real. Next session should extend `withAdminReceipt` to the rest of the admin surface before this row can read `fixed`. |
| 65 | LOW, cleanup | 7 - Stack | **2026-07-30.** A scratch file, `tsconfig.receipts.json`, was created at the repo root this session to run a narrow `tsc --noEmit` check outside the full project config (the sandbox could not resolve `astro`'s type declarations — `node_modules/astro` is not actually installed in this sandbox, a pre-existing environment gap, not a code issue). The sandbox's `rm` returned `EPERM` on this file through the mounted filesystem, so it could not be deleted from this session; its content was overwritten with an inert marker explaining why. Delete it before merging. |
| 66 | RESOLVED | 1 - Security | **2026-07-31.** A literal NUL byte in `src/lib/org-contacts.ts` (1 occurrence, `computeContentHash`'s join delimiter) and `scripts/proof.mjs` (5 occurrences, `generateCandidateId`/`dedupeKey`) — both should have been a plain space character, per the surrounding code and every rendered view of the files. Fixed: both replaced with spaces. This mattered beyond cosmetics — `scripts/scan-secrets.mjs`'s current-tree scanner explicitly skips any file containing a null byte, so `org-contacts.ts` (a PII-access file) had been invisible to every text-based secret scan run against this repo. A repo-wide sweep of all 918 tracked `.ts`/`.tsx`/`.mjs`/`.astro`/`.sql`/`.json`/`.py` files found no other source file with the same problem; two legitimately UTF-16-encoded data/cache files were left untouched. Root cause not determined — same pattern in two unrelated files is a flag, not conclusive evidence of a cause. Follow-up not built this session: add a CI check that fails (rather than silently skips) on a tracked text file containing a null byte. |
| 67 | RESOLVED | 1 - Security | **2026-07-31.** `src/lib/org-contacts.ts`'s `upsertOrgContact()` only enforced "an agent must never publish a contact" (`is_public`) as a doc comment, not in code — `input.isPublic ?? 0` on insert would have honored `isPublic: 1` from any caller. Real risk is theoretical today (migration unapplied, no route wires this table live), but it's exactly the AI-attack-surface pattern this pillar's addition names: an agent with write access, reading unscrubbed external content (organizations' own web pages), with no code-level barrier to publication. Fixed: `is_public` is now hardcoded to `0` on every insert regardless of input; the field is marked `@deprecated` on the `UpsertOrgContactInput` type. Publishing a contact now has no code path except a human-gated function that does not exist yet and should be built deliberately if ever needed. |
| 68 | OPEN, MEDIUM | 1 - Security | **2026-07-31.** Four secrets have no rotation date on record in `SECURITY-AUDIT.md`'s inventory: `SLACK_SIGNING_SECRET`, `SLACK_WEBHOOK_URL`, `GITHUB_TOKEN` (has `contents:write` on `jeffthomas4-lab/parent-coach-playbook` — a leak here is a repo-write compromise, the highest-impact of the four), and `BULK_IMPORT_TOKEN` (lower risk since no route auto-approves on it anymore, but still undated). No session can read a secret's value or confirm a dashboard-side rotation date; this needs Jeff to confirm each is set and log a date the next time he's in the Cloudflare dashboard. |
| 69 | OPEN, MEDIUM | 1 - Security | **2026-07-31.** No Cloudflare WAF (edge-layer) rate-limiting rule confirmed on `/admin/*` or `/api/admin/*`. Live-verified this session that the app-side gate works (`curl`: both paths 302 to Cloudflare Access for an unauthenticated request), but this pillar's addition requires brute force to die at the WAF, not only in app code, and nothing in the repo indicates an edge rule exists. Jeff: Security > WAF > Rate limiting rules in the Cloudflare dashboard — there's a one-click "protect your login" preset. |
| 70 | CRITICAL, NEW | 14 - Mobile | **2026-07-31.** `/camps/` renders at **84,749px tall at 390px width** — roughly 10x this pillar's own ~8,000px flag threshold, and by far the worst page on the site (homepage is 9,563px). Live-measured against the deployed site (fetch+iframe technique, same as the 2026-07-30 pass). Root cause: `src/pages/camps/index.astro` calls `listApprovedCamps(env.DB)` with no `LIMIT` and server-renders every one of 1,300+ approved camps as a full `<li>` card — the same over-fetch already flagged under Pillar 7 ("no over-fetch/pagination on camps/index.astro", that row's own open finding) but never previously connected to its Pillar 14 consequence. Also carries 396 tap targets under 44px on this one page, which scales with camp count rather than being 396 independent defects. **Not fixed this session:** the page's ~450-line client-side map/filter script (`campsLite` JSON payload) depends on the full dataset being present in the DOM/JS for instant client-side search, so capping the server-rendered card list without breaking search or the map needs a real design decision (DOM windowing, incremental render-on-scroll, or a paginated list under the map with the map itself still fed the full dataset) — not a mechanical edit, and risking the site's primary camp-finder feature blind was judged worse than logging this clearly. Next Pillar 14 or Pillar 7 session should own a windowing/virtualization pass on this page before this item can close. **2026-07-31, later same day (anchor: CAMPS-RENDERING-REBUILD-2026-07-31): that windowing pass is done.** See item #78 below for the full fix. Note on numbering: the task brief that commissioned this fix called it "open item #74" — #74 is actually the unrelated Pillar 8 Queues finding above; this row (#70) is the real CRITICAL Pillar 14 `/camps/` finding the fix addresses, and item #78 is where the fix is logged. |
| 71 | LOW, cleanup | 14 - Mobile | **2026-07-31.** `/what-to-buy/` (5,996px, 29 tap targets under 44px) and `/sports/soccer/` (10,519px, 36 under 44px) were measured live at 390px this session for the first time — the 2026-07-30 Pillar 14 pass only covered the homepage. Neither was cross-referenced against current source the way the homepage's 28-item gap was (see the Pillar 14 row above): given the homepage pattern (every live gap traced to an already-fixed-in-source, not-yet-deployed class), these two are likely the same stale-production artifact rather than open source defects, but that was not verified item-by-item this session for time. Re-measure against the deployed site after the next production deploy and itemize any real remainder. |
| 72 | RESOLVED | 4 - UI / 10 - SEO | **2026-07-31.** Item #36 (810 OG cards generated against the retired warm palette) is confirmed already closed as of this session's baseline: `public/og/` holds 812 `.jpg` files, and `scripts/build-og-images.py`'s brand-color constants already match `pcd-tokens.mjs`'s navy/silver-green values (a `STANDARD-AUDIT #36` comment in the script documents the fix). No further action needed. |
| 73 | FIXED | 14 - Mobile | **2026-07-31.** Extended the responsive-image pattern (`Illo.astro` / `build-illustration-variants.py`) from the homepage's 16 images to every illustration on the site. Generated `-480w`/`-960w` variants for all 268 `public/illustrations/*.webp` source files (804 files on disk total, up from 48). Converted every remaining static illustration `<img>` to `<Illo>`: `src/pages/body/index.astro`, `src/pages/youth-sports-pendulum.astro`, `src/pages/team-parent/index.astro`, `src/pages/pathways/index.astro`, `src/pages/parent-coach.astro`, `src/pages/newsletter.astro`, `src/components/NewsletterSignup.astro`, `src/layouts/PillarLayout.astro` (×2, drive-there/drive-home), `src/components/LeadMagnetCTA.astro`, `src/pages/cost-calculator.astro`, `src/pages/camps/index.astro` (empty-state illustration). Converted `src/layouts/ArticleLayout.astro`'s per-article hero image, the single highest-leverage image on the site (renders on all 812 published articles), to `<Illo>` with the variant base derived from the article's `hero` frontmatter path at render time, keeping a plain-`<img>` fallback for the theoretical case a hero ever points outside `/illustrations/`. Added `srcset`/`sizes` (not `Illo`, since the crop/dimensions differ from the component's default) to the hero thumbnails in `src/pages/reads/[topic].astro` and `src/pages/team-parent/[topic].astro`'s article-grid cards. Left out of scope on purpose: `/camp-photos/*` (user/admin-uploaded, dynamic post-build, would need an image-resizing service per this pillar's own reasoning in `Illo.astro`'s comments) and the admin-only camp-photo preview in `admin/camps/[id].astro` (gated behind Cloudflare Access, no reader impact, matching this file's established convention for admin surfaces). All 14 touched files validated clean through `@astrojs/compiler`'s `transform()`. |
| 76 | OPEN, informational | 1 - Security | **2026-07-31.** (Renumbered from a duplicate #70 created by two parallel audit lanes on the same day.) This site has no live app-level session-issuing auth today. admin is 100% Cloudflare Access (session lifetime is an Access-application dashboard setting, not app code; Jeff should confirm it's set on purpose, not left at the default), and the customer/owner auth layer in `src/lib/customer-*.ts`/`owner-*.ts` is fully gated behind `PCD_CUSTOMER_FOUNDATION_ENABLED=false` with no live route except `src/pages/owner/index.astro`. The standard's session-lifecycle asks (lifetime set on purpose, concurrent-session cap/visibility, kill-on-password-change) have no live trigger to apply to yet. Not a finding against the site as it stands; flagged as a build-time gate for whoever ships that feature — this section needs a real re-run the moment the flag flips `true`, not a copy-paste of this note. |
| 74 | OPEN, HIGH | 8 — Ops | **2026-07-31.** Slow work still runs inline instead of through Cloudflare Queues: `camps/submit.ts` awaits two email sends (Resend, Slack-staging fallback) before responding, and `publish.ts`/`admin/editorial/approve.ts`/`set-status.ts` each await two sequential GitHub Contents API calls inline. All are wrapped in try/catch with their own timeouts so a failure degrades rather than 500s, but none carries a `max_retries`/`dead_letter_queue`/idempotency-key story if the provider is slow rather than down. The account has been on Workers Paid since 2026-07-18, so Queues (an incremental cost already effectively covered) is available. Not wired this session — needs a queue binding in `wrangler.production.jsonc`, a consumer Worker, and a live-preview verification pass this sandbox cannot build to prove; wiring it blind was judged riskier than logging it. |
| 77 | FIXED | 4 - UI / 14 - Mobile | **2026-07-31. The tap-target sweep broke every nav dropdown that carries a blurb, live in production.** Reported by Jeff mid-audit with screenshots of the Reads and Tools menus. Item #48's 44px sweep added `min-h-11 flex items-center` to three anchor patterns in `src/components/NavBar.astro` (the Reads grid at line 46, and both Tools columns at 122 and 131). Those anchors hold two children, a bare label text node and a `<span class="block">` blurb. `display:flex` promotes the bare text node to an anonymous flex item, so the label and the blurb became siblings in a **row** and rendered side by side, overlapping, instead of stacked. The single-line anchors in the same file were unaffected because they hold one child, which is why the sweep looked clean when it shipped. Fixed by moving the three to `flex flex-col justify-center items-start`, which keeps the 44px target the sweep was buying and restores the stack. Swept the rest of `src/` for the same shape (a `flex items-center` container holding a block-level child) and found no other instance. `@astrojs/compiler` `transform()` on the changed file: zero diagnostics. **Lesson for the standard:** adding a display mode to hit a tap-target minimum is a layout change, and `/web:mobile`'s 44px rule needs a companion check that the element's own children did not get reflowed by it. This is the same class of failure as item #54's `.container-px` shorthand. |
| 75 | OPEN, MEDIUM | 9 — Testing | **2026-07-31.** A full happy/failure/auth test inventory across all 51 files under `src/pages/api/**` against all 70 files under `tests/api/` has not been done by hand this session — spot-checked that the ten editorial routes this session's logging rollout touched all have a matching test file, which they do, but a route-by-route "does it have all three" pass for the full surface (including the newer admin-receipts-wired camps/reviews routes and `src/lib/admin-receipts.ts` another agent added this session) is still owed as its own pass. |
| 78 | FIXED | 14 - Mobile / 7 - Stack (anchor: CAMPS-RENDERING-REBUILD-2026-07-31) | **2026-07-31.** Closes item #70, the CRITICAL `/camps/` 84,749px finding. Re-measured the live page first rather than trusting the earlier "1,300+ camps" guess in item #70's own text: the deployed `/camps/` is 603,000 bytes, 323 `.camp-card` DOM nodes (311 approved camps in the PNW/National grids + 0 featured + 12 Recently Added), and the `camps-data` JSON payload is 218,759 bytes. **Root cause fixed at the query:** `src/lib/camps-db.ts`'s `listApprovedCamps()` had no `LIMIT` at all; it now takes an optional `limit` (default `APPROVED_CAMPS_HARD_CAP = 1000`, about 3x today's 311-camp inventory) and logs a loud `console.error` if a call ever comes back at the cap, so a truncation is a visible signal, not silent data loss. Its one other caller (`approvedCampsWithinRadius`, camps-db.ts:1544) keeps the same default, no behavior change. **Root cause fixed at the render:** `src/pages/camps/index.astro` used to build every approved camp into a DOM card because the client filter script only ever toggled `display:none` on cards that already existed — it never rendered a card itself. Pulled `renderCardHtml`/`renderFeaturedCardHtml` into a new shared module, `src/lib/camp-card.ts`, imported by both the Astro frontmatter (SSR) and the client-side filter script, so a server-built card and a client-built card are byte-identical. The PNW and National grids now server-render only the first 24 matching cards each (`CARD_WINDOW = 24`, mirroring the client's default filter so nothing flashes and then disappears); a "Show more" button per region renders 24 more from the already-shipped `campsLite` JSON on click, and any filter change re-windows to the first 24 of the new match set. Featured and Recently Added stay fully rendered — already small and bounded (0 and 12 today). The map and the zip/haversine filter still get every matching camp regardless of what's in the DOM: `campsLite` stays the full (bounded-by-query) dataset, only the card *count* is windowed, not the *data*. Extended `campsLite` with exactly the four fields the card renderer needed and it didn't carry before: `hero_photo_key`, `price_text`, `last_verified_at`, `date_added`. The results line (`#camps-results-count`, now `aria-live="polite"`) reports the true total match count under the current filters, not the rendered-card count — computed server-side for first paint and kept current by the client on every filter change. **No-JS readers get:** the Featured rail (full), Recently Added rail (full, capped 12), and the first 24 PNW + first 24 National cards under the default filter (leagues and full programs hidden, matching the JS default) — a `<noscript>` line under each grid states this plainly, and a `<noscript><style>` rule hides the "Show more" buttons so no-JS never sees a control that does nothing. Search, sport/age/date filters, the map, and "Show more" all require JS, same as before this fix (the filter bar had no non-JS fallback previously either — this fix does not add or remove that dependency, it only changes how many cards no-JS sees). **Real numbers, before -> after (before = live-measured today; after = projected from source, not deployed, so not directly measurable):** DOM `.camp-card` count 323 -> 60 (24 PNW + 24 National + 0 Featured + 12 Recently Added). Card HTML bytes 344,643 measured -> ~64,000 projected (60/323 of the measured total; per-card markup is unchanged, only the count shrinks). `camps-data` JSON 218,759 measured -> ~253,000-254,000 projected (adding `last_verified_at` to all 311 records is exact, since verified=0 for all of them live today so it's `null` on every row: +7,464 bytes measured-exact; `date_added` is NOT NULL in the schema so it's exact too: +12,129 bytes; `hero_photo_key` and `price_text` growth is estimated, not measured, assuming roughly 30% and 40% live-data coverage respectively since neither field was ever serialized before this fix: +8,400 and +7,000 bytes estimated). Total page bytes 603,000 measured -> ~358,000 projected. Mobile height at 390px: **not measured, the branch is not deployed** — projected only, and stated as a projection per this pillar's own rule against claiming a measured number that wasn't measured. Using the measured 84,749px / 323-card figure as a per-card baseline (~244px/card once a rough fixed-chrome allowance is backed out) and 60 rendered cards, the projected height lands roughly in the 15,000-21,000px range: a 75-82% cut from today, but likely still above this pillar's ~8,000px flag, because 60 substantial one-column cards (photo, title, location, CTA, padding) do not compress to 8,000px on their own. Closing this fully to under 8,000px would need a smaller first window (12 instead of 24) or scroll-triggered lazy rendering instead of a click-triggered "Show more" — logged as a real residual, not claimed as solved. **Verification method:** `@astrojs/compiler`'s `transform()` on the changed `.astro` file — 0 errors (3 pre-existing informational hints on unrelated `<script>` tags, not touched by this fix). `npx tsc --noEmit` scoped to `camp-card.ts`, `camps-db.ts`, `camp-freshness.ts` via a narrow tsconfig — 0 errors. The client `<script>` block's TypeScript, extracted and typechecked the same way against a DOM-lib tsconfig — 0 errors. `astro build` and `npx vitest run` could not be run in this sandbox (the former bus-errors, the latter fails on a missing native `@rolldown/binding-linux-x64-gnu` module — both pre-existing, previously documented sandbox limits, not caused by this change). Full detail: `reports/CAMPS-RENDERING-REBUILD-2026-07-31.md`. |

| 79 | FIXED (anchor: CAMPS-RENDERING-QA-2026-07-31) | 1 - Security / 6 - A11y / 14 - Mobile | **2026-07-31, independent QA of item #78's rebuild.** Verified the rebuild's specific claims against source rather than the report. Two real fixes, everything else confirmed correct: **(1) Stored-XSS, `src/pages/camps/index.astro`'s `renderMap()`.** The Leaflet popup builder interpolated `c.name`/`c.city`/`c.state` into an HTML string passed straight to `marker.bindPopup()` with no escaping — `renderCardHtml`/`renderFeaturedCardHtml` escape every field correctly (confirmed), this one function didn't. Predates this session's rebuild (`git diff` confirms `renderMap()` untouched by item #78), not a regression, but a real gap. Fixed: escaped `name`/`city`/`state`/`slug` with the same `escHtml` the card renderer uses. **(2) `listApprovedCamps`'s cap-hit signal was a bare `console.error`, not this repo's structured logger** (`src/lib/log.ts`, used by 30+ other files including `lib/events.ts` for the same "no Request object" case). Fixed to `log('error', {...})` with a generated `requestId`, matching the existing non-request-caller pattern. Everything else in the rebuild checked out: server/client card markup is byte-identical (confirmed by typechecking the extracted client script against the shared `camp-card.ts`, not stubbed); a filtered camp outside the rendered window is always reachable via "Show more" (traced `applyFilters()`/`renderGrids()`); the results count reads `visibleCamps.length`, the true filtered total, not the window; the map iterates the full filtered set, not the DOM window; the hard cap (1000) covers both callers of `listApprovedCamps`, including `approvedCampsWithinRadius`. **New gap closed:** "Show more" had no `aria-live` announcement — clicking it re-rendered the grids silently for a screen-reader user, since the page's only live region (`#camps-results-count`) doesn't change on that click. Added a second `aria-live="polite"` region (`sr-only`) and an announcement on each "Show more" click. **Mobile-height residual (item #78's own open item) closed with arithmetic, not assumption:** recomputed real per-card height at 390px from confirmed CSS values (`.container-px` 16px mobile padding, card `p-5` 20px, `t-card-title` 18px/`leading-tight`, etc.) instead of assuming 250px — landed at ~248px/card full-featured, cross-checked against the live 84,749px/323-card baseline (implies ~4,645px of non-card chrome, independently itemized bottom-up at ~3,700px, reasonable agreement). Found the "Added [date]" line renders on every card unconditionally (`date_added` is `NOT NULL`), a fixed cost the original projection didn't flag as guaranteed. Fixed by (a) hiding the 16:9 photo below 768px via a new `.camp-card-photo` CSS hook — the single largest per-card element, ~191px, and decorative rather than informational at phone width; (b) capping the client's mobile initial window to `CARD_WINDOW_MOBILE = 6` per grid (desktop unchanged at 24) via `isMobileViewport()`, since the page is edge-cached with no `Vary` on viewport so SSR can't do this — the existing unconditional `applyFilters()` re-render on load already re-renders the grids anyway, so this costs nothing extra; (c) capping the Recently Added rail to 3 visible on mobile (CSS `nth-child`) since it duplicates camps already in the main grids. Projected new mobile height: 15 compact cards (6+6+3) × 190px + ~4,645px chrome ≈ **7,495px**, under the ~8,000px flag with ~500px margin. Labeled a projection throughout — branch not deployed, no real browser layout engine involved. **Escalated to Jeff:** confirm with a real Lighthouse/mobile-emulator pass after deploy; if the real number lands materially over 8,000px, the documented next lever is `IntersectionObserver`-based scroll loading instead of the "Show more" button. Verification: `npx tsc --noEmit` on `camp-card.ts`/`camps-db.ts`/`camp-freshness.ts`/`log.ts` (added to the narrow tsconfig this pass used) — 0 errors. Extracted client `<script>` typechecked against a DOM-lib tsconfig with the real shared modules alongside it — 0 errors. `@astrojs/compiler` `transform()` on the changed `.astro` file — 0 errors, same 3 pre-existing informational hints as item #78. Full detail: `reports/CAMPS-RENDERING-REBUILD-2026-07-31.md`, "QA pass" section. |

| 80 | FIXED (anchor: CAMPS-LITE-ENDPOINT-2026-07-31) | 8 - Ops / 13 - Protect-the-App | **2026-07-31.** Closes item #23 (Pillar 8: "Camp-listing pages read D1 on every request with no Cache API or KV layer in front of them"). Also finishes the byte-weight half of item #78's rebuild: the DOM fix in #78 stopped rendering all 323 cards, but the full `campsLite` array (projected ~253,000-254,000 bytes at 311 camps) still shipped as an inline `<script type="application/json" id="camps-data">` island on every page load. **What changed:** new `GET /api/camps/lite` (`src/pages/api/camps/lite.ts`) serves the same data, projected from `Camp[]` by one shared function (`toCampsLite`, `src/lib/camps-lite.ts`) so the endpoint and the card renderer never drift in shape. `src/pages/camps/index.astro` no longer builds or inlines `campsLite` at all; the client fetches it in the background after first paint (`requestIdleCallback`, `setTimeout` fallback for Safari) via `scheduleCampsLiteLoad()`. Every filter control, both "Show more" buttons, and the map stay natively `disabled` (not just visually) until the fetch resolves, with a visible and `aria-live="polite"` status banner (`#camps-data-status`) covering pending, ready, and error states, plus a retry button on failure. The server-rendered cards need nothing from this fetch, so the page stays useful if it never succeeds. Not paginated, on purpose: the client's filters and the Leaflet map read the full array on every change, and a paginated lite endpoint could make a sport filter in a low-inventory state silently miss camps on an unfetched page. **Caching:** the Cache API (`caches.default`), not KV. No wrangler binding needed; this repo's one KV namespace (`SESSION`) is unrelated. Stated TTL 5 minutes fresh (`CAMPS_LITE_CACHE_TTL_SECONDS`), stale-while-revalidate up to 1 hour, plus a background `waitUntil()` refresh at 4 minutes old so a fresh copy is usually ready before the entry expires. Stated invalidation: `purgeCampsLiteEdgeCache()` runs after every admin mutation that can change the result set (`admin/camps/[id]/{approve,reject,verify,update,photo}.ts`) and after `cron/camps-sweep.ts`'s stale-archive stage when it archives anything. Stated limitation, not hidden: Cloudflare's Cache API purges per-colo, not globally, so the 5-minute TTL is the real cross-edge staleness bound, not the purge call. **Projected byte impact (branch not deployed, arithmetic not a measurement):** prior projected total ~358,000 bytes, minus the ~253,000-254,000-byte JSON island removed, plus source-measured additions (2,223 bytes markup, 678 bytes CSS, 7,311 bytes pre-minification JS) lands around 105,000-115,000 bytes. Inline camp JSON on first paint: zero. **A real bug found and fixed along the way:** `tests/helpers/context.ts`'s `makeContext()` defaulted five rate-limiter bindings but not `PUBLIC_READ_RATE_LIMITER`, which `enforcePublicWriteRateLimit()` fails closed on with a 503 when undefined; that means the pre-existing `camps-nearest.test.ts` and `camps-search-priority.test.ts` would both get a 503 instead of their asserted status on a real `npm test` run. Fixed by adding the matching default. **Verification:** `@astrojs/compiler` `transform()` on the changed `.astro` file, 0 errors (2 pre-existing informational hints, down from 3 now that the removed script island is gone). `tsc --noEmit` against narrow throwaway tsconfigs (not committed) covering the two new lib files, the endpoint plus its dependency chain, all five admin routes plus the cron sweep, the extracted client `<script>` against a DOM-lib config, and the new test file plus the fixed `context.ts`: all six passes, 0 errors. One real TS bug surfaced in that process: this project's tsconfig has no explicit `lib` override, so the default lib silently includes DOM, whose `CacheStorage`/`Cache` types (no `.default`) shadow `@cloudflare/workers-types`' declarations for the same names, so `caches.default` fails to typecheck even though it is a real, documented, working Cloudflare API. Fixed in `src/lib/camps-lite-cache.ts` with local interfaces cast through `unknown`, confirmed with an isolated repro against this repo's own `node_modules` before treating it as real. `npm test`/`astro build`/`npm run check` could not run in this sandbox (same pre-existing limits as items #78/#79). Full detail: `reports/CAMPS-RENDERING-REBUILD-2026-07-31.md`, "Background data load for campsLite" section. **Escalated to Jeff:** run `npm install && npm run build && npm run check && npm test` before shipping; once deployed, `curl -i` the endpoint twice and check `cf-cache-status`/age, then approve a camp in `/admin/` and confirm the next read reflects it within the stated staleness window; and confirm the ~105,000-115,000-byte projection against a live `curl | wc -c` or Lighthouse pass. |

**2026-07-20 note (Pillar 7):** Website-Build-Standard Pillar 7 gained a bullet naming Cron Triggers as the native scheduled-task tool and citing this repo's `camps-sweep-cron.yml` as the reference pattern for a Pages site with no native cron. No status change: this site already runs the pattern the standard now names. Retire the GitHub Actions cron for a native Cron Trigger once the Pages-to-Workers migration (open item #13) lands.

## Code-quality reviewers (non-pillar)

| Reviewer | Status | Note |
|----------|--------|------|
| /web:structure | pass | Conventional Astro 5 layout. .gitignore covers dist/, .env*, node_modules. Dead Sanity entries harmless. |
| /web:data-model | pass | D1 queries parameterized with .prepare().bind(). Dynamic column builder in updateCamp uses allowlist only. 10 forward-only migrations. |
| /web:naming | pass | Routes match URL patterns. Components PascalCase, utils camelCase. |
| /web:states | pass | DB availability check + try/catch + empty-state renders in camps/index.astro and SSR pages. |
| /web:errors | pass | Generic messages to client, detailed errors to console.error only. No stack traces in responses. |
| /web:performance | pass | Cache headers solid (1yr immutable for hashed bundles, edge cache for HTML). Fonts self-hosted. CLS fallbacks present. Leaflet and web-vitals now bundled — no CDN deps remaining. |
| /web:mobile | pass | Mobile-first nav with hamburger at lg:hidden. article-body responsive font. container-px 24px mobile padding. clamp() type scale. |
| /web:deploy | pass | Deploy commands match Deployments.md. wrangler.jsonc correct project/database bindings. |
| /web:demo-test | pass | npm run build (astro build + worker bundle) passes cleanly on all changes. |

## Definition of done checklist

- [x] STANDARD-AUDIT.md exists at root — every pillar marked.
- [x] SECURITY-AUDIT.md (11-item gate, secrets inventory) mostly complete — gitleaks scan date still missing.
- [x] DATA-MAP.md exists and retention/deletion paths documented. `REGULATORY-RISK.md` now exists too (added 2026-07-30, `org_contacts`) — lawyer read still owed, see open item #59.
- [x] Privacy policy and Terms of Service live and linked in footer.
- [x] Accessibility pass (skip link, focus ring, ARIA, prefers-reduced-motion fixed).
- [x] Build compiles — re-verified 2026-07-14: `npm run build` clean (0.62 MiB worker bundle), `npx vitest run` all 20 suites / 107 tests green, `npm run check` 0 type errors.
- [ ] **No open Critical or High items — FALSE as of 2026-07-15. Three open Highs: #17 (burned OpenAI key, unrotated), #19 (live 500 on 296 camp pages, fixed in source, not deployed), #20 

## Session note 2026-07-20 (admin fixability pass)

Every issue surface in /admin now carries a fix path. New: /admin/data-quality (row-level drill-down with inline fixes for all Directory data quality metrics), /admin/agents (pause/resume, acknowledge needs-you runs), link-health actions (recheck, mark resolved, replace URL), source-quality skip-list (D1-enforced at submit) plus bulk reject-by-domain, suggestion-to-draft-camp promotion, editorial publish/send-back/reopen/resolve-flag, camps queue inline edits now save (they were decorative before). Bug fixed: operations-status data-quality query referenced city/state/address on programs (columns live on organizations), so the block always read "query failed" in production. Bug fixed: worker-link-checker bound D1 8336fa9f while the admin page reads activity-radar; rebound. Live-data auto-fixes applied via API with backup (backups/2026-07-20-autofix-backup.md): 4 registration URLs http-to-https, 93 source_domains derived. Migrations pending remote apply: worker-link-checker/schema.sql (link_health was missing from activity-radar entirely), 0016_link_health_actions, 0017_domain_skip_list. New endpoints all sit behind requireAdmin + requireSameOrigin with bound parameters; 508/508 API tests green including new suites.
