// Stray scratch file from a syntax-check step during the 2026-07-15 SEO
// session. This sandbox's mount would not allow deleting it (rm failed
// with "Operation not permitted" on an owned, 0700 file -- a mount
// limitation, not a permissions bug). Safe to delete by hand; not
// referenced anywhere. The real tool is redirect-fixer.mjs in this same
// directory.
