# Remediation of Codex NO-GO review — 2026-07-17 (Claude Code)

All changes are local working-tree edits. No commit, deploy, push, migration, secret, provider, or send occurred. Commit is a Windows-host step. Diff: run `git diff` / `git status` in the repo.

| # | Codex item | Action taken | Status |
|---|---|---|---|
| 1 | Reject not concurrency-idempotent | `rejectCamp` (src/lib/camps-db.ts) rewritten to a single atomic conditional `UPDATE programs SET pcd_status='rejected' ... WHERE id=? AND pcd_status!='rejected'`; returns `{camp, transitioned}` where `transitioned` derives ONLY from `result.meta.changes`. Both reject routes gate `upsertDomainQuality` on `transitioned`; the prior read-then-write (getCampById) removed. Tests: sequential replay, race-shaped (upsert gated on change-count not a prior read), not-found→404, both route aliases; new tests/api/camps-db-reject-atomicity.test.ts asserts no decision-making SELECT precedes the UPDATE. | FIXED |
| 2 | CSRF compares only host | `requireSameOrigin` (src/lib/admin-auth.ts) now compares full `new URL(x).origin` (scheme+host+port) on both the Origin and Referer paths; fails closed. Tests added for http-vs-https mismatch (Origin and Referer), exact-match allow. | FIXED |
| 3 | JSON-LD cleanup | Archived the offending scratch file out of `src` → `wip-archive/what-to-buy-slug.astro.WIP-backup-2026-06-15.txt`; `grep -rn "set:html={JSON.stringify" src` is now empty. `serializeJsonLd(undefined|null)` now throws a clear guarded error naming the fix, instead of a bare TypeError. | FIXED |
| 4 | Buildout duplicate | `buildout/hit-rate-test/out/results.jsonl`: removed exactly one record — the second `org-a3b0872c-...` entry, which was byte-for-byte identical DATA to the first (differed only in JSON spacing) and absent from HEAD. The two other duplicate org_ids (1891ec7c, 0e9979d6) are PRE-EXISTING in HEAD and were left untouched. Re-validated: all lines parse; a3b0872c now appears once. No other buildout file touched. | FIXED |
| 5 | P1-3 rollback validator | Added `PCD_OPS_DB: 'd1'` to requiredBindings in scripts/worker-rollback-target.mjs + a negative test. The committed 2026-07-16 receipt was NOT regenerated (needs approved fresh live read), so `npm run check:rollback-target` now correctly FAILS against the stale receipt — the intended signal. The happy-path unit test uses a completed fixture so test:unit stays green. | FIXED (receipt refresh deferred, owner-gated) |
| 6 | P1-4 verified production deploy | New scripts/deploy-production-verified.mjs: refuses dirty worktree / `HEAD != --sha`; requires wrangler.production.jsonc; builds+verifies the exact production manifest (Worker name, D1/R2/KV bindings incl PCD_OPS_DB, origins, all 7 flags "false"); requires `--confirm-production` + a typed confirmation token; writes a post-deploy receipt only after an approved deploy. Default = dry-run plan. NOT executed. node --check clean. | DONE (not run) |
| 7 | Plan 018 boundaries | New scripts/check-ops-migration-boundary.mjs pins SHA-256 for exactly the 12 files 0011–0022, rejects any extra/missing migration, and `assertPendingLedgerExactly` guards the remote ledger; passes live. Plan 018 revised: reviewed-commit-SHA field, 12-entry hash table, pre/post ledger checks, manifest/config digest capture, and reference commands replaced with the boundary check + `deploy-production-verified.mjs` invocation (REFERENCE ONLY / DO NOT RUN). | FIXED |
| 8 | Offsite backup tools | Both scripts hardened: create-only PutObject (`IfNoneMatch:'*'`) + unique per-run prefix + recorded VersionId; post-upload HeadObject verifies actual Object Lock mode + retain-until (throws on mismatch); streaming upload/readback (lib-storage + createReadStream, incremental hash); `--skip-restore` removed (restore mandatory — no path returns verified without it); Windows-safe python + os.tmpdir(); unused SECRET_ENV_NAMES removed; refactored to exported functions with tests/offsite-backup.test.ts (manifest/path/traversal, duplicate-key, retention mismatch, hash failure, mandatory restore). node --check clean. Not run with credentials. | FIXED |
| 9 | Counsel worksheet + matrix | Counsel R-05 corrected (the public `/camps/submit` form IS live and collects submitter email + address; distinct from the disabled trust-intake form). Added risk rows R-20–R-24 for the reject race, origin-scheme, rollback-receipt, migration-boundary, and offsite-tool items (compensating controls noted; decisions left blank). R-10 reworded so the R2 disposition is described as an owner decision recorded in-session (citing the disposition file), not "independently verified." LAUNCH-AUTHORIZATION-MATRIX reconciled to "seven gates pending; r2_recovery not_applicable by owner disposition 2026-07-17." | FIXED |
| 10 | Release evidence re-cut | rc01 given a `superseded_note`: source_commit/tests_and_build/deploy_approval/post_deploy_observation describe the prior Plan 015 committed candidate; they must be re-established after the operator commits + a fresh full suite runs. Count re-cut (Codex saw 111/574; now higher) deferred to that post-commit run. | PARTIAL (re-cut deferred to post-commit, per your item 10) |

## Test commands / results
Could not run the full suite in this cloud sandbox (Windows-native node_modules; bridged Linux VM has no network/disk to rebuild). Verified here: all changed/new `.mjs` pass `node --check`; `check-ops-migration-boundary.mjs` PASSes live; `npm run check:release-evidence` and `check:secrets` pass; reject/CSRF/route logic verified by direct re-read. Backup functions verified via a manual Node harness (all cases pass). REQUIRED on the Windows host after commit: `npm run test:unit`, `test:integration`, `check`, `check:release-evidence`, `check:secrets`, `npm audit --audit-level=high`, `check:production-manifest`, plus `check:rollback-target` (expected to FAIL until the receipt is refreshed).

## Deferred (with reason)
- Fresh worker-rollback-target receipt including PCD_OPS_DB — needs a separately approved read-only live binding/version read (your item 5).
- rc01 count re-cut + source_commit/deploy/observation re-establishment — after operator commit + fresh suite (your item 10).
- Executing deploy-production-verified.mjs, any migration, backup upload, notification drill, access probe — all owner/live-gated.
