# Claude Code continuation prompt — Parent Coach Desk

You are Claude Code, the autonomous local implementation partner for Parent
Coach Desk. Codex is the independent architect, critic, and verifier. Jeff is
the sole authority for production-affecting or external actions.

Work in:

`C:\Users\jeffthomas\Desktop\Claude Cowork\Outputs\parent-coach-desk`

## Objective

Continue from the completed bounded local overnight build. Verify what is
already present, then advance the remaining plan in the safest evidence-backed
order. Do not repeat finished work, weaken existing gates, or describe a runtime
or business outcome as complete without direct evidence.

## Read before acting

Read these files completely, in this order:

1. `AGENTS.md` and any more-specific applicable agent instructions.
2. `coordination/CURRENT_STATE.md`.
3. `coordination/BUILD-PLAN-COMPLETION-AUDIT-2026-07-18.md`.
4. `coordination/handoffs/2026-07-18-overnight-build-handoff.md`.
5. `PCD-CODEX-BUILD-PLAN-v2-2026-07-18.md`.
6. `DEPLOY.md` and the protected deployment runbook it identifies.
7. `automation/TASK-RUN-LOG-RECONCILIATION.md` before touching any scheduled
   task workflow.

Then inspect the current diff, recent commits, relevant tests, and generated
control inventories. Treat the current checkout—not this prompt—as the final
authority if anything has changed.

## Current verified baseline

The bounded local build is complete through commit:

`5a24aa8 Build Parent Coach Approved foundation`

The last verified baseline was:

- 161 Vitest files and 809 tests passing with one worker.
- Astro diagnostics: 0 errors, 0 warnings, 356 existing hints.
- Production build passing.
- Built public internal-link contract passing.
- 139/139 application routes classified.
- 85/85 tracked root artifacts classified.
- 36/36 tracked top-level directories classified.
- No deployment, migration, secret change, provider activation, payment,
  posting, outreach, or production data mutation was performed.

Re-run only proportionate checks during discovery. Do not claim the baseline is
still current until you verify it in this checkout.

## Work completed

The repository now includes:

- Canonical repository ownership, exhaustive root-artifact classification, and
  machine-enforced top-level directory roles.
- Reconciled developer guidance and one protected Worker deployment authority.
- Evidence-qualified HowTo and connected Organization, WebSite, publisher,
  author, and Article structured data.
- A deterministic 18-month editorial refresh queue covering the current live
  corpus, with protected aggregate reporting and human-gated decommissioning.
- Expanded live-404 checks, fail-closed camp sitemaps, Worker-first sitemap
  routing, and release-gated built-link integrity.
- Evidence-governed camp verification, discovery filters, verification
  freshness, duplicate visibility, protected quality metrics, and conservative
  Event schema that fails closed on incomplete dates or locations.
- Privacy-minimized demand reporting and content-opportunity scoring.
- Affiliate destination validation, release gating, consented first-party click
  intent, and protected aggregate affiliate reporting.
- A newsletter lifecycle evidence contract and consented CTA-location intent
  tracking without email, URL, identity, or subscription outcome.
- Seasonal zero-write controls for August through November.
- Secure agent-run callers, an exact ten-task reconciliation inventory, and a
  redacted canary/rollout evidence packet that stores no bearer values.
- Draft-only Pinterest and Facebook distribution foundations with measured
  first-party links and mandatory human publication gates.
- A protected trust dashboard and inert source-backed relationship research
  registry with no outreach authority.
- Access evidence gates, generated secret-name preservation, durable smoke
  receipts with detached checksums, and exact deployed-asset verification.
- A separate `/parent-coach-approved/` product-mark foundation. It requires 12
  weeks of use, two alternatives, wear evidence, candid verdict/wrong-for/skip
  guidance, valid human approval, and no paid placement. The registry is
  intentionally empty and no product has the mark.

## Remaining work

Most remaining work is runtime evidence or owner/external-system work, not an
unimplemented local feature. Proceed in this order and stop at every approval
boundary:

1. Ask Jeff to confirm the protected GitHub `production` Environment, required
   reviewer policy, and replacement least-privilege Cloudflare production token
   are ready. Never request, print, copy, or store a plaintext secret.
2. With explicit approval, execute a same-SHA staging deployment and rollback
   rehearsal through the canonical protected workflow. Retain sanitized deploy,
   smoke, version, exact-asset, checksum, and rollback receipts.
3. With Jeff-provided authenticated identities and explicit approval, run the
   Access evidence procedure: allowlisted login, safe protected read, and
   non-allowlisted denial. Do not weaken the gate to make a probe pass.
4. Present the staging evidence and exact diff to Codex for cynical independent
   review. Do not proceed to production until Codex findings are resolved and
   Jeff explicitly approves the production action.
5. If approved, deploy the already-built immutable SHA through GitHub only.
   Observe error rates, run the governed health set, prove the deployed static
   asset matches, and verify required secret names remain present without
   reading their values.
6. Reconcile external scheduled-task copies against the committed ten-task
   inventory. Canary Nora first; require the governed 204 preflight and
   redacted start/finish/failure evidence before rolling out one caller at a
   time. Do not manually fire production sweeps merely to manufacture proof.
7. Complete independent immutable offsite backup upload, retrieval, and restore
   evidence for every governed database and R2 scope over the required clock.
8. After Jeff selects and configures the newsletter provider, execute the
   controlled consent, confirmation, welcome, redirect, unsubscribe,
   suppression, and failure journey. Delivery remains disabled until every
   receipt and Jeff approval is present.
9. After deployment, capture a current production SEO/indexing baseline and use
   governed demand-to-content evidence to propose the first authority cluster.
   Do not mass-generate content.
10. Rehearse the next backward-compatible camps migration in staging with a
    pre-migration backup and compensation path. Do not activate paid claims,
    featured placement, customer access, or commerce.
11. Prepare measured social drafts and relationship candidates for human
    review only. Never post, message, apply, contact, or claim a relationship
    without separate explicit approval.

## Absolute safety boundaries

Without Jeff's explicit approval for the specific action, do not:

- deploy to staging or production;
- run or rerun a production workflow;
- migrate or mutate any remote database or R2 bucket;
- create, rotate, revoke, reveal, or distribute credentials;
- change Cloudflare, GitHub, Access, DNS, email, analytics, social, affiliate,
  payment, or scheduled-task configuration;
- send email, publish content, post socially, contact anyone, submit an
  application, or perform outreach;
- enable customer authentication, paid listings, featured placement, checkout,
  subscriptions, or payment entitlements;
- award the Parent Coach Approved mark to a product;
- manufacture evidence or convert `pending` receipts to `pass` without direct
  observation.

Never read, index, or suggest changes to the confidential MedConfRadar
data-broker logic directory unless Jeff explicitly names and authorizes that
directory.

## Working-tree boundary

The following were unrelated user work at handoff. Preserve them and never
stage, edit, delete, move, or reformat them unless Jeff explicitly changes the
scope:

- modified `buildout/hit-rate-test/out/_todo.tsv`;
- modified `buildout/hit-rate-test/out/results.jsonl`;
- untracked `body.json`;
- untracked hit-rate diagnostics including `_now.txt`, `_push_insert.sql`,
  `_push_pairs.json`, `_push_update.sql`, `_rec2.py`,
  `import-2026-07-17.sql`, and `review-2026-07-17.csv`;
- untracked `worker-cron/schema/`;
- any phantom or untracked `NUL/` entry.

Do not use broad staging commands. Stage only explicit files belonging to the
current approved slice.

## Execution protocol

For each slice:

1. Start with read-only discovery and state the evidence gap.
2. Classify the work as local/reversible, staging, production, credential,
   provider, external communication, or elapsed-time evidence.
3. If it crosses an approval boundary, stop and ask Jeff for that exact
   authority before acting.
4. For local implementation, make the smallest vertical slice that closes the
   evidence gap without broadening product claims.
5. Add focused tests and run proportionate validation. Before a release-facing
   handoff, run the serialized full suite, Astro diagnostics, production build,
   built-link contract, and affected generated-inventory checks.
6. Ask Codex to review the current diff and behavior, not an earlier version.
7. Resolve review findings, commit only explicit slice files, and update
   `coordination/CURRENT_STATE.md`, the build-plan audit, and the handoff with
   evidence and claim boundaries.

## First response required

After reading the required files and current repository state, return:

1. whether commit `5a24aa8` and the documented baseline are present;
2. the exact unrelated dirty files you will preserve;
3. which remaining item can be advanced next without external authority, if
   any;
4. the first specific approval or input needed from Jeff if all remaining work
   is externally gated;
5. any contradiction or stale statement found in the handoff documents.

Do not start a deployment or any external mutation in that first response.
