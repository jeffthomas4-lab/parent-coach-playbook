# Parent Coach Desk overnight build handoff

**Prepared:** 2026-07-18  
**Branch:** `main`  
**Scope:** safe local increments from the Parent Coach Playbook business plan,
the overnight implementation brief, and `PCD-CODEX-BUILD-PLAN-v2-2026-07-18.md`.

This handoff does not authorize a deployment, migration, secret change,
scheduled-task update, external message, public post, provider activation, or
production data mutation.

## 1. Implemented and verified locally

- Repository organization: canonical ownership plus an exhaustive registry for
  all 85 tracked root artifacts (`68141ad`, `67504e0`), followed by developer
  entrypoint reconciliation that removes obsolete Pages, Kit fallback, and
  dual-CMS guidance.
- Coaching and trust: evidence-qualified HowTo markup and a public camp
  verification methodology (`cf8f8a0`, `ecb125e`, `6d3ab2a`), plus stable
  Organization/WebSite/author/publisher identifiers across shared Article
  variants while preserving the default-off Person reveal.
- Search and discovery: expanded live-404 contract, fail-closed camp sitemap,
  Worker-first sitemap routing, and trusted local camp filters (`2f85417`,
  `5255939`, `d2b914b`, `cec8d0c`).
- Camps provenance: evidence-grade source requirements and governed review
  state before a listing can be represented as verified (`ddcc887`).
- Demand intelligence: privacy-minimized reporting, retention boundaries, and
  content-coverage opportunity scoring (`a6fe763`, `e7685b5`).
- Affiliate integrity: destination inventory validation and a release gate
  (`371cce5`, `b2461d7`).
- Newsletter and social: fail-closed provider journey proof plus draft-only,
  measured social staging with a human publication gate (`4f5acbd`, `99a6c91`).
- Operations: seasonal zero-write behavior, secure agent-run caller contracts,
  all roster skills wired to the source contract, and the ten-task
  reconciliation inventory (`c47cdcb`, `4cd322d`, `aa3ace7`).
- Trust operations: protected dashboard evidence metrics and an inert,
  source-backed relationship research registry (`a734e0a`, `926da5b`).
- Release engineering: single Worker deployment authority, complete Access
  evidence gate, required secret-name preservation, durable smoke receipts,
  three-day local backup proof validation, and exact deployed static-asset byte
  verification (`b484da7`, `a68765c`, `682bd79`, `f8e1bfb`, `7e0ca6c`,
  `08e6647`).

Latest clean validation before this handoff: 150 test files / 774 tests passed.
The immediately prior attempt failed overall after a Vitest worker exited even
though 147 files completed; that failed attempt is retained rather than counted
as green. The new scheduled-task inventory contract passes 1 file / 2 tests.

## 2. Implemented but awaiting staging or production evidence

- Same-SHA staging and production builds, deployment receipts, version IDs,
  error-rate observation, exact-asset match, secret-name preservation, and
  rollback rehearsal.
- Production camp sitemap population after the Worker-first deployment.
- Authenticated Cloudflare Access allowlisted login, protected admin read, and
  non-allowlisted denial. Anonymous redirect coverage is proven for 38 routes.
- Newsletter consent, confirmation, welcome, unsubscribe, suppression, and
  failure receipts against the selected provider.
- Agent-run token delivery to the external scheduled-task store, one canary,
  and distinct start/finish/failure evidence for each of ten PCD callers.
- Deployed seasonal maintenance behavior for the site and cron Workers.

## 3. Blocked on owner credentials, approval, or external systems

- Repair or replacement of the GitHub production Environment Cloudflare token,
  Environment protections, and any production workflow rerun.
- Worker/cron deployment, credential rotation, runtime secret change, D1
  migration, or schedule reconciliation.
- Provider configuration for newsletter delivery, email authentication, social
  accounts, affiliate applications, and external analytics consoles.
- OpenAI key revocation in the provider dashboard; no plaintext key was read.
- Counsel, insurance, accessibility review, provider contracts, paid listings,
  payments, and customer activation.
- Multi-day offsite immutable backup retrieval/restore and multi-deployment
  operational clocks.

## 4. Next ten highest-value tasks

1. Owner verifies the protected GitHub `production` Environment and replaces
   the invalid least-privilege Cloudflare token without exposing its value.
2. Rehearse same-SHA staging deployment and rollback; retain deploy, smoke,
   version, exact-asset, and rollback receipts.
3. Run the allowed/denied authenticated Access evidence procedure and complete
   the production deployment gate packet.
4. Deploy the approved production artifact through GitHub only, observe error
   rates, and verify required secret names remain present.
5. Reconcile the ten external scheduled-task copies against
   `automation/TASK-RUN-LOG-RECONCILIATION.md`; canary Nora first, then roll out
   one caller at a time.
6. Complete independent immutable offsite backup upload, retrieval, and restore
   proof across the governed databases and R2 scope.
7. Select the newsletter provider configuration and execute the fail-closed
   consent-to-suppression journey before enabling delivery.
8. Run a current production SEO/indexing baseline after deployment, then use
   demand-to-content coverage to select the first evidence-backed authority
   cluster rather than mass-generating content.
9. Rehearse the next backward-compatible camps migration in staging with a
   pre-migration backup and documented compensation path; do not enable paid
   claims or featured placement.
10. Execute the governed distribution loop: prepare measured first-party social
    drafts and relationship research candidates for human review, with no
    automatic posting or outreach.

## Review questions for Claude

1. Does the exact-asset smoke proof remain bound tightly enough to the immutable
   production artifact, or should the proof itself also receive a detached
   checksum inside the artifact receipt?
2. Do the ten scheduled-task mappings match the current external scheduler, and
   are any task copies still using unversioned prompt text?
3. Are the maintenance behaviors task-specific enough to prevent every
   ungated August-November write while preserving the year-round legal watch?
4. Does any locally built trust or verification surface imply endorsement,
   inspection, accreditation, or a commercial relationship beyond its evidence?
5. Before the first protected deployment, is any remaining local control
   missing that could turn credential failure, asset skew, or rollback failure
   into downtime?

## Working-tree boundary

The pre-existing modified `buildout/hit-rate-test/out/_todo.tsv` and
`results.jsonl`, untracked hit-rate diagnostic files, `body.json`, and
`worker-cron/schema/` are unrelated user work and were not staged or changed by
this overnight sequence.
