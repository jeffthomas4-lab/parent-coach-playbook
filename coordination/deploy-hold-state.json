{
  "consecutiveHeldNights": 6,
  "firstHeldDate": "2026-08-10",
  "lastReason": "Held 2026-08-16. Sixth straight night, same blocker every night: DIRTY WORKING TREE. Step 2 forbids pushing from a dirty tree and Dana will not override it unattended. Full dirty list enumerated cleanly tonight (the mount was fast, unlike 08-14): M reports/ops/stale-locks.log, ?? reports/affiliate/DEPLOYED_2026-08-14.md, ?? src/content/articles/when-the-cuts-list-goes-up.md (Ed draft, draft: true). All three are inert. None is a published article, none is code, none would change the site. THE HOLD IS NO LONGER CHEAP. Local main is 4 ahead of origin/main and 1 behind, i.e. DIVERGED. Three draft:false articles published 2026-08-16 by Penny are stranded local-only and are not on origin or the live site: setting-up-the-soccer-center-circle, squaring-soccer-field-corners-with-the-3-4-5-method, your-hungriest-player-needs-a-second-track. Two more are draft:true and not at stake (cross-country-time-trial-varsity-spot 08-17, what-has-to-be-signed-before-football-practice 08-17). SECOND, SEPARATE PROBLEM: WORKERS BUILDS APPEARS STUCK. Production build-info.json still reports commit c40448cd builtAt 2026-08-14T06:31Z, builtBy workers-builds. That is roughly 48 hours stale. origin/main has since advanced to 9c91654d, which is the BabyLoveGrowth article youth-volleyball-drills (articleId 724708, draft: false, publishedAt 2026-08-16) sitting in src/content/articles, a watched path. Workers Builds should have shipped it and has not. That article is therefore almost certainly 404ing on the live site right now while the webhook reports success. This is NOT caused by the dirty tree and clearing the tree will NOT fix it: the volleyball article is already on origin, so nothing Dana pushes changes its fate. Jeff has to look at the Workers Builds deployment history directly. Live-URL spot checks were again refused as out of provenance; the build-info SHA comparison is the evidence used instead, and it is conclusive since production commit c40448cd predates commit 9c91654d. GOOD NEWS: builtBy is still workers-builds, not local, so no rogue local wrangler deploy has run. GATE: applied to origin/main..HEAD, what Dana would actually push, which is content-only and CLEAR (editorial-queue.md, 3 illustrations, 5 articles, plus buildout/hit-rate-test/out data files). Read literally step 5 spans refs/pcd-deployed..HEAD, 55 files including .gitignore, imports/.cache and scratch tmp files, but those reached origin/main via the 2026-08-14 cleanup session, not via Dana, and cannot be unpushed. check-content-field-lengths.mjs --check RAN CLEAN tonight: 1487 entries scanned, BUILD-BREAKING none. The push is ready the moment the tree is clean. Dana did not run wrangler deploy and will not.",
  "lastBranch": "main"
}
