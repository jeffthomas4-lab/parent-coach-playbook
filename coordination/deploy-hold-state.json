{
  "consecutiveHeldNights": 9,
  "firstHeldDate": "2026-08-10",
  "lastReason": "Held 2026-08-26, second run same day. Local main is 2 ahead / 0 behind origin/main (25c50ece): commits d04f29d4 and d9e68816 from this morning's 09:48-09:52 PDT run, both content-only (coordination/deploy-hold-state.json, reports/ops/stale-locks.log), never pushed. Not diverged, so no rebase needed, but Step 2 stopped this run cold anyway: the tree was dirty on arrival. Found coordination/deploy-hold-state.json and reports/ops/stale-locks.log both staged with content that reverted to pre-2026-08-26 versions (deploy-hold-state.json read back the 2026-08-17 'Seventh straight night' narrative even though HEAD already carries this morning's newer entry; stale-locks.log was missing the two safe-commit lines HEAD has from 16:48 and 16:52 UTC, which are the lock-clear lines from this morning's own commits). No explanation found for how the revert happened; it predates this session and was not caused by anything this run did. Flagging it rather than guessing. Also found and cleared via npm run git:unlock: a 2.9h-old .git/HEAD.lock plus two evacuated corpse files (HEAD.lock.stale.4, index.lock.stale.4), logged to reports/ops/stale-locks.log. OPERATIONAL NOTE, NEW THIS RUN: plain `git status` and `git branch`/`git status` combos hang indefinitely on this mount when core.preloadIndex is left at its repo-config default of true — not just slow, genuinely stuck at near-zero CPU for 90s+. Disabling it (`git -c core.preloadIndex=false status`) lets status complete but still takes about 100 seconds for the full 5445-file tree. Worth a permanent core.preloadIndex=false in .git/config so future runs do not have to rediscover this. Reconstructed stale-locks.log by taking HEAD's version and appending this run's 3 new lines rather than committing over the gap, so no history was lost. Rewrote this file fresh rather than re-staging the reverted content. WORKERS BUILDS, UNCHANGED FROM THIS MORNING: build-info.json still reports commit cc4b4d77, builtAt 2026-08-19T04:31:57Z, builtBy workers-builds, buildId 3730e230-a0a3-4648-a91d-fd0858394bc5. Still 7 days stale, still 5 commits behind origin/main. Did not re-run the article-level cat-file/show comparison this pass, so the baseball-throwing-drill and cross-country-time-trial-varsity-spot stranded-article findings from this morning are carried forward, not independently reconfirmed tonight. Did not re-run check-content-field-lengths.mjs since no push is happening. Dana did not run wrangler deploy and will not.",
  "lastBranch": "main"
}
