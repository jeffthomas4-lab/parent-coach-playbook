{
  "consecutiveHeldNights": 10,
  "firstHeldDate": "2026-08-10",
  "lastReason": "Held 2026-08-30. Not a dirty-tree hold this time, a push-credential hold: `git push origin main` failed with `fatal: could not read Username for 'https://github.com': No such device or address` — this Cowork session's sandbox has no GitHub credentials, so nothing can be pushed from here regardless of repo state. Local main is fully synced and ready: fetched origin, found diverged (1 local commit 65f1060b, 1 origin-only BabyLoveGrowth commit b4b0e0c1, both touching disjoint files), and merged them via plumbing (read-tree/commit-tree/update-ref on a throwaway index) after `git merge` and `git stash` both proved unreliable on this mount for a 400+ file dirty tree (stash silently no-op'd with zero output and no new stash entry; merge left a dangling MERGE_HEAD with 'Unable to write index' and needed a manual same-filesystem `mv` into .git/stale-locks/ to clear, since cross-filesystem mv to /tmp fails here the same way rm does). HEAD is now e2a566647c5a462a612e14625903e72af0a9a3f2, 3 commits / 0 behind origin/main, containing 6 draft:false articles (fall-ball-softball-whats-different, is-fall-ball-worth-it, making-it-fun-for-everyone-when-your-own-kid-is-struggling, team-budget-youth-sports, the-kid-on-your-team-who-is-mean-to-your-kid, when-the-cuts-list-goes-up) plus 1 draft:true (swim-practice-group-placement), stranded off origin. check-content-field-lengths.mjs run clean, no build-breaking entries. This range also carries code (scripts/ci-deploy-guard.mjs, src/lib/babylove-growth.ts, worker-cron/src/index.ts, .claude/CLAUDE.md) from prior sessions' commits between refs/pcd-deployed and HEAD, so even with credentials this push would trip the deploy guard and need Jeff's approval trailer before content ships. SEPARATE FINDING, HELD NOT COMMITTED: repo arrived with roughly 413 modified src/content/*.md files plus VOICE-RUBRIC.md, scripts/check-voice-rubric.mjs, and src/styles/global.css, all one coherent, well-documented, dated 2026-08-29 change retiring *emphasis* markup in headlines (a house-style rule going out because it reads as an AI tell), plus one unrelated new file src/pages/api/admin/babylove/reconcile.ts (an admin-gated manual-trigger route for BabyLove reconciliation, uses requireAdmin + requireSameOrigin, no secrets logged). None of this was committed by Dana — it is exactly the 'someone mid-edit' case the gate says to hold and name, not decide on. Committed only the inert bookkeeping dirt found on arrival (buildout/hit-rate-test/out/*, reports/affiliate/*, reports/camps/*, reports/link-health/replacement-queue.json, reports/ops/stale-locks.log) as 42bbe3bc via safe-commit.sh. Also worth flagging: refs/pcd-deployed was already many commits stale on arrival (pointing back near 25c50ece), meaning this exact push-blocked pattern has been recurring across prior sessions, consistent with the mounting streak here. Dana did not run wrangler deploy and did not write an approval trailer.",
  "lastBranch": "main"
}
