Run aborted before staging — repo unreachable via git, not a queue or content problem.

**Approved items found (4), none processed:**
- wrestling-headgear — B003BSRB3U → B07GZX6Y6S (Cliff Keen Youth Signature Headgear)
- hockey-pads-starter — B0CQTM2SBD → B07KFV9MFL (Winnwell Youth Starter Kit) — card at src/content/guides/hockey.md names Bauer, proposed is Winnwell, needs a copy rewrite
- basketball-shoes-youth — B0F14NVK54 → B0F14PHJCY (Nike Team Hustle D 12)
- gymnastics-slippers-youth — B0GC3TTFQN → B0DQYG586R (Capezio Daisy 205C) — three named cards (gymnastics.md, ballet.md, dance.md) describe split-sole canvas construction, proposed item is full-sole leather, needs a copy rewrite

**Blocker:** the mounted Claude Cowork drive is at 100% capacity (100MB free of 237GB). Every `git` command against this repo (status, etc.) hung indefinitely and had to be killed at both 15s and 40s timeouts — confirmed it's not a one-off by retrying. `git --version` works fine, so it's not a broken git install, it's this repo/mount specifically. Found and cleared one stale `.git/HEAD.lock` left over from a prior run, but that did not fix the hang, so the lock wasn't the real cause — the full disk is the likely one.

**What I did NOT do:** no edits to affiliates.json, no content card rewrites, no re-validation in browser, no build/test run, no branch/commit/push, no PR. Nothing in the repo changed. Queue statuses are untouched — all 4 items are still "approved" and ready to pick up next run once the repo is reachable again.

**For Jeff:** two separate things worth a look —
1. Free up space on the drive backing the Claude Cowork mount — it's essentially full, which is likely blocking any git operation in this repo (and maybe others).
2. This repo's `.git` folder has a lot of debris from past sessions (`_stale_*` lock files, `HEAD.lock.bak`, `_deleted_branches_backup/`, `_stray_junk/`, a `filter-repo/` dir) — worth a `git fsck` / cleanup pass once space is freed, since repeated lock contention across agent runs is a sign something upstream keeps interrupting git mid-operation.

Next run: retry staging these same 4 approved items once the disk-space issue is resolved.
