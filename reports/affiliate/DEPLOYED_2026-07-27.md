# Affiliate Link Stage — 2026-07-27 (ALFRED, fourth pass)

## Result: NO STAGING NEEDED — both approved swaps still confirmed live in production. Repo still unsafe to write to; same git hazard as 07-23/07-24, now a fourth consecutive day.

## Approved items found: 2 (unchanged from replacement-queue.json)

1. **soccer-ball-size4** — `B001L3URAS` (Wilson, unavailable) → `B007ZQWLZE` (Champion Sports Retro Soccer Ball, Size 4, $10.99)
   - Re-validated in browser just now: In Stock, $10.99, "ideal for players ages 8 to 12," matches `product_title`. Approval is 6 days old (well past the 72h freshness threshold) — re-validated per the freshness rule, passes.
   - Content check: `src/content/guides/soccer.md` (ages 8–12 card) already reads "Champion Sports Retro soccer ball, size 4" — matches the deployed product, no mismatch.

2. **soccer-shin-guards-ankle-youth** — `B006IXH79A` (Vizari Malaga, buy box lost) → `B00R2VBASG` (Vizari Soccer Shin Guards with Ankle Protection, Small)
   - Re-validated in browser just now: In Stock, $9.01 (was $9.29 at approval — normal Amazon price drift, not a blocker), ankle protection confirmed, 4.6 stars / 1,004 ratings, matches `product_title`. Same 6-day-old approval, re-validated, passes.
   - Content check: no named-product card references this slug anywhere on site. No copy fix needed.

## Why nothing was staged

Both swaps are **already live in `origin/main`**, and have been since roughly 07-22 — confirmed again this run:

- `git show origin/main:src/data/affiliates.json` (via `git log`/`git show`, which don't touch the working tree) shows both slugs already pointing at the approved destinations, `tag=parentcoachpl-20` intact on both.
- `git merge-base --is-ancestor 8bdd2b50 origin/main` → yes. Commit `8bdd2b50` ("Trim footer to Explore/The Desk + inline legal strip; slim homepage...", merged to main as PR #23) bundled in the affiliate-swap edit to `affiliates.json` alongside unrelated homepage work — this is how the swap got to production without ever going through ALFRED's PR flow.
- The `soccer.md` card copy fix is also already on `origin/main`.

So there is no diff left to stage: applying these swaps again would be a no-op. No new branch, commit, or PR was opened.

## Why the repo wasn't touched otherwise

Same hazard flagged on 07-23 and 07-24, present again today:

- `git status` hung for the full 20-second timeout and did not return.
- `.git/worktrees/pcd-main/locked` and `.git/objects/maintenance.lock` are present, plus 20+ dated `*.lock*.done`/`.stale*` marker files spanning 07-23 through today — evidence of repeated lock contention, not a one-time stale lock.
- Given the working tree can't be safely written to, no build/test run, no branch, no commit, no push, no PR this run either. Nothing was edited in `affiliates.json`, `corrections.ts`, or `replacement-queue.json`.

## Needs Jeff's attention (repeating from 07-23/07-24, now day 4)

1. **Queue cleanup**: `reports/link-health/replacement-queue.json` still shows both items `status: "approved"`, but the underlying swaps have been live since ~07-22. Worth flipping both to `"deployed"` by hand once you've confirmed the PR #23 merge actually reached the production Worker (a merge to `main` isn't the same as the protected `production` environment being approved — that part I can't verify without `gh` access this run).
2. **The git lock hazard itself**: four days running now. Something is repeatedly writing lock/stale-marker files into this exact worktree (`pcd-main`) and `git status` hangs outright. Worth finding whatever process runs recurring jobs against this repo and pausing it, since it'll block the next write-needing run (affiliate, book report, or otherwise) the same way.
