4 approved items found. All 4 re-validated live in browser, content cards fixed where needed, swaps applied to affiliates.json, build/test signals green — but the PR could not be pushed. Blocked at the last step on missing GitHub write credentials in this sandbox, not a data or content problem.

**Approved items — all re-validated, applied, held locally on a ready-to-push branch:**

- **wrestling-headgear** — B003BSRB3U → B07GZX6Y6S (Cliff Keen Youth Signature Headgear YE58). Live, In Stock, $35.29, 4.3★/221. Named card at `src/content/guides/wrestling.md` already said "Cliff Keen youth wrestling headgear" — no copy change needed.
- **hockey-pads-starter** — B0CQTM2SBD → B07KFV9MFL (Winnwell Youth Cleansport NXT Hockey Pad and Bag Starter Kit). Live, In Stock, $219.99, 4.4★/172. Named card at `src/content/guides/hockey.md` said "Bauer Prodigy" — rewrote the h3 and description to Winnwell's actual 6-piece kit (shoulder/elbow/shin pads, gloves, pants, bag).
- **basketball-shoes-youth** — B0F14NVK54 → B0F14PHJCY (Nike Unisex Kids' Team Hustle D 12). Live, In Stock, $67.00, 4.6★/844. No named card for this slug — no copy change needed.
- **gymnastics-slippers-youth** — B0GC3TTFQN → B0DQYG586R (Capezio Daisy 205C). Live, In Stock, $17.98, 4.5★/5,186. Three named cards (`gymnastics.md`, `ballet.md`, `dance.md`) all described "split-sole canvas" construction; the Daisy is full-sole leather with a drawstring and elastic. Rewrote all three descriptions (and ballet's "Our take" line, which referenced stretch) to match the actual product. This construction mismatch was already flagged in the queue at approval time and Jeff approved the swap anyway, so the copy fix ships with it.

**Content mismatch check:** ran against all four. Two of four needed rewrites (hockey, gymnastics/ballet/dance triple); both done before the JSON swap, per the runbook order. No slug held for an unresolved mismatch this run.

**Corrections log:** none needed. All four are either named-card swaps where the copy was already fixed in place, or (basketball) a pure link-target swap with no named card — neither case requires a `corrections.ts` entry per the runbook.

**Build + test verification:** `npm run build:production` and `npm test` don't fit inside a single 45-second tool call on this sandbox, so I ran them via a subagent working from a scratch copy on fast local disk (not the slow FUSE-mounted repo). Results:
- `check:content-lengths`: pass — 1,465 entries scanned, 0 build-breaking.
- `build:manifest`: pass — 689 unique URLs across 559 files.
- `build:og:safe`: pass by design (falls back cleanly, no Pillow/brotli in this sandbox).
- `astro build`: never ran to full completion in a single 45-second window — best single-window run rendered 2,661 pages with zero errors before hitting the cutoff. Environmental limitation, not a build failure signal.
- `astro check` (TypeScript/template diagnostics, full-repo): completed clean — 2 pre-existing errors in `src/pages/camps/index.astro`, unrelated to anything touched this run.
- `npm test`: full suite, 197/197 test files, 1,087/1,087 tests, zero failures.

Net: strong evidence the four edits are safe (valid JSON, tag intact on every Amazon destination, type-check clean, full test suite green, thousands of pages prerendered with zero errors) but I cannot report a from-scratch, single-shot green `astro build` because of this sandbox's execution limits. Flagging that gap honestly rather than calling it a pass.

**Staging (blocked):**
- Branch: `pcd/affiliate-swaps-2026-07-31`, built off `origin/main` (`cb80e90a`).
- Commit `591023a8` — the 5 correct files only (`src/data/affiliates.json`, `src/content/guides/hockey.md`, `gymnastics.md`, `ballet.md`, `dance.md`), verified via tree diff, nothing else pulled in.
- `git push -u origin pcd/affiliate-swaps-2026-07-31` failed: `fatal: could not read Username for 'https://github.com': No such device or address`. No `gh` CLI, no `.git-credentials`, no `.netrc`, no credential helper, no `GITHUB_TOKEN`/`GH_TOKEN` in this sandbox. Read access (fetch) works fine; write access has no credentials configured anywhere.
- The branch and commit exist locally only — nothing reached GitHub. No PR was opened (couldn't be, with nothing pushed).

**What this run did NOT do:** did not deploy anything, did not mark anything "deployed," did not advance `lifecycle.json`, did not write-back the queue (left all 4 items `status: "approved"` since nothing actually reached GitHub for Jeff to review — flipping them to "staged" would be inaccurate).

**Repo housekeeping done along the way (non-destructive):** cleared several orphaned `.git` lock files left over from past sessions getting killed mid-operation on this sandbox's 45-second command cap (confirmed via `pgrep` that no live git process held them before touching any). Also found and quarantined 55 stray `*.lock.stale_cleanup*` files sitting inside `refs/heads/` and `refs/remotes/origin/` that were making `git fetch` fail outright — moved them to `.git/quarantine_stale_lock_debris/` rather than deleting (this mount blocks `rm` on some `.git` internals; rename works). Worth a proper `git fsck` / cleanup pass when someone has time — this is the second run in a row (see `DEPLOYED_2026-07-30.md`) where `.git` debris from interrupted sessions caused real friction.

**For Jeff:**
1. This sandbox has no GitHub write credentials configured (`gh` CLI isn't installed, no token, no credential helper). That blocks every future PR-staging run the same way until it's set up. Worth fixing once so this doesn't recur.
2. To ship these 4 swaps now: pull branch `pcd/affiliate-swaps-2026-07-31` (or re-run staging from an environment that has push access) — the commit is ready, just not on GitHub yet.
3. The `.git` directory in this repo keeps accumulating lock debris from sessions that get killed mid-operation. A cleanup pass would help future runs move faster.

Next run: retry push + PR open for `pcd/affiliate-swaps-2026-07-31` (or a fresh equivalent) once credentials exist. The 4 approved queue items are unchanged and still ready.
