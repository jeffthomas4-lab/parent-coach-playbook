# Affiliate Link Stage — 2026-07-24 (ALFRED, third pass)

## Result: NO STAGING NEEDED — both approved swaps still confirmed live in production. Repo not touched this run, same git hazard as 07-23, now worse.

## Approved items found: 2 (unchanged from replacement-queue.json)

1. **soccer-ball-size4** — `B001L3URAS` → `B007ZQWLZE` (Champion Sports Retro Soccer Ball, Size 4)
2. **soccer-shin-guards-ankle-youth** — `B006IXH79A` → `B00R2VBASG` (Vizari Soccer Shin Guards with Ankle Protection, Small)

## What I checked

`origin/main`'s `src/data/affiliates.json` still has both new destinations (`tag=parentcoachpl-20` intact on both) via commit `8bdd2b50` — confirmed an ancestor of `origin/main` with `git merge-base --is-ancestor`. The `soccer.md` guide card also still reads "Champion Sports Retro soccer ball, size 4," matching the deployed product. This lines up with the 07-23 run, which confirmed the same swap live in production by hitting the real `/go/soccer-ball-size4/` and `/go/soccer-shin-guards-ankle-youth/` redirects in a browser. Nothing changed since. No new work for ALFRED on these two slugs.

## Why I didn't touch anything in the repo

Same hazard the 07-23 report flagged, and it has gotten worse, not better:

- `.git/index.lock` was present again at session start. `rm` on it returned "Operation not permitted" (not a normal ownership error). A `mv` off it succeeded once, then a fresh `.git/index.lock` reappeared three minutes later, mid-session, while nothing in my own run touched git.
- `.git/` now has 15+ `*.lock*.done` marker files dated across 07-23 07:25 through 07-24 21:29 (today), including several from earlier this morning and one from minutes ago. Whatever is generating these ran again today, during this session.
- Both `git status` and `git diff` (even scoped to a single file) hang indefinitely on this working tree. Plumbing commands that don't touch the working tree (`git rev-parse`, `git log`, `git show <ref>:<path>`) work fine, so the hazard is specifically around whatever process is contending for the index/working tree, not a dead repo.
- The two files the 07-23 report flagged as at risk (`CONTENT_ROADMAP.md`, `src/content/articles/the-hardest-player-to-coach-is-me.md`) are both present and intact right now.

Given there's live evidence of something actively writing to this exact working tree right now, I made no git commits, no branch, no push, and no edits to `affiliates.json` or `replacement-queue.json` — an uncommitted edit sitting in a repo under active contention is exactly how the CONTENT_ROADMAP.md edit got reverted on 07-23.

## Untouched, and why

- `src/data/affiliates.json` — already correct in production, not edited.
- `reports/link-health/replacement-queue.json` — still shows both items `status: "approved"`, still stale (they're live), left as-is for the same reason as 07-23: can't safely write to this repo right now.
- No branch, no commit, no PR — nothing to stage.

## Needs Jeff's attention

This is the third consecutive day (07-23, and now 07-24) this exact repo has shown signs of a concurrent process fighting over `.git/index.lock`. It's not clearing on its own and it's not a stale one-off: something regenerated the lock during this session, in real time. Worth finding whatever is running against `Outputs/Field and Forge/parent-coach-desk` on a recurring basis (another scheduled task, a sync tool, something left running) and stopping it before the next affiliate run, book report run, or anything else that needs to write to this repo hits the same wall. Once it's clear, `replacement-queue.json` needs both items flipped to `"deployed"` by hand — the underlying work has been live since roughly 07-22.
