# Affiliate Link Deploy — 2026-07-21 (ALFRED)

## Result: ABORTED — build failed, nothing deployed

## Approved items found: 2

1. **soccer-ball-size4** — `B001L3URAS` (Wilson, unavailable) → `B007ZQWLZE` (Champion Sports Retro Soccer Ball, Size 4, $10.99)
   - Approved by Jeff Thomas, 2026-07-21 (same day, no freshness issue).
   - Re-validated in browser: In Stock, product matches `product_title`.
   - Content mismatch check: named-product card on `src/content/guides/soccer.md` already read "Champion Sports Retro soccer ball, size 4" — the copy fix was already in place before approval. No further edit needed. No corrections.ts entry needed.
   - `affiliates.json` edited: destination updated, `tag=parentcoachpl-20` preserved.

2. **soccer-shin-guards-ankle-youth** — `B006IXH79A` (Vizari Malaga, buy box lost) → `B00R2VBASG` (Vizari Soccer Shin Guards with Ankle Protection, Small, $9.29)
   - Approved by Jeff Thomas, 2026-07-21 (same day, no freshness issue).
   - Re-validated in browser: In Stock, product matches `product_title`, ankle protection confirmed in listing.
   - Content mismatch check: no named-product card references this slug anywhere on site, only inline text links. No copy fix or correction needed.
   - `affiliates.json` edited: destination updated, `tag=parentcoachpl-20` preserved.

## Why this run stopped

`npm run build:production` fails in this sandbox at the `astro build` step:

```
Error: Cannot find native binding. npm has a bug related to optional dependencies
Cannot find module '@rolldown/binding-linux-x64-gnu'
```

Root cause: the repo's `node_modules` was installed on Windows (only `@rolldown/binding-win32-x64-msvc` is present under `node_modules/@rolldown`). The Linux sandbox this scheduled task runs in has no matching native binding, so Astro's bundler can't load. This is an environment mismatch, not a problem with the code or the affiliate change — the same build should succeed on Jeff's own machine where `node_modules` was actually installed.

Per the deploy guardrail ("Build before commit, commit before deploy. Abort the whole deploy on a failed build ... never push a red build"), this run stopped here:

- `src/data/affiliates.json` has the two swaps applied and **is left uncommitted** (verified via `git status --short` — only that one file is modified, nothing staged or committed).
- No `git commit`, no `wrangler pages deploy`, no `git push`.
- `reports/link-health/replacement-queue.json` — **left untouched**, both items remain `status: "approved"` so the next successful run can pick them up without re-approval.
- `reports/affiliate/lifecycle.json` — **left untouched** (only updated on successful deploy).

## What Jeff needs to do

Run the deploy from an environment where `node_modules` was actually installed (e.g. his own Windows machine per the normal PowerShell deploy flow), or fix this sandbox's `node_modules` (reinstall for linux-x64-gnu) so this scheduled task can build going forward. The two approved swaps are already staged in `affiliates.json` uncommitted — a plain `npm run build:production && npm test && git add -A && git commit ... && npx wrangler pages deploy dist --project-name parent-coach-playbook --branch main && git push` from a working environment will pick them up as-is.
