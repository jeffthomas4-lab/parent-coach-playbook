# Backlog — captured items, not yet scheduled

## Reads: filter and sort by activity (requested 2026-06-10)

The topic organization on /reads/ stays. Add the ability to search and then sort/filter by activity:

- /reads/ and /reads/[topic]/ get an activity filter (chips or dropdown sourced from SPORTS in site.ts), filtering articles by `sport` frontmatter. "Multi-sport" reads stay visible under an "All activities" default.
- /search/ results get an activity facet and a sort control (newest, topic, activity).
- Implementation note: articles already carry `sport` frontmatter and the /sports/[slug] hubs already aggregate by it, so this is a UI/filter layer, not a data model change. Client-side filtering on the prebuilt lists keeps it static-friendly.

## Parked (decisions made, awaiting scheduling)

- Pinterest pin templates for packing-list and cost pages (approved 2026-06-10, part of weekly schedule revamp)
- Author reveal timing, which now gates the backlink outreach campaign
- 15+ age band wave, track/XC pair, lacrosse-girls parity, tennis/golf pathways (coverage audit punch list items 5-10)
